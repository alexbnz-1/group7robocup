GROUP 7 ROBOCUP PROJECT STATUS
Last updated: 2026-09-21 (Pacific/Auckland)
Repository: C:\Users\alex\Desktop\group7robocup
Current branch: testing
Current committed revision before the XSHUT4 edit: 3617188 - Added bumper on off button

===============================================================================
0. READ THIS FIRST — AUTHORITATIVE RESUME SNAPSHOT
===============================================================================

This section is the current handoff point. Sections 1–34 retain the detailed
development history and evidence, so some early sections mention assignments
that were later replaced. When two statements conflict, use this section,
PlatformIO/include/config.h, PlatformIO/debug_config.json, and the newest
numbered section rather than an older historical statement.

Repository state at this handoff:

- Branch: testing.
- Revision before the current XSHUT4 edit: 3617188.
- Current local changes add the sixth point TOF on XSHUT4 plus its generated
  definition, Wiring Guide migration, diagnostics, tests and documentation.
  They also correct the 8x8 top-down geometry so equal-depth returns make a
  straight boundary rather than a circular arc, and add the JSON-configured
  D21 inductive proximity input with debounced telemetry. The current work also
  adds the front ultrasound board on trigger D24 and echo D14.
- Desktop platform used for testing: Windows.
- Firmware target: Teensy 4.0 using Arduino through PlatformIO.
- Arena dimensions: 2400 mm wide, 4900 mm long, 400 mm walls.
- The complete current file inventory is in section 36.
- The quickest component/file guide is in section 37.

Current hardware assignments:

- CH9143 Bluetooth serial: carrier SERIAL1 / Teensy Serial1, 115200 baud.
- Herkulex DRS-0101 bus: carrier SERIAL2 / Teensy Serial2, 115200 baud.
- First 203 DC motor driver: channel A D27, channel B D26.
- Second 203 DC motor driver / tracked drive bank: channel A D25, channel B
  D15. Negative 100 percent is forward on both fitted drive channels.
- HX12K level-shift outputs: A D33, B D32, C D31, D D30.
- Dual quadrature encoders on Digital Raw 2: encoder 1 D2/D3 and encoder 2
  D4/D5. These pins must not also be enabled as motor outputs.
- Inductive proximity sensor: D21 digital input, configured active-low with
  internal pull-up and 20 ms debounce. The signal at D21 must remain 0–3.3 V.
- Dual ultrasound interface on its 8-pin cable: test mapping channel A trigger
  D14 / echo D24; channel B trigger D22 / echo D20. They are sampled alternately and
  non-blockingly at 100 ms intervals with a 30 ms timeout. Echo inputs must
  remain 0–3.3 V.
- Encoder calibration: encoder 1 is inverted at 0.09094 mm/count; encoder 2 is
  not inverted at 0.09592 mm/count. The 40 mm wheel track is still a supplied
  value that should be physically confirmed.
- Six point TOFs are selected through the SX1509 XSHUT expander: XSHUT1 and
  XSHUT2 long range; XSHUT5, XSHUT0, XSHUT3 and XSHUT4 short range. Their assigned
  I2C addresses start at 0x40. Configuration names live in debug_config.json.
- SEN0628 8x8 TOF: raw I2C1 / Wire1, detected at 0x33.
- BNO055 IMU: I2C1 / Wire1 at 0x28. The driver probes I2C1 first and I2C0 as a
  fallback. It exposes Euler angles, quaternion, linear acceleration, gravity,
  temperature, calibration, system status/error, operating mode and self-test.

Most recent live verification:

- Bluetooth receiver was COM10; direct Teensy programming USB was COM12.
  Windows can renumber both, so identify the port by device description.
- A 12-second read-only COM10 run received 79 telemetry signals evenly and no
  serial, framing or JSON errors.
- BNO055 was available on bus 1, fusion status was running, self-test passed,
  gyro/magnetometer calibration were 3/3, and no active system error existed.
- The 8x8 sensor was valid at 0x33 and delivered 23 frames in that sample.
- Before XSHUT4 was added, all five configured point sensors reported available,
  although bottom_mid_left returned
  zero throughout and bottom_mid_right occasionally returned 8191. Zero, 8191
  and all other values outside 10–3500 mm are shown raw but rejected by the
  arena mapper.
- The offline desktop suite passes all 33 tests.

Newest desktop behavior:

- Arena View uses encoder distance and fused BNO055 heading for local dead
  reckoning. It does not claim absolute localisation.
- Stationary encoder lock suppresses BNO yaw drift while both wheels are still.
- Range spikes must repeat before becoming persistent map evidence.
- Grey means unknown, green measured free, red obstacle endpoint, and purple a
  repeated possible-weight candidate seen by a bottom sensor but not a paired
  upper sensor.
- Grid cells default to 10 mm. Sensor position, angle and top/bottom layer are
  editable graphically and persist in local settings.
- The raw panel below Arena View shows all point readings and all 64 matrix
  zones before mapping filters.
- All 64 matrix zones now contribute to a dense horizontal cone projection.
  Because a floor plan has no vertical axis, the mapper robustly combines the
  eight vertical samples within each of the eight real horizontal columns and
  paints that column's angular sector as a filled wedge. Literal row/column
  data remains visible in the raw table and dedicated 8x8 tab.
- Dashboard arrow keys drive motor bank 2 only after Connect, Debug Mode, Run,
  and explicit Keyboard Drive enable. The GUI sends compact drive heartbeats;
  firmware stops the bank after 500 ms without a heartbeat.

Resume procedure:

1. Read this section, then sections 33–39.
2. Start the GUI with
   Non-Teensy-Files\BluetoothDebugGUI\run_gui.ps1.
3. Restart any already-running GUI after code changes; an old process can both
   hide changes and hold COM10 open.
4. Connect to the CH9143 port at 115200. Confirm the protocol-error counter
   remains zero before enabling motion.
5. Raise the drive tracks clear before the first powered keyboard test.
6. The latest raw-panel/cone changes are desktop-only and require no firmware
   upload. Upload firmware only after changing PlatformIO sources/configuration.
7. The next useful validation is a driven arena run: verify encoder direction,
   map trail direction, turn behavior, 64-zone cone stability and the 500 ms
   keyboard deadman. Record the run for offline comparison.

===============================================================================
1. PURPOSE OF THIS FILE
===============================================================================

This file is the self-contained technical handover for the repository. It
records what exists, how the pieces connect, what has been verified, what is
currently only present in the working tree, and what remains to be integrated
or tested. A new team member should read this file before changing firmware,
wiring, the Bluetooth protocol, or the desktop tools.

The project currently consists of four main areas:

1. Teensy 4.0 firmware managed by PlatformIO.
2. A live Bluetooth/serial robot-debugging GUI.
3. Recording, visualisation, and command-line analysis tools for saved runs.
4. A collection of supplied hardware example sketches used as references.

The newest work makes the debug GUI data-driven. GUI parameter and command
controls are described in one JSON file. The PlatformIO build validates and
embeds that JSON, while a firmware library handles the complete serial workflow.

===============================================================================
2. CURRENT HARDWARE COMMUNICATION LAYOUT
===============================================================================

Controller:

- Teensy 4.0.
- Firmware framework: Arduino through PlatformIO.
- Firmware environment name: teensy40.

Bluetooth debug connection:

- Hardware: supplied, pre-matched CH9143 Bluetooth serial board pair.
- One board plugs into the robot carrier board.
- The other board connects to the computer through USB-C.
- The pair behaves as a transparent serial cable. The desktop software sees a
  conventional Windows COM port and does not implement a separate radio layer.
- Robot-side connector: SERIAL1.
- Teensy UART: Serial1.
- Teensy Serial1 pins: RX1 pin 0 and TX1 pin 1.
- Baud rate: 115200.
- Framing: 8 data bits, no parity, 1 stop bit.
- Application framing: one JSON object followed by a newline.
- During verified testing, Windows identified the receiver as
  "USB-BLE-SERIAL CH9143" on COM10.
- During the same testing, COM7 was the Teensy's direct USB programming port.
- COM numbers are assigned by Windows and may change. Select the device by its
  description instead of relying permanently on COM10.

Herkulex smart-servo connection:

- Bluetooth now occupies SERIAL1.
- The Herkulex bus is therefore configured to use Teensy Serial2.
- The Herkulex cable must be plugged into the carrier board's SERIAL2 connector.
- Servo UART baud rate: 115200.
- Sorting-gate default ID: 1.
- Kicker-cam default ID: 2.
- Accepted position-command range: approximately -160 to +160 degrees.
- Movement time accepted by the debug command: 50 to 2850 ms.
- Startup, STOP, and leaving Debug Mode disable broadcast servo torque.

Important configuration inconsistency to clean up:

- PlatformIO/include/config.h correctly assigns HerkulexConfig::PORT to
  Serial2 and BluetoothConfig::PORT to Serial1.
- The older Pins::SMART_SERVO_RX and Pins::SMART_SERVO_TX constants and nearby
  comments still say pins 0 and 1. Those constants are stale and conflict with
  the current Serial2 assignment. The active workflow uses the HardwareSerial
  object, so the working code is not using those stale constants, but the names
  and comments should be corrected before other firmware starts depending on
  them.

Other configured hardware assignments:

- Drive left PPM: pin 2.
- Drive right PPM: pin 3.
- Reel left PPM: pin 4.
- Reel right PPM: pin 5.
- Front ultrasonic trigger: pin 9.
- Front ultrasonic echo: pin 10.
- Inductive proximity input: pin 11.
- Left analogue IR: A0.
- Right analogue IR: A1.
- TOF sensor bus: Wire / I2C bus 0.
- Colour sensor bus: Wire1 / I2C bus 1.
- SX1509 expander address: 0x3F.
- Runtime-configured point TOF addresses use 0x40 through 0x47.

Several values in config.h are explicitly placeholders. Sorting-gate angles,
kicker angles, sensor counts, XSHUT expander pins, and the analogue IR presence
threshold must be tuned or confirmed on the completed robot.

===============================================================================
3. TEENSY BUILD SYSTEM
===============================================================================

Firmware root:

    PlatformIO\

PlatformIO/platformio.ini configures:

- Platform: teensy.
- Board: teensy40.
- Framework: arduino.
- Serial-monitor speed: 115200.
- Warnings: -Wall and -Wextra.
- Include path: PlatformIO/include.
- A pre-build JSON embedding script.

Declared libraries:

- ArduinoJson 7.4-compatible release.
- Pololu VL53L0X 1.3.1.
- Pololu VL53L1X 1.3.1.
- SparkFun SX1509 IO Expander 3.0.6.
- Local HerkulexTeensy library.
- Local RobotDebug library.
- Local BluetoothDebugWorkflow library.

Known local PlatformIO executable on this development computer:

    C:\Users\alex\.platformio\penv\Scripts\platformio.exe

Build command from the PlatformIO directory:

    C:\Users\alex\.platformio\penv\Scripts\platformio.exe run

Upload command used while the Teensy was COM7:

    C:\Users\alex\.platformio\penv\Scripts\platformio.exe run --target upload --upload-port COM7

The newest JSON-driven refactor compiles successfully. After adding angle
feedback, its measured code size was approximately 56,176 bytes, with ample
Teensy flash and RAM remaining.

Current upload state:

- The JSON-driven workflow, including Set Herkulex angle and Read Herkulex
  angle, has been compiled and flashed successfully to the Teensy on COM7.
- The physical CH9143 link on COM10 advertised all configured controls.
- The latest verification received 17 valid JSON messages, zero invalid lines,
  and passed handshake, definition, state, and telemetry checks.
- The Read Herkulex angle control definition was observed in received data.

===============================================================================
4. CURRENT MAIN PROGRAM
===============================================================================

File:

    PlatformIO/src/main.cpp

The file has deliberately been reduced to construction and two calls. It:

- Creates one BluetoothDebugWorkflow object.
- Passes it the configured Bluetooth and Herkulex UARTs.
- Calls debugGui.begin() once from setup().
- Calls debugGui.update() repeatedly from loop().

There is no protocol parsing, JSON construction, command dispatch, telemetry
formatting, or servo-control logic in main.cpp. This is intentional. Future
normal robot operations can be added around debugGui.update() without moving
the debugging implementation back into main.cpp.

===============================================================================
5. JSON-DRIVEN GUI CONFIGURATION
===============================================================================

Human-edited source file:

    PlatformIO/debug_config.json

Generated build file:

    PlatformIO/include/debug_config.generated.h

Generator:

    PlatformIO/scripts/embed_debug_config.py

Detailed editing guide:

    PlatformIO/DEBUG_GUI_CONFIG.md

Build process:

1. PlatformIO runs embed_debug_config.py before compiling firmware.
2. The script reads debug_config.json using Python's strict JSON parser.
3. Invalid JSON stops the build and reports the editing error early.
4. Valid JSON is compacted.
5. The compact JSON is written into debug_config.generated.h as a C++ string.
6. The BluetoothDebugWorkflow library parses the embedded string during Teensy
   startup.
7. When the desktop GUI connects, the library advertises every configured
   parameter and command.

Do not edit debug_config.generated.h manually. Any manual edit will be replaced
on the next PlatformIO build.

Current JSON parameters:

- debug.telemetry_interval_ms
  - Label: Telemetry interval.
  - Type: integer.
  - Unit: milliseconds.
  - Default: 200 ms, giving 5 telemetry updates per second.
  - Minimum: 50 ms.
  - Maximum: 2000 ms.
  - Step: 50 ms.
  - This parameter is connected to live behavior; changing it changes the
    actual firmware telemetry period.

Current JSON commands:

- ping
  - Action: ping.
  - No arguments.
  - Returns "Pong from Teensy" as a log and sends current state.

- stop
  - Action: stop.
  - No arguments.
  - Broadcasts torque-off to the Herkulex bus.
  - Sets stopped state true.
  - Sends state and a warning log.

- run
  - Label: Run Robot.
  - Action: run.
  - Requires Debug Mode.
  - Clears stopped mode without moving or energising a servo by itself.
  - A relative movement command remains necessary before torque is enabled.

- set_herkulex_angle
  - Label: Set Herkulex angle.
  - Action: herkulex_angle.
  - Requires Debug Mode.
  - Arguments:
      id: integer from 1 through 253, default 1.
      angle_deg: numeric from -160 through +160, default 0.
      move_time_ms: integer from 50 through 2850, default 500.
  - Clears the selected servo's errors, enables its torque, sends the movement,
    updates commanded-position telemetry, and reports running state.

- read_herkulex_angle
  - Label: Read Herkulex angle.
  - Action: herkulex_read_angle.
  - Argument: servo ID from 1 through 253, default 1.
  - Requests position feedback from the selected servo.
  - Publishes a valid result as servo.measured_angle_deg telemetry.
  - Reports an error if no valid response packet is received.
  - The first successful manual read enables automatic reads for that servo ID
    every 200 ms until the Teensy resets.

- zero_herkulex_here
  - Label: Zero Herkulex here.
  - Action: herkulex_zero_here.
  - Argument: servo ID from 1 through 253, default 1.
  - Reads the current physical position without moving the servo.
  - Stores that position as the temporary software-zero offset for that ID.
  - All later movement angles for that ID are interpreted relative to this zero.
  - The offset is held in RAM and is cleared by reset or power loss.
  - Movement is rejected until the selected servo has been zeroed.

What is automatic when editing JSON:

- Adding a parameter object creates a GUI parameter control.
- Removing a parameter object removes that control.
- Numeric parameter changes are validated, clamped to JSON min/max values,
  held in Teensy RAM, and confirmed to the GUI.
- Adding a command object creates a GUI command control.
- Removing a command object removes that control.
- Command arguments generate the appropriate integer/float/boolean/string/enum
  controls supported by the desktop GUI.
- Setting debug_mode_required protects a command until Debug Mode is enabled.
- Selecting an already implemented action connects the GUI command to that
  firmware behavior.

What is not possible from JSON alone:

- JSON can describe a control and select an existing action, but it cannot
  invent new electrical or mechanical behavior.
- A completely new action name requires one matching C++ implementation in
  BluetoothDebugWorkflow.cpp.
- A generic parameter is stored and displayed automatically, but using it in a
  new drivetrain, sensor, or control algorithm requires that algorithm to read
  or bind the value.
- Parameter values are currently temporary and return to JSON defaults after a
  Teensy reset. Persistent EEPROM/configuration storage has not been added.

===============================================================================
6. BLUETOOTH DEBUG WORKFLOW LIBRARY
===============================================================================

Files:

    PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.h
    PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp

Responsibilities:

- Start the Bluetooth UART.
- Start the Herkulex UART.
- place Herkulex servos in safe torque-off state at startup.
- Parse the embedded GUI configuration.
- Process incoming desktop messages.
- Advertise parameter and command definitions.
- Apply and confirm parameter changes.
- Route configured commands to built-in actions.
- Enforce Debug Mode for protected commands.
- Send state, logs, errors, and periodic telemetry.
- Pace definition messages to avoid overflowing the CH9143 radio link.
- Suppress duplicate startup definition bursts.

Incoming computer-to-robot message types handled:

- hello.
- request_definitions.
- command.
- parameter.
- parameter_request.

Outgoing robot-to-computer message types produced:

- telemetry.
- parameter_definition.
- parameter_value.
- command_definition.
- log.
- state.
- error.

Startup behavior:

1. Bluetooth Serial1 opens at 115200.
2. Herkulex Serial2 opens at 115200.
3. Broadcast torque-off is sent to servo ID 0xFE.
4. Embedded JSON is parsed.
5. The configured telemetry interval is loaded.
6. A ready log and safe state are transmitted.

Connection behavior:

1. Desktop sends hello.
2. Firmware sends a connected log and current state.
3. Firmware immediately sends definitions.
4. Desktop also sends request_definitions.
5. Firmware suppresses that request if definitions were sent within the last
   500 ms, avoiding duplicate traffic during radio startup.

The immediate advertisement was added because physical testing showed that the
matched bridge could occasionally deliver hello but lose the next back-to-back
request. Definition packets are separated by 40 ms, and normal telemetry is
limited to 5 Hz by default. This eliminated observed packet corruption without
requiring RTS/CTS wiring.

Current telemetry signals:

- system.uptime_s.
- system.debug_mode.
- system.stopped.
- bluetooth.messages_received.
- servo.last_id.
- servo.commanded_angle_deg.
- servo.measured_angle_deg, after the first successful angle read.
- servo.zeroed.
- servo.zero_offset_deg, after the selected servo has been zeroed.
- servo.auto_read_enabled.
- servo.responding.
- servo.tracking_fault.
- servo.position_error_deg, once a target and measurement are available.

Commanded angle records the most recent movement request. Measured angle records
position feedback returned by the servo and remains absent until a read succeeds.
Once zeroed, both commanded and measured angles are relative to the stored zero.
Absolute targets are checked against the servo's +/-160 degree protocol range.
Automatic monitoring tolerates individual packet losses. Three consecutive
read failures mark the servo unresponsive and set fault state. After a movement
has had its requested duration plus 300 ms to settle, three consecutive readings
more than 8 degrees from target set a tracking fault. Recovery clears the fault.

===============================================================================
7. LOW-LEVEL ROBOT DEBUG SERIAL LIBRARY
===============================================================================

Files:

    PlatformIO/lib/RobotDebug/RobotDebug.h
    PlatformIO/lib/RobotDebug/RobotDebug.cpp

Responsibilities:

- Maintain a fixed receive buffer for serial bytes.
- Reassemble messages that arrive over several UART reads.
- Use newline as the packet boundary.
- Ignore carriage returns.
- Reject lines exceeding 767 bytes.
- Parse JSON with ArduinoJson.
- Reject malformed JSON and non-object JSON values.
- Call the workflow's message handler with an instance context pointer.
- Serialize outgoing JSON followed by a newline.
- Provide helpers for logs, errors, state, and parameter confirmations.

The callback was refactored to carry a context pointer. This allows the reusable
BluetoothDebugWorkflow instance to receive parsed messages without putting
application routing in main.cpp or relying on a global singleton.

===============================================================================
8. HERKULEX LIBRARY
===============================================================================

Files:

    PlatformIO/lib/HerkulexTeensy/HerkulexTeensy.h
    PlatformIO/lib/HerkulexTeensy/HerkulexTeensy.cpp
    PlatformIO/lib/HerkulexTeensy/library.json

Implemented operations:

- Open and close a selected Teensy hardware UART.
- Reboot a servo.
- Clear errors.
- Enable and disable torque.
- Move using a raw position.
- Move using degrees.
- Initialize one servo.
- Initialize all servos.
- Read position.
- Read angle.
- Read status.
- Flush received data.
- Construct Herkulex packets and calculate both checksums.
- Validate response headers, sizes, and checksums.

The angle helper clamps commands to about +/-160 degrees and converts angle to
the Herkulex 0-1023 position representation around centre 512. Movement time is
limited to the protocol/library maximum of approximately 2856 ms.

This library was tested successfully earlier, and physical Herkulex movement
was reported working. The Bluetooth angle command uses this same library.

===============================================================================
9. SENSOR LIBRARIES
===============================================================================

IR library:

    PlatformIO/lib/Ir/Ir.h
    PlatformIO/lib/Ir/Ir.cpp

The project includes an IR abstraction. The configuration notes record that
analogue distance readings were too noisy for dependable ranging, especially
off-angle. The intended use is therefore object-presence detection with a raw
ADC threshold. The current configured threshold is 400 and requires tuning on
the installed sensors.

TOF library:

    PlatformIO/lib/Tof/Tof.h
    PlatformIO/lib/Tof/Tof.cpp

The project includes support structure for Pololu VL53L0X short-range and
VL53L1X long-range sensors, with shutdown lines intended to be controlled by an
SX1509 I/O expander. Address allocation and sensor counts are configured in
config.h. Final physical sensor count, bus layout, expander pins, and mounting
still require confirmation.

Neither sensor library is currently integrated into the minimal main program or
the active Bluetooth telemetry set.

===============================================================================
10. DESKTOP LIVE DEBUG GUI
===============================================================================

Folder:

    Non-Teensy-Files/BluetoothDebugGUI/

Main files:

- DebugGUI.py
  - PyQt6 graphical application.
  - COM-port selection and refresh.
  - Baud-rate selection.
  - Connection status.
  - Dashboard.
  - Live telemetry table.
  - Live plots.
  - Dynamically generated parameter controls.
  - Dynamically generated command controls.
  - Logs.
  - Raw serial view.
  - Recording controls and fault markers.
  - Automatically prefers a port whose description contains CH9143, preventing
    accidental selection of the Teensy USB programming port when both exist.

- BluetoothSerial.py
  - Discovers serial ports using pyserial.
  - Opens the selected COM port in a worker thread.
  - Prevents serial I/O from freezing the GUI.
  - Sends compact newline-delimited JSON.
  - Parses received JSON dictionaries.
  - Routes telemetry, definitions, values, commands, logs, states, and errors
    into Qt signals.
  - Sends hello and request_definitions after connection.

- DataRecorder.py
  - Records a complete live session into SQLite-backed .rdbg files.
  - Writes from a background thread.
  - Uses queued, batched commits.
  - Stores metadata, telemetry, logs, commands, parameter changes, snapshots,
    states, raw serial, faults, and annotations.

- smoke_test.py
  - Opens a specified COM port.
  - Performs a non-actuating hello and definition request.
  - Reads responses for a bounded period.
  - Validates that lines are JSON objects.
  - Requires log, state, parameter-definition, command-definition, and telemetry
    message types for a pass.
  - Discards the first partial line after opening to synchronize with continuous
    telemetry.

- communicationProtocol.md
  - Full protocol design and examples.
  - Explains framing, connection sequence, message types, GUI behavior,
    telemetry timing, safety, validation, and extension conventions.

- setup.ps1
  - Creates a local .venv.
  - Uses the normal Python launcher when available.
  - Falls back to a python executable or PlatformIO's bundled Python.
  - Installs required GUI packages.

- run_gui.ps1
  - Starts DebugGUI.py using the folder's .venv.
  - Produces a clear error if setup has not been run.

- requirements.txt
  - PyQt6.
  - pyqtgraph.
  - pyserial.
  - NumPy.

The GUI environment was created successfully on the current development
computer. PyQt6, its Qt runtime, pyqtgraph, pyserial, NumPy, and supporting
packages installed successfully. Importing BluetoothSerial, DataRecorder, and
DebugGUI was verified successfully.

Launch commands:

    cd C:\Users\alex\Desktop\group7robocup\Non-Teensy-Files\BluetoothDebugGUI
    .\run_gui.ps1

One-time setup if the environment is missing:

    .\setup.ps1

Non-actuating link test, using the currently observed receiver port:

    .\.venv\Scripts\python.exe .\smoke_test.py COM10

===============================================================================
11. VERIFIED BLUETOOTH RESULTS
===============================================================================

Initial failure:

- The robot-side Bluetooth board was physically connected to SERIAL1.
- An early firmware version transmitted debug data through Serial2 to keep the
  former Herkulex assignment untouched.
- The desktop therefore received zero bytes.
- Examination of the supplied matched-board instructions and the stated socket
  selection identified the UART mismatch.

Correction:

- Bluetooth was changed to Serial1.
- Herkulex was moved to Serial2.
- The firmware was rebuilt and flashed.

Successful physical tests:

- One test received 35 valid JSON messages through the physical CH9143 pair.
- It included handshake, state, parameters, commands, and live telemetry.
- A later clean test received 21 valid JSON messages with zero invalid lines.
- After adding the Herkulex GUI control, a burst test exposed malformed and
  missing messages during startup.
- The cause was excessive back-to-back traffic through the transparent link.
- Definition pacing, duplicate-request suppression, and a 200 ms telemetry
  interval were added.
- The final pre-refactor test received 16 valid JSON messages and zero invalid
  lines and reported PASS for handshake, definitions, state, and telemetry.

No actuator movement was performed by the automated link checks. The smoke test
only sends hello and request_definitions.

The JSON-driven refactor is running on the Teensy and has passed the CH9143
smoke test. The angle-read control's advertisement is verified. A physical
feedback read still depends on a powered, responsive servo on SERIAL2.

===============================================================================
12. SAVED RUN RECORDING AND OFFLINE ANALYSIS
===============================================================================

Recording directory:

    Non-Teensy-Files/DataVisualisationAfter/Data/

Known recordings currently present:

- RobotRun_2026-09-05_23-11-40.rdbg
- RobotRun_2026-09-05_23-28-05.rdbg
- RobotRun_2026-09-05_23-49-27.rdbg
- RobotRun_2026-09-06_12-08-13.rdbg
- RobotRun_2026-09-06_12-09-41.rdbg
- RobotRun_2026-09-14_09-14-21.rdbg

The last three appeared as untracked files during the earlier working-tree
inventory. Their contents were not modified during the Bluetooth work.

Offline visualiser:

    Non-Teensy-Files/DataVisualisationAfter/DataVisualiser.py

Features documented in the repository include:

- Browse recordings.
- Display session metadata and summaries.
- Search signals.
- Plot selected signals.
- Safe automatic scaling, including all-zero signals.
- Click-to-inspect cursor.
- Measurement regions.
- Elapsed-time or robot-time axes.
- Fault, command, parameter, and state event markers.
- Compare two recordings.
- Align comparison around a first fault.
- Derived signals.
- Automatic anomaly inspection.
- Raw serial inspection.
- CSV export.

Command-line analysis tool:

    Non-Teensy-Files/DataVisualisationAfter/DataAnalysisCLI.py

Documented commands include:

- summary.
- signals.
- faults.
- events.
- context.
- stats.
- range.
- correlate.
- pid.
- anomalies.
- export-json.
- report.

Workflow documentation:

- Non-Teensy-Files/DataVisualisationAfter/ROBOT_DATA_WORKFLOW.md
- Non-Teensy-Files/ROBOT_DEBUG_TOOLKIT_GUIDE.md

The root requirements.txt contains the broader desktop tool stack: NumPy,
SciPy, Matplotlib, pandas, pyserial, PyQt6, and pyqtgraph. Root setup_env.py can
create a repository-level virtual environment and install those packages.

===============================================================================
13. SUPPLIED EXAMPLE CODE
===============================================================================

The PROVIDED EXAMPLE CODE directory is reference material rather than the
active PlatformIO application. It contains examples for:

- IMU.
- Colour sensor.
- IR XY position.
- Several VL53L0X/VL53L1X TOF layouts.
- Serial mini TOF.
- Eight-sensor matrix lidar, including a supplied archive.
- Analogue IR distance.
- Digital ultrasonic sensor.
- Load cell.
- Encoder.
- Limit switch.
- RFID, including a supplied archive.
- Optical-flow sensor and its readme.
- Herkulex smart-servo basics, blinking, bus scanning, and coordinated moves.
- Stepper motor.
- DC motor.
- Conventional RC servo.
- FastLED smart LED strip.

These examples should be treated as hardware references. They are not all
compiled together and are not proof that every device is integrated into the
current robot firmware.

An experimental HXT12K/HW-399 sketch was created temporarily, then removed when
that work was explicitly shelved. No active HXT12K control remains in the
current firmware.

===============================================================================
14. SAFETY MODEL
===============================================================================

The desktop STOP button is a debugging convenience, not a physical emergency
stop. Wireless data can be delayed, dropped, corrupted, or disconnected.

Current firmware protections:

- Herkulex torque is disabled during workflow startup.
- Leaving Debug Mode disables broadcast servo torque.
- STOP disables broadcast servo torque.
- Herkulex movement requires Debug Mode.
- Servo ID, angle, and movement time are range checked in firmware.
- Parameter values are checked and clamped in firmware.
- Unknown message types, parameters, commands, and action names return errors.
- Oversized or malformed JSON messages are rejected.

Still required at robot level:

- Physical emergency stop or power cutoff.
- Safe actuator power-up procedure.
- Motor-driver enable handling.
- Hardware current limiting.
- Mechanical travel-limit verification.
- Communication-loss timeout for any future continuous motion commands.
- Watchdog strategy.
- Final validation of all placeholder limits.

Always perform first actuator tests with mechanisms clear and the robot lifted
or otherwise restrained. Start with small angle changes and keep a physical
power cutoff within reach.

===============================================================================
15. VERSION-CONTROL STATE
===============================================================================

Branch history shows the following major milestones, newest first:

- 5b5daed: Teensy-to-application communication code.
- acebecb: expanded toolkit functionality and usage documentation.
- 10e0419: bug fixes.
- d284623: visualiser functionality.
- f24f856: debug GUI and post-run analysis tools.
- c6570a2: project restructure.
- 08315ea: requirements and automatic virtual-environment setup.
- 060a296: peer-to-peer Bluetooth debug/testing structure.
- 9d33bf0: Herkulex configuration files.
- 0cef38a: Herkulex Teensy library.
- fb2713c: IR library and branch preparation.
- 83afcd4: example code and initial library migration.

At the time this status was written, the newest JSON-driven refactor was not
committed. Modified tracked files included:

- PlatformIO/lib/RobotDebug/RobotDebug.cpp
- PlatformIO/lib/RobotDebug/RobotDebug.h
- PlatformIO/platformio.ini
- PlatformIO/src/main.cpp

New working-tree files/directories included:

- PlatformIO/debug_config.json
- PlatformIO/include/debug_config.generated.h
- PlatformIO/scripts/embed_debug_config.py
- PlatformIO/lib/BluetoothDebugWorkflow/
- PlatformIO/DEBUG_GUI_CONFIG.md
- status.txt

Preserve these changes when switching branches or cleaning the repository.
Do not use a destructive checkout/reset until the work has been reviewed and
committed.

===============================================================================
16. IMMEDIATE NEXT STEPS
===============================================================================

Required follow-up checks:

1. Launch the GUI and confirm Parameter and Command controls match
   debug_config.json.
2. Press Read Herkulex angle with the powered servo bus on SERIAL2 and confirm
   servo.measured_angle_deg appears.
3. Enter Debug Mode and perform a small, mechanically safe Herkulex movement.
4. Read the angle again and compare measured and commanded values.
5. Press STOP and confirm torque-off behavior.
6. Correct the stale SMART_SERVO pin constants/comments in config.h.
7. Review and commit the JSON-driven workflow as one coherent change.

Recommended next integration work:

- Add actual robot sensor values to Bluetooth telemetry.
- Add explicit APIs for normal robot code to publish telemetry through the
  workflow without exposing protocol details.
- Add parameter-binding helpers so JSON parameters can be connected cleanly to
  PID gains and other runtime variables.
- Add a registration mechanism for project-specific command handlers, keeping
  built-in transport logic separate from robot mechanisms.
- Add measured Herkulex position/status telemetry if bus timing permits.
- Decide which parameters should persist across reset and implement storage.
- Integrate drive, intake, sorter, and sensor subsystems into normal operation.
- Add firmware-side tests for malformed messages, boundaries, and unknown
  actions.
- Add a loopback or mock-stream test for the complete workflow library.

===============================================================================
17. QUICK OPERATING CHECKLIST
===============================================================================

Firmware:

[ ] Bluetooth board plugged into SERIAL1.
[ ] Herkulex bus plugged into SERIAL2.
[ ] Teensy visible over USB for upload.
[ ] Current PlatformIO firmware built and flashed.
[ ] Robot mechanisms clear before enabling Debug Mode.

Desktop:

[ ] CH9143 USB receiver connected.
[ ] Run Non-Teensy-Files\BluetoothDebugGUI\run_gui.ps1.
[ ] Select the port labelled USB-BLE-SERIAL CH9143.
[ ] Select 115200 baud.
[ ] Click Connect.
[ ] Confirm Frames/s is nonzero.
[ ] Confirm Signals/s is nonzero.
[ ] Confirm telemetry rows update.
[ ] Confirm Parameters and Commands populate from debug_config.json.
[ ] Use Ping Teensy before actuator commands.
[ ] Enter Debug Mode only when ready.
[ ] Use a small initial servo movement.
[ ] Press STOP after testing.

Recording:

[ ] Enter a meaningful test name and notes.
[ ] Start recording before the physical test.
[ ] Add a fault marker immediately when an issue is observed.
[ ] Stop recording cleanly.
[ ] Verify the new .rdbg file in DataVisualisationAfter\Data.
[ ] Inspect metadata, telemetry, raw serial, commands, parameters, and faults.

===============================================================================
18. HERKULEX DRS-0101 CONTINUOUS-VELOCITY CONTROL
===============================================================================

Continuous-turn support has now been added to the reusable Herkulex and
Bluetooth debug libraries. The DRS-0101 does not treat infinite rotation as a
position beyond its approximately 320-degree position-control range. It uses a
separate turn/velocity mode. The firmware therefore keeps position commands and
continuous-velocity commands as distinct operations.

The HerkulexTeensy library now exposes moveVelocity(). Its signed speed input is
limited to -1023 through +1023. The magnitude selects the requested drive level;
the sign selects direction. A speed of zero requests no rotation. The outgoing
S_JOG packet sets the DRS turn-mode bit and encodes negative direction with the
protocol's direction flag.

Two JSON-defined GUI commands were added:

- Set continuous velocity: accepts servo ID and signed speed.
- Stop continuous rotation: accepts servo ID, sends zero speed, and removes
  torque.

Set continuous velocity requires Debug Mode and the Run Robot state. The
suggested first test is a low magnitude such as +100 or -100 with the mechanism
clear. The firmware changes modes using the safe sequence required by the servo:
torque off, clear errors, send a zero-speed turn-mode command, torque on, then
send the requested velocity. The position command follows the corresponding
torque-off mode-switch sequence before executing a position move.

The ordinary STOP command also cancels the stored continuous-rotation state and
removes servo torque. While continuous rotation is active, position tracking
fault checks are suspended because a continuously turning shaft does not have a
fixed target. Automatic angle reads may continue, and telemetry now includes:

- servo.continuous_velocity_active
- servo.commanded_velocity

Zero Here is rejected while the selected servo is actively in continuous mode.
Stop Continuous Rotation first, then read/zero or issue position commands.

Recommended continuous-turn test sequence:

1. Ensure the mechanism can rotate freely.
2. Connect the desktop GUI to the CH9143 Bluetooth serial port at 115200 baud.
3. Enter Debug Mode.
4. Press Run Robot.
5. Use Set continuous velocity with speed +100 or -100.
6. Confirm the direction and telemetry.
7. Press Stop continuous rotation before changing operating modes.

Verification completed on 14 September 2026:

- The Teensy 4.0 firmware compiled successfully in PlatformIO.
- The resulting firmware uploaded successfully to the Teensy through COM7.
- The non-actuating COM10 smoke test passed after the GUI released the port. It
  received the Bluetooth handshake, parameter and command definitions, state,
  logs, and telemetry. Both continuous-turn commands were present.
- smoke_test.py now retains partial serial data across timeouts. This prevents a
  slow wireless frame from being incorrectly treated as a complete JSON line.
- The CH9143 link was observed intermittently losing catalogue frames while
  continuing to deliver telemetry. The desktop GUI now loads the shared
  PlatformIO/debug_config.json directly at startup, so parameter and command
  controls are always present. Definitions received wirelessly remain supported
  and update the already-created controls.

===============================================================================
19. 203 DC MOTOR CONTROLLER ON SERIAL3
===============================================================================

Support for the 203_DCMotor controller has been added as a reusable library and
integrated into the Bluetooth debug workflow. Although it is plugged into the
SERIAL3 connector, it does not use UART messages. Its two channels expect
repeating servo-style control pulses. Teensy TX3/pin 14 controls channel A and
RX3/pin 15 controls channel B.

The DcMotor203 library implements the documented pulse range:

- -100 percent: 1050 microseconds, full reverse.
- 0 percent: 1500 microseconds, stopped/neutral.
- +100 percent: 1950 microseconds, full forward.

Intermediate signed percentages are mapped linearly. Both inputs are clamped to
-100 through +100. begin() attaches both pulse outputs and immediately commands
neutral, so startup does not deliberately turn either motor.

The JSON catalogue now creates Set 203 DC motor speed and Stop 203 DC motor
controls. Motion requires Debug Mode and Run Robot. The dedicated motor stop,
global STOP, and leaving Debug Mode all write the 1500-microsecond neutral pulse.
Run Robot only clears stopped mode and never starts the motor itself.

Telemetry fields are:

- dc_motor_203.active
- dc_motor_203.channel_a_percent
- dc_motor_203.channel_b_percent
- dc_motor_203.channel_a_pulse_us
- dc_motor_203.channel_b_pulse_us

The firmware compiled and uploaded successfully to the Teensy through COM7 on
14 September 2026. Physical rotation was not commanded during verification.

The initial integration incorrectly treated the board as a single-channel
controller and drove only TX3/pin 14. Comparison with PROVIDED EXAMPLE
CODE/203_DCMotor/203_DCMotor.ino showed that the supplied controller exposes two
independent servo-pulse channels. This was corrected on 14 September 2026:

- Channel A uses TX3/pin 14.
- Channel B uses RX3/pin 15.
- Two Servo objects are attached with plain attach(pin), matching the example.
- The speed control now accepts channel_a_percent and channel_b_percent.
- Stop and startup write 1500 microseconds to both channels.
- Telemetry reports the percentage and pulse width for each channel separately.

The corrected two-channel firmware compiled and uploaded successfully to COM7.

The controller location was subsequently corrected by the operator: it is on
SERIAL7, not SERIAL3. The final channel mapping is therefore channel A on
TX7/Teensy pin 29 and channel B on RX7/Teensy pin 28. Configuration, direct test
code, and debug-workflow motor code all use these shared corrected pin constants.

For direct bench diagnosis, PlatformIO/src/main.cpp was temporarily changed to
bypass the Bluetooth debug workflow. It creates DcMotor203 directly, holds both
channels at neutral for two seconds during startup, then commands channel A and
channel B to +25 percent continuously. This diagnostic build must not be flashed
unless both motors are mechanically clear. Restore the BluetoothDebugWorkflow
main program after the direct output test is complete.

The first direct-test build was compiled but deliberately not uploaded. After no
motion was reported, main.cpp was changed again to reproduce the supplied
203_DCMotor example exactly on Serial3's two Teensy signals: both channels at
1050 microseconds for 1.5 seconds, both at 1950 for 1.5 seconds, opposite
directions for 1.5 seconds, then both at 1500 neutral for 1.5 seconds. This
sequence repeats and bypasses all debug-GUI logic.

After the direct motor test, main.cpp was restored to the normal minimal
BluetoothDebugWorkflow program. The automatic cycling test is no longer active.
The reusable 203 motor integration and corrected Serial7 pin mapping remain in
place for later diagnosis.

===============================================================================
20. HX12K SERVO THROUGH DIGITAL LEVEL SHIFT
===============================================================================

The CPU and level-shifter diagrams in Parts Summary 2026B-compressed.pdf were
reviewed for this integration. The chosen route is the CPU board's 8-pin DIGITAL
RAW1 header through the 502_Digital_Level_Shift board. DIGITAL RAW1 is labelled
3V, GND, D30, D31, D32, D33, GND, 3V. The firmware uses Teensy pin D30.

The level-shifter power jumper must be in Ex when an external servo supply is
used. The external supply connects to the level-shifter power input with correct
polarity. The servo connects to output D with ground, external supply, and
signal aligned to the board markings. Servo current must not be drawn from a
Teensy 3.3 V pin.

Physical testing confirmed that DIGITAL RAW1 D30 appears on level-shifter output
D, not output A. The firmware pin was already D30 and therefore worked when the
servo was moved to output D. Documentation and source comments were corrected;
no firmware behavior change was needed.

PlatformIO/lib/Hx12kServo now contains a reusable positional-servo library. It
uses the 1000-2000 microsecond range shown by the supplied 204_Servo example and
maps it to the HX12K's approximately 135-degree usable travel. Startup leaves
the PWM output detached, avoiding an unsolicited movement. setAngle(),
setPulseMicroseconds(), centre(), disable(), and status accessors are provided.

The JSON debug catalogue includes Set HX12K servo angles and Disable HX12K
output. Movement requires Debug Mode and Run Robot. Global STOP and leaving
Debug Mode detach the outputs. Telemetry reports enabled state and commanded
angle for each channel. PlatformIO/HX12K_WIRING.md contains the wiring and safe
first-test procedure.

The HX12K integration was expanded to all four Digital Level Shift outputs. The
mapping is A=D33, B=D32, C=D31, and D=D30; D30-to-D was physically confirmed and
the other channels follow the connector's reversed ordering. The Set HX12K servo
angles command has a selection checkbox and independent angle for every output.
Only selected servos receive a new position. Global STOP, leaving Debug Mode,
and Disable HX12K output detach all four channels.

The desktop Commands tab now uses a three-column grid instead of a single
vertical list. Every command card has a star toggle. Favourites are saved using
the application's persistent settings and only starred commands appear on the
Dashboard; un-starring a command removes its dashboard card. An offscreen GUI
test confirmed 12 command cards, three grid columns, and working favourite
add/remove behavior. The multi-servo firmware compiled and uploaded to COM7.

Command cards now display a category badge read from each command's JSON
category field. Current badges are System, Herkulex, HX12K, and 203 DC Motor.
The Dashboard's permanent Emergency / General panel now has a large green RUN
ROBOT button beside the large red STOP ROBOT button. Run uses the existing safe
run command: the connection must be active and firmware still requires Debug
Mode before leaving stopped state. An offscreen GUI test verified all badge
types and the Run/Stop pair. The synchronized catalogue compiled and uploaded
to COM7.

===============================================================================
21. VL53L1X TOF SENSOR ON XSHUT1
===============================================================================

The TOF configuration now matches the fitted hardware: one long-range VL53L1X
on the CPU board's XSHUT1 connector. XSHUT1 is controlled as SX1509 expander
line 1 at I2C address 0x3F. No short-range VL53L0X sensors are currently
configured. The long-range sensor is brought up on Wire at 400 kHz, reassigned
from its default 0x29 address to its runtime port address, and placed into 50 ms continuous mode.

The reusable Tof library no longer halts the entire Teensy when the expander or
sensor cannot be initialised. begin() reports success, each configured sensor
has an availability flag, and invalid or unavailable reads return -1. Timeout
queries also safely handle missing sensors.

BluetoothDebugWorkflow starts the TOF library without adding code to main.cpp.
At every normal telemetry interval it reads the sensor and publishes:

- tof.xshut1.available
- tof.xshut1.timed_out
- tof.xshut1.distance_mm, only when a valid non-timeout reading is available

The JSON command catalogue contains Read VL53L1X now in the TOF category. This
allows an immediate manual read without enabling Debug Mode or Run because the
operation cannot command an actuator. Successful manual reads publish the same
distance telemetry; missing hardware and timeouts produce clear GUI errors.

The embedded JSON catalogue was regenerated by the PlatformIO pre-build script.
The complete Teensy firmware compiled successfully for teensy40. Hardware
detection and distance accuracy remain to be checked with the sensor connected;
the firmware was subsequently uploaded successfully to the Teensy on COM7.
COM10 could not be opened for the non-actuating link check because the running
desktop GUI already owned the port. Reconnect the GUI after the upload so it
requests the new command catalogue and receives the new TOF telemetry fields.

The TOF integration was subsequently generalised into a JSON-configurable
multi-sensor workflow. PlatformIO/debug_config.json now has a top-level
tof_sensors array. Each entry supplies a unique name, an optional descriptive
label, type long for VL53L1X or short for VL53L0X, and a port from XSHUT0 to
XSHUT7. Up to eight sensors are supported. Duplicate names, duplicate ports,
invalid types, invalid ports, and overlong names are rejected with link errors.

At startup all eight XSHUT lines are held low. Listed sensors are enabled one at
a time and assigned an address from 0x40 through 0x47 based on their XSHUT port;
unlisted sensors remain shut down. A sensor that fails initialisation is shut
down again so it cannot collide at the default address while later sensors are
started. Long and short sensor objects can be mixed in any order.

Every configured sensor publishes independent telemetry named
tof.<name>.available, tof.<name>.timed_out, and tof.<name>.distance_mm. The
current XSHUT1 VL53L1X is named front, so its distance is now
tof.front.distance_mm. The Read all TOF sensors now command forces one complete
telemetry packet for every configured device. Adding or removing a TOF requires
only editing the JSON array and rebuilding/uploading; main.cpp and the desktop
GUI remain unchanged. The generalised firmware compiled successfully.
The generalised multi-TOF build was uploaded successfully to the Teensy on
COM7. The GUI must reconnect once to discard the old definitions and request
the updated TOF command and named telemetry.

===============================================================================
22. SEN0628 8X8 MATRIX TOF
===============================================================================

A reusable PlatformIO/lib/Tof8x8 library now wraps DFRobot's official
DFRobot_MatrixLidar driver. The wrapper uses Wire/I2C0 at address 0x33, selects
8x8 matrix ranging mode, reads all 64 row-major distance zones in millimetres,
tracks successful frame count, and exposes safe availability and read status.
A missing sensor does not trap the rest of the robot in the example program's
infinite retry loop.

The official DFRobot_MatrixLidar repository is declared as a PlatformIO
dependency. Point VL53L0X/VL53L1X sensor addresses were moved to 0x40-0x47 so
XSHUT3 can never collide with the matrix sensor's fixed 0x33 address.

BluetoothDebugWorkflow owns and starts the matrix sensor, leaving main.cpp
unchanged. Every 250 ms it sends a tof_8x8 packet containing availability,
frame validity, frame number, timestamp, and the complete 64-value array. These
packets remain visible in Raw Serial and are therefore retained in recordings.

BluetoothSerial recognises tof_8x8 as a first-class message. DebugGUI now has a
dedicated 8x8 TOF tab with X0-X7 columns, Y0-Y7 rows, a millimetre value inside
each cell, near-to-far heatmap colouring, a fixed adjustable maximum distance,
optional automatic colour scaling, frame number, min/max values, last-update
time, and explicit unavailable/invalid-frame status.

PlatformIO/TOF8X8_WIRING.md documents the complete four-wire I2C0 connection,
sensor I2C-mode and 0x33 selector settings, required power cycle, GUI test, and
fault checks. The firmware compiled successfully with the downloaded official
driver. A headless application test created the new tab and verified all 64
cells update from a synthetic frame. Hardware readings remain to be tested.
The complete matrix-enabled firmware was then rebuilt and uploaded successfully
to the Teensy on COM7.

After the first hardware connection reported unavailable, SEN0628 diagnostics
were expanded. Startup now scans all four valid switch-selected addresses,
0x30-0x33, prefers 0x33, and falls back to another responding address. Matrix
packets include the detected address, an acknowledgement mask, and the I2C0 bus
name. The 8x8 TOF tab reports either the working address or exactly which of
0x30-0x33 responded. The diagnostic build compiled and uploaded to COM7.

At the operator's request, 0x52 was added as an additional diagnostic address
and made the preferred SEN0628 address. The normal 0x30-0x33 probes remain as
fallbacks. Matrix packets and the 8x8 tab now report whether 0x52 acknowledged.
The 0x52 diagnostic build compiled and uploaded successfully to COM7.

The SEN0628 startup diagnostic was subsequently expanded from the expected
addresses to a complete scan of usable 7-bit I2C addresses 0x08-0x77. Every
address that acknowledges on I2C0 is now included in each unavailable tof_8x8
packet. The application displays this list as "all bus devices" beside the
existing 0x30-0x33 and 0x52 results. This distinguishes an SEN0628 at an
unexpected address from a cable, power, mode, socket, or whole-bus problem.
The full-scan firmware compiled and uploaded successfully to the Teensy on COM7.

After the full I2C0 scan proved that other devices responded at 0x3F, 0x41,
and 0x42 while the SEN0628 did not acknowledge, the matrix sensor was moved in
firmware to Wire1/I2C1 for an isolated socket and bus test. The packet now names
I2C1, and the application uses the packet's bus field in both unavailable and
live-frame status text instead of assuming I2C0.
The I2C1 diagnostic firmware compiled and uploaded successfully to COM7.

The course-provided 106_TOF_X8.ino and DFRobot_MatrixLidar.zip were then checked
directly. The example constructs DFRobot_MatrixLidar_I2C at address 0x33 on the
default Wire bus. A source comparison found that the supplied driver and the
previously downloaded driver contain identical program text; their file hashes
differed only because of line-ending encoding. The firmware was therefore
returned to Wire/I2C0 and its preferred address restored to 0x33. The project
now vendors the supplied ZIP library locally instead of downloading it.
This restored course-example build compiled and uploaded successfully to COM7.

The physical DIP markings were then reinterpreted after the sensor still did
not acknowledge. The printed 1, 2, and 3 are switch identifiers, while the edge
marked ON indicates the active position. All levers toward the numbers means
all switches are OFF. For the course example's I2C/0x33 configuration, all
three levers must instead face the ON KE edge, followed by a complete sensor
power cycle. The wiring guide was corrected to state this unambiguously.

The operator confirmed the switch configuration and chose to bypass the normal
connector path by plugging the SEN0628 directly into raw I2C1. The firmware was
changed again to Wire1 at the course-example address 0x33. The complete 0x08-
0x77 acknowledgement scan remains active and the application packet identifies
the tested bus as I2C1.
The raw-I2C1 test firmware compiled and uploaded successfully to COM7.

The 8x8 TOF page gained a Raw/Fused view selector. Raw displays the exact 64
sensor measurements. Fused retains the same 8x8 dimensions but combines each
zone with agreeing cardinal neighbours and its previous-frame value using
weighted averaging. Depth-difference gates prevent blending across likely
object boundaries, and invalid measurements are not introduced into valid
zones. Switching the selector immediately redraws the latest received frame.
The updated desktop source passed Python bytecode compilation and repository
whitespace validation.

The earlier fused-grid interpretation was replaced after clarification. The
view selector now offers Raw grid and Smoothed image. Raw grid retains all 64
numeric cells. Smoothed image converts the same 8x8 distance colours into an
enlarged bilinearly interpolated heatmap, producing the continuous blurry-image
appearance of a low-resolution thermal camera without claiming extra measured
spatial resolution.

A third 8x8 view, Stable detail, was added for dependable interpretation. It
maintains five raw frames and filters every physical zone independently using a
temporal median followed by conservative smoothing. Invalid samples can be
covered briefly by recent valid measurements, while changes more than 250 mm
nearer are accepted immediately so a newly appearing obstacle is not hidden by
old history. Farther jumps remain filtered so a single noisy reading cannot
make an obstacle disappear. No neighbouring zones are averaged, preserving measured boundaries.
The filtered map is displayed with the same smooth colour interpolation.

The recording Weight8x8.rdbg was analysed directly for matrix performance. It
contained 263 valid 8x8 frames across 65.20 seconds, averaging 4.02 Hz. The
median frame interval was 0.249 seconds and the longest was 0.319 seconds. A
typical frame contained all 64 valid zones and the minimum was 63. Consecutive
valid measurements had a median absolute change of 0 mm and a 95th percentile
of 72 mm; 2.86 percent changed by more than 250 mm. The scene changed strongly
throughout the test, including an object approaching from roughly 304 mm to
74-91 mm before the final view became broadly uniform near 300-370 mm. The
recording showed healthy acquisition and transport, with the main caution being
occasional large edge/range transitions that benefit from obstacle-safe
temporal filtering.

The 8x8 view selector gained Raw v2. It presents a smoothly enlarged current
frame, colours measurements below 200 mm neutral grey, and removes isolated
spatial spikes. A pixel is replaced by its neighbourhood median only when at
least four valid surrounding pixels agree within 250 mm and the candidate
differs from that median by more than 600 mm. This deliberately retains coherent
near objects, wide changes, and genuine depth edges rather than applying a
blanket blur.

The recording WeightWallTubeWall.rdbg was analysed for the angled black-wall
case. It contained 177 valid matrix frames over 43.79 seconds at 4.02 Hz, with
63-64 valid zones per frame. Normal consecutive changes were small (median 2 mm
and 95th percentile 43 mm), but the dark angled surface caused coherent groups
of upper zones to jump from the approximately 500 mm wall surface to roughly
1500-1900 mm, with occasional transitions above 3000 mm. This is a low-return
or grazing-angle depth failure affecting adjacent zones, not isolated random
noise. The isolated-pixel Raw v2 filter therefore cannot safely remove all of
it; a future surface-consistency/persistence stage is required.

The 8x8 selector gained Dark surface safe. This mode accepts nearer changes
immediately but requires a jump more than 400 mm farther to persist for three
frames before discarding a known nearer surface. It then performs two
conservative surface-repair passes: a far zone is replaced only when at least
three surrounding zones agree within 220 mm and the zone is more than 450 mm
behind their median. The limited passes can repair small coherent dark-surface
holes without flattening large genuine openings. Values below 200 mm use the
same neutral-grey display treatment as Raw v2.

HX12K angle-command responsiveness was hardened after individual GUI commands
sometimes appeared to be missed until the operator clicked repeatedly. The GUI
now sends the idempotent HX12K angle command immediately and repeats it after
60 ms and 120 ms. The servo driver programs the requested pulse width before
attaching the output, so its first emitted pulse is the target rather than the
library's centre default, and attach uses explicit 1000-2000 microsecond bounds.
The Teensy sends updated state immediately after applying an HX12K command so
the application receives prompt confirmation of the commanded channels.
The desktop source passed Python bytecode compilation and the firmware built
successfully. An upload was attempted, but the Teensy programming USB device
was not present; only Bluetooth COM3/COM4 and the CH9143 on COM10 were visible.
The firmware-side first-pulse and confirmation changes therefore remain pending
upload, while the desktop retry behaviour will take effect after restarting the
application.

Malformed incoming JSON was observed when issuing Herkulex movements over the
Bluetooth serial link. The generated position-command JSON is valid, so this
indicates a damaged or truncated wireless serial line rather than invalid GUI
encoding. Set Herkulex angle now receives the same short guarded retransmission
used for HX12K positions: immediate send plus retries at 80 ms and 160 ms. Each
command type has a generation token, so pressing a new target cancels pending
retries of the older target and prevents stale motion. HX12K retries were moved
to this guarded mechanism as well.

Follow-up testing showed the first Herkulex move succeeded but the next command
could fail, demonstrating that automatic repeats were worsening link pressure.
Those repeats were removed. The actual desktop transport faults were corrected:
outgoing JSON now loops until every byte has been accepted and flushes the
serial writer, while incoming bytes are accumulated across read timeouts and
decoded only after a newline arrives. Previously, readline could return a
partial Bluetooth fragment after its 50 ms timeout and the application treated
that fragment as a complete protocol line. A 65536-byte guard prevents an
unterminated corrupt stream from growing memory without limit.

An automated full Qt application reproduction then isolated the remaining
Herkulex failure to connection startup. The GUI sent hello followed immediately
by a duplicate request_definitions. Each request made the Teensy broadcast the
entire catalogue with a transmit flush and 120 ms delay after every definition,
blocking incoming processing for several seconds. Motion commands accumulated
past the Teensy UART receive capacity and subsequently appeared as malformed
JSON. The duplicate desktop request was removed, and the unnecessary per-item
flushes and delays were removed from firmware definition transmission. Hardware
serial writes already apply the required backpressure.

The follow-up Qt reproduction kept telemetry and the event loop healthy but
showed that 57-58 byte zero/read commands arrived while the 91-92 byte position
commands disappeared without acknowledgement. This isolated an additional
CH9143 long-write problem under simultaneous telemetry. Desktop transmission
now divides each newline-terminated JSON message into 20-byte UART writes with
a 3 ms inter-chunk gap, then flushes only after the full line. The Teensy parser
already accumulates until newline, so protocol content is unchanged while each
radio bridge transfer remains modest.

Paced desktop writes did not resolve the length-dependent loss: approximately
58-byte zero/read commands consistently arrived, while 91-92 byte position
commands disappeared during active telemetry. The Teensy Bluetooth UART was
therefore given 2048 bytes of persistent additional receive storage before
Serial1 starts. This prevents long newline-terminated JSON commands from
overflowing the default hardware-serial RX buffer while the main loop is busy
transmitting telemetry or definitions.

The enlarged Bluetooth receive buffer uses the Teensy-specific
HardwareSerialIMXRT interface because the generic Arduino HardwareSerial type
does not expose addMemoryForRead. BluetoothConfig and BluetoothDebugWorkflow
now preserve that concrete Serial1 type, while the Herkulex port remains a
generic hardware serial reference. The corrected firmware builds successfully.
It still needs to be uploaded through the Teensy programming USB connection
before the receive-buffer correction can affect the running robot.

Live testing after that upload confirmed that long Herkulex commands now reach
the Teensy, but exposed a separate outbound bandwidth fault. Telemetry and 8x8
TOF objects were being transmitted quickly enough to overrun the wireless
bridge: captured lines contained missing sections or the start of a TOF object
inside an unfinished telemetry object. RobotDebug now serialises each outbound
object into a complete buffer, transmits it in paced 20-byte chunks, appends its
newline only after the complete object, and flushes the completed line. The
default telemetry and 8x8 frame intervals were changed to 500 ms. This retains
all fields at two updates per second while leaving capacity for reliable
bidirectional commands.

The paced firmware was verified live through COM10 with the full Herkulex
servo 4 sequence: enter debug mode, run, zero at the current position, read the
angle, command -30 degrees, hold for 30 seconds, command +30 degrees, hold for
30 seconds, then repeat both positions and perform a final angle read. Every
zero, read, and movement command was acknowledged immediately. The desktop
event loop completed 1201 heartbeat checks, telemetry stayed continuous with a
worst observed age of approximately 0.51 seconds, the final telemetry age was
approximately 0.21 seconds, and the complete 132-second test ended with zero
protocol errors.

===============================================================================
23. TWO 203 DC MOTOR DRIVERS ON DIGITAL OUTPUTS
===============================================================================

The original 203 DC motor driver was moved from the Serial7-labelled connector
to digital outputs D27 (channel A) and D26 (channel B). The 203 controller does
not use a UART protocol; each channel expects a servo-style 1.05-1.95 ms pulse,
with 1.5 ms neutral. Its existing GUI speed and stop commands retain their
names and now identify the first driver explicitly.

A second independent 203 driver was added on D25 (channel A) and D15 (channel
B), reusing the same DcMotor203 pulse driver. The JSON debug configuration now
advertises separate speed and stop commands for the second driver, with -100%
to +100% speed control for both channels. Telemetry reports each driver's
active state, commanded percentages, and pulse widths independently. Firmware
startup initializes both controllers at neutral; global Stop and leaving Debug
Mode also neutralize both. New motor commands still require Debug Mode and Run.

D15 is also Teensy analogue input A1 and is reserved by the dormant IR_RIGHT
placeholder in config.h. It cannot be wired to that IR sensor while used for
the second motor driver. The firmware builds successfully, but the new pin
assignment has not yet been uploaded or electrically tested.

===============================================================================
24. EDITABLE CPU WIRING GUIDE IN THE DEBUG CONSOLE
===============================================================================

The desktop debug console now has a Wiring Guide tab. Its upper panel draws a
CPU-to-device diagram with one connector/pin card and one connected-device card
per wiring entry; the lower panel contains an editable table with type, device,
CPU connector, CPU pins, and notes. A short form adds sensors, motors, motor
drivers, servos, communications modules, and other devices. The selected row
can be removed. Direct table edits, additions, and removals redraw the diagram
and immediately save the complete list to the application's local QSettings
store. A new app instance restores that saved list, including deletions.

Initial entries reflect the currently configured CPU-facing connections:
Bluetooth on Serial1; Herkulex on Serial2; the two 203 drivers on D27/D26 and
D25/D15; HX12K outputs on D33-D30; point TOF sensors via XSHUT1/XSHUT2; and
the SEN0628 on raw I2C1 at address 0x33. These are editable documentation,
not firmware configuration. Changing a diagram pin does not change Teensy code
or physically move a cable. The full app tab and persistence round trip passed
headless GUI tests.

===============================================================================
25. DIGITAL RAW 2 DUAL QUADRATURE ENCODERS
===============================================================================

The CPU pinout on page 31 of the supplied parts summary shows Digital Raw 2
carrying D5, D4, D3, and D2. The course's 112_Encoder.ino pairs D2/D3 for
encoder 1 and D4/D5 for encoder 2. These assignments are now explicit in
config.h. The older D2-D5 drivetrain and reel constants remain inactive
placeholders and cannot be used concurrently with the encoder board.

The new DualEncoder library wraps Teensy's interrupt-driven Encoder library
and decodes changes on both A and B for each channel. It keeps signed 32-bit
counts, per-sample count differences, and signed counts per second. The
Bluetooth debug workflow starts the pair automatically and publishes all six
values with normal telemetry. A JSON-advertised Zero both encoder counts
command clears both positions and velocity estimates without moving motors.
main.cpp remains unchanged. RPM is deliberately not calculated until the
installed sensor's x4 counts per revolution and shaft location are known.

ENCODER_WIRING.md documents the CPU connector, ground and voltage precautions,
direction convention, and D2-D5 conflict. The Wiring Guide gains a Digital
Raw 2 encoder entry, including a one-time migration that preserves existing
user edits. The Teensy firmware built successfully and three headless Wiring
Guide tests passed. No encoder hardware measurement or firmware upload has
been performed yet.

===============================================================================
26. BNO055 IMU TELEMETRY AND LOCAL ARENA VIEW
===============================================================================

The first operational visualisation now builds a live local-frame top-down map
inside a new Arena View tab of the Bluetooth Debug GUI. Its arena outline uses
the supplied 2400 by 4900 mm dimensions; the physical walls are 400 mm high,
though a top-down view does not display their height. There was no arena brief
file in the project, so these dimensions came directly from the team. The
robot's initial screen position is an arbitrary local origin at the outline's
centre, not an absolute surveyed arena coordinate. The view shows the current
robot orientation, a blue motion trail, current point-TOF/matrix rays, and a
bounded history of surface returns. It is an observation map, not a verified
wall map or autonomous collision-avoidance system.

A new Bno055Imu library checks the provided BNO055 chip identity before use,
looking first on I2C1 at 0x28/0x29 (the stated socket shared with the SEN0628)
and then on I2C0 as a fallback. It configures NDOF fusion and reads heading,
roll, pitch, and system/gyro/accelerometer/magnetometer calibration levels.
The Bluetooth workflow includes these in its ordinary 500 ms telemetry frame,
plus presence and sample-validity flags. A disconnected IMU is retried every
five seconds; three failed reads mark it unavailable. The new code does not
drive any actuator and main.cpp remains only setup/update calls. Firmware was
compiled successfully for Teensy 4.0; IMU hardware response remains to be
verified after upload. The outgoing JSON buffer was expanded from 2048 to
3072 bytes to preserve headroom for the added fields without dropping a long
telemetry frame.

The arena model integrates both signed encoder counts for translation and the
BNO055 heading for rotation. It handles heading wrap at 0/360 and can use
differential encoder turns if a valid wheel-track distance is supplied. It
rejects out-of-range TOF readings and projects only the two centre matrix rows
as a horizontal slice, rather than misrepresenting all 64 depth pixels as
independent ground-plane points. The editable arena dimensions, wheel scale,
wheel track, encoder polarities, point-sensor angles, and estimated matrix FOV
persist through QSettings. Changing mapping geometry resets the local map.
The wheel scale defaults to an explicitly approximate 80 mm circumference /
(663 pulse/revolution times four quadrature edges), about 0.0948 mm/count.
This requires measured-distance verification. The reported wheel-track value
of 40 mm was not used as a default because it appears inconsistent with two
80 mm drive wheels; encoder-only heading fallback stays disabled until a
centre-to-centre track measurement is confirmed.

The GUI's 8x8 tab now has a persistent robot-perspective horizontal flip,
enabled by default. The raw grid column labels and smoothed image mirror
together; Arena View uses the same setting for ray projection. This corrects
the left/right reversal when the robot is viewed from in front. The raw
telemetry keys and physical sensor wiring are not silently renamed, because
the front photo alone does not identify which XSHUT connector belongs to each
visible sensor. PlatformIO build succeeded. Five synthetic arena-model tests,
three existing Wiring Guide tests, an offscreen GUI startup check, and the
orientation-toggle check passed. Physical pose, direction, distance scale,
TOF mounting angles, and visual left/right alignment still need a bench test.
ARENA_VIEW_SETUP.md documents that first-run procedure and limitations.

===============================================================================
27. FIVE-SENSOR WIRING SYNC AND SQUARE FREE/OBSTACLE GRID
===============================================================================

The persisted Wiring Guide was inspected directly. Its current point TOF
entries are long-range top-middle-left on XSHUT1, long-range top-middle-right
on XSHUT2, short-range bottom-middle-left on XSHUT5, short-range bottom-middle-
right on XSHUT0, and short-range bottom-far-right on XSHUT3. The previous
firmware JSON only enabled two sensors and classified XSHUT2 as short-range.
debug_config.json now enables all five ports with the types stated in the
Wiring Guide. After rebuilding and uploading, firmware telemetry names will
be tof.top_mid_left, tof.top_mid_right, tof.bottom_mid_left,
tof.bottom_mid_right, and tof.bottom_right_right. Each has availability,
timeout, and distance fields. The BNO055 on I2C1, the SEN0628 on I2C1, and
both Digital Raw 2 encoders continue supplying heading, depth and motion.

Arena View now joins each configured point sensor to the saved Wiring Guide
entry by XSHUT port. It has individual editable robot-right position, forward
position, and beam angle for each sensor, with persisted settings. The initial
mounting offsets are rough guesses from the guide labels, not measurements;
all five angles initially point forward pending physical alignment. Wiring
Guide edits refresh these GUI descriptions. A newly entered guide port cannot
generate firmware data until debug_config.json also lists it and the Teensy is
rebuilt and uploaded; the map reports such unmatched ports.

The previous endpoint dots have been replaced with a square evidence grid,
initially 100 mm per cell and editable in the GUI. A valid range beam decreases free-space evidence along the measured
path (green) and increases obstacle evidence at the endpoint (red). Unobserved
or balanced cells remain grey. Readings that are unavailable, timed out,
invalid, or outside the accepted range do not colour cells. The 8x8 sensor
contributes its central horizontal slice; the IMU and encoders register these
observations as the robot moves. Colour is only accumulated sensor evidence,
not a certified safe/blocked map. Moving surfaces, dark angled surfaces,
incorrect pose calibration, and uncertain beam mounting can make it wrong.

The Teensy firmware builds successfully with all five point sensors. Twelve
headless GUI/model tests pass, including a synthetic all-five-sensor case,
invalid-sensor rejection, red endpoint / green path, and an offscreen drawing
check. Physical response from the three newly enabled ports and grid alignment
remain unverified until the updated firmware is uploaded and the robot is
tested in front of a known obstacle.

===============================================================================
28. CENTIMETRE MAP, VISUAL SENSOR PLACEMENT, HEIGHT-AWARE CANDIDATES
===============================================================================

Arena View's default occupancy cell size is now 10 mm rather than 100 mm.
The 2400 by 4900 mm outline therefore contains about 117,600 map pixels.
The GUI caches the grey/green/red/purple grid in an image and updates changed
cells instead of drawing every individual square on every frame. Mouse-wheel
zoom, drag-to-pan, and a zoom/pan reset button make 1 cm cells inspectable;
the full-arena overview cannot visually distinguish every cell on a normal
display. A one-time settings migration changes installations still using the
old 100 mm default to 10 mm, preserving other explicitly chosen sizes.

A graphical robot-perspective sensor layout editor was added to Arena View.
It displays all five point TOFs plus the 8x8 module on a schematic body.
Dragging a marker changes its right/left and forward/backward position;
dragging its arrow tip changes beam direction. The matching numeric controls
and a per-sensor top/bottom height-layer selector provide precise edits. The
diagram marks upper sensors with cyan circles and lower sensors with orange
squares. All settings persist in the GUI's local settings store, reset the
current observation map when changed, and are read back on restart. The
diagram body and starting mount coordinates are not measured physical
dimensions. Vertical top/bottom labels no longer imply a different forward
offset; initial top and bottom point beams have the same approximate forward
offset until the user places them correctly. The matrix module's position and
direction now affect how its rays are projected into the arena grid.

Purple cells represent a possible low object or weight candidate. The model
compares a bottom point return with a nearby, similarly aimed top point
return, or a recent matching central-row 8x8 ray when no top point reading
exists. The bottom return must be at least 150 mm nearer than the upper
return on two observations before a cell turns purple. The 150 mm comparison
threshold is editable in the GUI. Missing, timed-out,
stale, or unaligned top readings do not create purple cells. Agreement between
upper and lower readings removes the candidate vote for that endpoint.
This is deliberately a candidate label: different fields of view, reflective
or dark surfaces, occlusion, vibration, and placement errors can produce the
same difference. It must not trigger autonomous intake without field tests.

The 1 cm grid, top/bottom comparison, graphical direction drag, layout
persistence and existing wiring/GUI behaviours passed 18 headless tests.
An offscreen draw benchmark with a 2 m observation took approximately 10 ms
for the first image build and under 1 ms for a cached redraw on this machine.
These are software checks only; the sensor pairing and purple classification
have not yet been validated against real weights or arena walls.

===============================================================================
29. BLUETOOTH RECONNECT DIAGNOSTICS AND TELEMETRY STATE
===============================================================================

The desktop debug GUI uses the computer-side CH9143 USB serial Bluetooth
adapter as a COM port (COM10 during the latest lab test, which may change).
Opening that COM port and receiving actual robot JSON are separate events.
The previous GUI displayed CONNECTED as soon as the port opened, even if no
bytes arrived from the Teensy. This made a dead wireless link look like an
application parser or GUI failure.

During the latest lab-side investigation, direct serial reads from COM10
initially received valid JSON telemetry and 8x8 TOF frames. A reconnect test
also received frames. A later repeated connect test reproduced the user's
failure: the port opened but both GUI attempts had zero frames, and an
independent serial read received zero bytes for several seconds after the GUI
closed. This rules out the GUI parser as the sole explanation for that
specific silent-port episode. It does not identify whether the wireless
pairing, radio power, robot power, or Teensy firmware stopped the data.
The hardware is no longer available for further tests at the time of this
entry, so the underlying physical/link cause remains unconfirmed.

The GUI now distinguishes PORT OPEN — WAITING FOR ROBOT from CONNECTED. If
the COM port is open for more than three seconds without any telemetry, it
shows PORT OPEN — NO ROBOT DATA with a link/power hint. If telemetry was
previously arriving but stops for more than three seconds, it shows PORT
OPEN — ROBOT SILENT. CONNECTED is only shown after valid telemetry arrives.
The status bar and system log likewise say the serial port is open and
waiting, not that the robot is already connected. The Raw Serial tab and
protocol error count remain available to distinguish absent bytes from
malformed or unsupported messages.

The lab-side JSON also exposed an old firmware image: it still emitted only
tof.front and tof.left point sensors and did not emit imu.available or the
new five point-sensor names. A visible GUI warning now identifies this old
front/left telemetry pattern and instructs a current PlatformIO upload.
The warning clears when imu.available is received. Thus a live Bluetooth
connection cannot be mistaken for confirmation that the newer Arena View
firmware has been flashed.

diagnose_live_connection.py is a manual, read-only connection/reconnection
check. It opens the selected COM port, sends only the normal hello handshake,
and prints frame and status snapshots before and after reconnecting; it does
not send motor or servo movement commands. Do not run it while the main GUI
owns the same port. The GUI regression suite now contains an offline test
for waiting, no-data, live, old-firmware warning, and stalled states. All
20 GUI/model tests pass headlessly in the project virtual environment.
No Bluetooth or Teensy connection was attempted after leaving the lab.

On 20 September 2026 the computer-side CH9143 and Teensy were available
again. Before uploading, COM10 passed one reconnect cycle but exposed the old
front/left sensor schema, confirming that the warning correctly identified
the firmware mismatch. COM12 opened but carried no debug JSON, which is
expected because this project sends debug traffic through the configured
Bluetooth UART rather than USB Serial.

The current Teensy 4.0 firmware then compiled and uploaded successfully.
Startup was checked in the source before upload: both 203 motor drivers begin
at their 1500 microsecond neutral signal and all HX12K outputs begin disabled.
The uploaded image used 124248 bytes of code flash, 18808 bytes of initialised
flash data, 28768 bytes of RAM1 variables, and 12416 bytes of RAM2 variables.

After upload, COM10 delivered the current 62-signal telemetry frame without
protocol errors. A ten-second sample observed 18 complete telemetry samples.
All five configured point TOFs were available and not timed out. The SEN0628
delivered 19 valid 64-zone frames in the same sample and reported address
0x33. The BNO055 was available and valid on I2C1 at 0x28. Both encoder counts
and rates remained zero while the robot was stationary; this confirms their
telemetry path, but not their polarity, count scale, or electrical response
to wheel movement.

The BNO055 produced plausible live Euler angles and reported gyro, accel and
mag calibration at 3/3, but system calibration remained 0/3 during a later
twenty-second sample. The calibration-register bit decoding agrees with the
provided 101_IMU example/library convention, so this is currently treated as
the sensor's reported state rather than a desktop parsing error. Arena View
therefore uses the designed encoder-only heading fallback and reports that
IMU-based turn mapping is paused until system calibration reaches at least
1/3. The robot must be physically moved through the BNO055 calibration
motions before this can be evaluated further.

The full GUI was also run offscreen against live COM10 data. Arena View
created 366 green free-space cells and 21 red obstacle cells during the first
connection, then continued to 385 free and 23 obstacle cells after reconnect.
No purple possible-weight cell was present in that static scene. Some live
point readings were below the configured 200 mm minimum or returned 8190 mm;
the map correctly rejected these rather than painting them as obstacles.

Safe command-path tests passed for Ping, Read All TOF, setting and reading
back the 500 ms telemetry interval, and emergency Stop. Stop returned the
expected torque-off log. There were no protocol or malformed-JSON errors.
Five rapid Bluetooth disconnect/reconnect cycles each received 22 messages
and produced no errors. The prior silent reconnect condition did not recur.
The offline GUI/model suite still passes all 20 tests. Remaining physical
tests are encoder direction and scale, measured sensor placement and pointing,
IMU calibration while moving, and actuator motion; none can be proven by a
stationary remote session.

===============================================================================
30. BNO055 FULL FUSION TELEMETRY AND LOCAL DISPLACEMENT
===============================================================================

The BNO055 driver now reads and publishes fused Euler heading/roll/pitch,
quaternion W/X/Y/Z, gravity, linear acceleration, temperature, operation mode,
fusion status, system status/error, built-in self-test result and the four
calibration fields. Live testing found the device at 0x28 on I2C1 in NDOF mode
12. It reported system status 5 (fusion running), self-test result 15 (all four
self-tests passed), stable gravity near 9.8 m/s2, sensible quaternion and Euler
values, and no active system error. The raw SYS_ERR register contained 9, but
the BNO055 defines that register as an active fault only when SYS_STATUS is 1;
with SYS_STATUS 5 it is retained diagnostic data, not a current error. Firmware
now reports both the raw code and imu.system_error_active to prevent ambiguity.

The separate overall calibration field remained 0/3 while gyro, accelerometer
and magnetometer each reported 3/3. Arena View no longer rejects a good heading
solely for that value. It uses the heading whenever the sensor reports that its
fusion engine is running, and falls back to encoder differential heading only
when fusion is unavailable. A live hand-rotation test produced a smooth 69.1
degree heading span and 63.5 degree net change with fusion and self-test healthy.

The BNO055 is not used to double-integrate translational acceleration because
small bias and chassis vibration would create rapidly growing position error.
Local displacement instead combines encoder distance with fused IMU heading.
Arena View now displays local X/Y pose, cumulative wheel travel, encoder-derived
linear speed and fusion health. A later handling test changed encoder 1 by -544
counts and encoder 2 by +645 counts while the IMU measured a 51 degree turn,
showing that both encoder telemetry and heading respond dynamically. The robot's
gearboxes cannot be back-driven for a straight hand-push calibration, so exact
mm/count still requires a controlled powered test. The supplied 40 mm wheel
track is retained as an editable starting value, but it appears unusually small
and should not be treated as measured truth. All 21 navigation/GUI tests passed.

===============================================================================
31. ARROW-KEY DRIVE FOR MOTOR BANK 2
===============================================================================

The Dashboard now contains a Keyboard Drive — motor bank 2 panel with an
adjustable 5–100 percent drive level and an explicit enable checkbox. It only
accepts arrows while connected, in Debug Mode, and in Run state. Up sends
channel A negative and channel B negative, matching left-forward and
right-forward on the fitted drive motor bank. Down sends both positive. Left
sends A positive/B negative and Right sends A negative/B positive. Combined
forward-turn keys stop the inner track and retain the selected power on the
outer track rather than doubling it. Releasing all arrows sends 0/0.

The control disarms and sends neutral when disabled, disconnected, stopped,
leaving Debug Mode, losing application focus, or closing the GUI. While a key
is held, the GUI sends a deadman heartbeat every 150 ms. The matching firmware
stops motor bank 2 if no heartbeat arrives for 500 ms, covering a lost key-up
packet, GUI freeze, cable loss or Bluetooth dropout. Ordinary non-keyboard motor
commands are not given this timeout. The firmware suppresses repeated heartbeat
logs while still accepting the refreshed command.

The direction mapping, diagonal limit and release-to-neutral path pass the
offline suite; all 22 tests pass. The firmware builds successfully. However,
the attempted upload after this change did not reach the Teensy: PlatformIO
printed "No Teensy boards were found" and the USB programming port was absent
from the serial-port list, even though the tool returned a success exit code.
Only COM10 and the Windows Bluetooth ports were present. Therefore the GUI
keyboard controls must not be used until the Teensy programming USB is
reconnected, the firmware is uploaded, and the 500 ms watchdog is verified
with the drive wheels safely raised. The running GUI must also be restarted to
load the new panel.

The programming USB was subsequently reconnected. PlatformIO rebuilt and
uploaded the current firmware without the previous missing-board message, and
the Teensy re-enumerated as USB Serial Device COM12 afterward. The BNO055
changes and keyboard-drive deadman are therefore now installed. The desktop
GUI still needs to be restarted to load its new Keyboard Drive panel. The
first powered arrow-key test should be performed with the chassis securely
supported and both drive wheels clear of the floor so direction and the
500 ms heartbeat timeout can be confirmed before ground driving.

===============================================================================
32. POWERED ENCODER DISTANCE CALIBRATION
===============================================================================

Three consecutive straight-run measurements were supplied as cumulative
encoder counts. The starting reading was encoder 1 -27409 and encoder 2 11242.
After 390 mm the readings were -31712 and 15270, giving deltas -4303/+4028.
After a further 385 mm they were -35931 and 19322, giving -4219/+4052. After
a claimed further 340 mm they were -40097 and 23382, giving -4166/+4060.

The first two runs agree closely in counts per millimetre. The third produced
almost the same count change for 340 mm as the others produced for 385–390 mm,
an approximately 12 percent discrepancy. It is excluded from the primary
scale as a likely distance, stopping-position or wheel-slip outlier. The two
consistent runs combine to 775 mm over 8522 encoder-1 counts and 8080
encoder-2 counts. Arena View now uses separate calibrated scales:

    Encoder 1: 0.09094 mm/count, inverted for forward-positive
    Encoder 2: 0.09592 mm/count, not inverted

Separate scales prevent the roughly 5.5 percent left/right count difference
from appearing as false translation or speed imbalance. The model converts
each wheel delta to millimetres before averaging translation or calculating
encoder-only rotation. Local X/Y displacement, cumulative travel and speed
therefore use the individual calibrations. A one-time settings migration
installs both values and the measured polarities while retaining editable
controls for later refinement. A new asymmetric-scale/polarity regression
test passes; the complete offline suite now passes all 23 tests.

The exact scale should be checked again with a longer taped distance because
the rejected third trial shows meaningful experimental scatter. Wheel track
also remains independently editable and still needs a reliable centre-to-
centre or controlled-turn calibration. The BNO055 remains the preferred turn
heading whenever its fusion engine is running.

===============================================================================
33. LIVE ARENA-MAP AND BLUETOOTH PROTOCOL DIAGNOSIS
===============================================================================

A stationary wall-facing live test exposed two independent causes of the
strange Arena View trails. First, the BNO055 heading moved by 8.75 degrees in
12 seconds while both encoder counts remained exactly unchanged. Applying
that apparent rotation to every range sample painted a fan even though the
chassis had not moved. Arena View now refreshes the IMU baseline but holds the
mapped heading whenever the combined encoder change is below four counts. The
status line explicitly reports when this stationary encoder lock is active.
Real turns continue to use fused BNO055 heading once the encoders show motion.

Second, isolated point-TOF or 8x8-column range spikes were being stored as
permanent free-space rays. Every mapping source now has an independent range
filter. Normal changes pass immediately and accepted values use a short
three-sample median. A jump larger than 150 mm or 35 percent must be repeated
consistently before it is admitted. This rejects a lone long return while
still allowing a genuine newly visible surface after two supporting frames.
It does not alter the raw telemetry or the dedicated 8x8 display.

A later live sample showed one point sensor alternating through 8191 mm and
another returning zero. These are invalid/sentinel readings rather than arena
distances. Point readings outside the configured 10–3500 mm mapping range are
now discarded before the temporal filter, so they cannot poison its baseline,
create a ray, or participate in the purple weight comparison.

Before these changes the stationary evidence occupied columns -74 through 36
and rows 24 through 155, including a long diagonal green scar. With the same
robot stationary after the changes, the occupied bounds reduced to columns
-29 through 34 and rows 24 through 93. The remaining short fan agrees with
the deliberately outward-pointing sensor geometry stored in the layout
editor. Regression tests cover stationary heading suppression and rejection
of a single large range jump.

The protocol-error investigation also found that keyboard driving previously
sent the full generic motor-command JSON approximately every 150 ms. That is
unnecessarily large on the half-duplex Bluetooth serial link while telemetry
and 8x8 frames are being transmitted. The GUI and firmware now use the compact
packet `{\"type\":\"drive\",\"a\":-30,\"b\":-30}` for keyboard heartbeats.
The firmware accepts it only in Debug Mode and Run state, validates both
channels, refreshes the existing 500 ms motor deadman, and avoids repeated log
traffic when the output has not changed. Key release and all GUI safety paths
still send 0/0.

The compact-drive firmware was built and uploaded successfully after the
programming USB reappeared. A six-second COM10 verification then received
continuous telemetry with zero framing or JSON errors: 80 signals, live data
from all five point TOFs, ten valid 8x8 samples at address 0x33, and a running
BNO055 fusion engine with passing self-test. The desktop application must be
restarted to load the map filters and compact keyboard packets. The next
powered driving check should watch the protocol-error counter and confirm the
arena trail remains coherent while turning.

===============================================================================
34. RAW TOF PANEL AND FULL 8X8 ARENA CONE
===============================================================================

Arena View now has a permanently visible Raw TOF readings section directly
below the map. Its point-sensor side lists every configured sensor using its
Wiring Guide label and XSHUT port. It shows the actual incoming millimetres,
OFFLINE, TIMEOUT, or an invalid marker for a value outside the 10–3500 mm map
range. Its matrix side shows all 64 raw X0–X7/Y0–Y7 readings together with the
frame number, I2C bus and responding address. This panel deliberately displays
data before spike rejection, median filtering, or occupancy processing.

The arena projection originally averaged only rows Y3 and Y4 into eight
horizontal readings. Its next version incorrectly distributed Y rows as extra
horizontal bearings, producing a starburst. The corrected projection uses all
eight Y readings to calculate a robust median for each real X column. It needs
at least three valid pixels in a column and two consistent frames before that
column can paint. Each of the eight X columns then paints its complete angular
sector as a filled green wedge with a red endpoint arc. Adjacent wedges form a
continuous cone without inventing bearings for vertical pixels.

Exact values and row/column identities remain available in the raw table and
dedicated 8x8 tab. Invalid zero/8191/out-of-range zones remain visible in raw
data but cannot paint the arena. The suite passes all 28 tests, including
column aggregation, cone width, startup confirmation, raw point display, raw
matrix display and the previous mapping/drive-safety checks.

===============================================================================
35. CURRENT BUILD, RUN, TEST AND RECOVERY COMMANDS
===============================================================================

Desktop setup, from the repository root in PowerShell:

    py -3 .\setup_env.py

The GUI also has its own setup helper:

    cd Non-Teensy-Files\BluetoothDebugGUI
    .\setup.ps1

Run the desktop application:

    cd Non-Teensy-Files\BluetoothDebugGUI
    .\run_gui.ps1

Run the complete offline GUI/mapping suite from the repository root:

    $env:QT_QPA_PLATFORM='offscreen'
    .\.venv\Scripts\python.exe -m unittest discover `
      -s Non-Teensy-Files\BluetoothDebugGUI -p 'test_*.py' -v

Build firmware:

    cd PlatformIO
    C:\Users\alex\.platformio\penv\Scripts\platformio.exe run

Upload firmware with the Teensy programming USB attached:

    C:\Users\alex\.platformio\penv\Scripts\platformio.exe run `
      --target upload --upload-port COM12

COM12 is only the most recently observed programming port. If the Teensy
loader reports that no board was found, check Windows ports and the programming
USB cable instead of trusting an old number. PlatformIO can also trigger the
Teensy loader without a fixed port when the board enumerates normally.

Read-only live diagnostics, from the repository root:

    .\.venv\Scripts\python.exe `
      Non-Teensy-Files\BluetoothDebugGUI\diagnose_live_connection.py COM10

    .\.venv\Scripts\python.exe `
      Non-Teensy-Files\BluetoothDebugGUI\diagnose_sensor_telemetry.py COM10 12000

    .\.venv\Scripts\python.exe `
      Non-Teensy-Files\BluetoothDebugGUI\diagnose_arena_live.py `
      COM10 10000 tmp\arena_live.png

Only one process can own COM10. Disconnect or close the GUI before running a
diagnostic. If a Python process survives without a visible window, inspect it
before starting another console; do not assume Bluetooth has failed merely
because a second process cannot open the port.

Persistent GUI state:

- Favorites, Wiring Guide entries, sensor placement, sensor pointing angles,
  height layers, encoder calibration, grid size, 8x8 mirroring and other view
  options are stored using Qt settings on the Windows user account.
- They do not live entirely in debug_config.json and do not automatically move
  to another computer with the repository.
- Firmware-visible commands, parameters and point-TOF ports do live in
  PlatformIO/debug_config.json. The pre-build script validates and embeds that
  file into PlatformIO/include/debug_config.generated.h.
- Changing debug_config.json therefore requires a firmware rebuild/upload.
- Changing only Python GUI code requires an application restart, not an upload.

Safety/recovery rules:

- STOP, leaving Debug Mode, disconnecting, closing the GUI, disabling keyboard
  drive, or losing the application focus sends/stores safe motor outputs.
- Keyboard motor bank 2 additionally has a 500 ms firmware deadman.
- Always test direction with tracks/wheels clear of the ground.
- HX12K and Herkulex mechanisms can jam mechanically; begin with small moves.
- Arena View is diagnostic dead reckoning, not yet safe autonomous navigation.
- IMU linear acceleration is shown but deliberately not double-integrated into
  position because bias and vibration would cause rapid displacement drift.

===============================================================================
36. COMPLETE PROJECT FILE STRUCTURE AT HANDOFF
===============================================================================

This is the complete project-owned source, configuration, documentation,
recording and supplied-reference tree at 2026-09-20. Directory internals for
.git, Python virtual environments, PlatformIO build output and __pycache__ are
excluded because they are generated machine state rather than project content.
The disposable tmp tree is listed separately after the primary tree.

group7robocup\
|-- .gitignore
|-- .vscode\
|   |-- c_cpp_properties.json
|   |-- extensions.json
|   `-- launch.json
|-- BLUETOOTH_SETUP.md
|-- group7robocup.code-workspace
|-- README.md
|-- requirements.txt
|-- setup_env.py
|-- status.txt
|-- Non-Teensy-Files\
|   |-- ROBOT_DEBUG_TOOLKIT_GUIDE.md
|   |-- BluetoothDebugGUI\
|   |   |-- ArenaView.py
|   |   |-- BluetoothSerial.py
|   |   |-- communicationProtocol.md
|   |   |-- DataRecorder.py
|   |   |-- DebugGUI.py
|   |   |-- WiringGuide.py
|   |   |-- diagnose_arena_live.py
|   |   |-- diagnose_live_connection.py
|   |   |-- diagnose_reconnect_stress.py
|   |   |-- diagnose_safe_commands.py
|   |   |-- diagnose_sensor_telemetry.py
|   |   |-- requirements.txt
|   |   |-- run_gui.ps1
|   |   |-- setup.ps1
|   |   |-- smoke_test.py
|   |   |-- test_arena_view.py
|   |   `-- test_wiring_guide.py
|   `-- DataVisualisationAfter\
|       |-- DataAnalysisCLI.py
|       |-- DataVisualiser.py
|       |-- latest_analysis.md
|       |-- ROBOT_DATA_WORKFLOW.md
|       `-- Data\
|           |-- RobotRun_2026-09-05_23-11-40.rdbg
|           |-- RobotRun_2026-09-05_23-28-05.rdbg
|           |-- RobotRun_2026-09-05_23-49-27.rdbg
|           |-- RobotRun_2026-09-06_12-08-13.rdbg
|           |-- RobotRun_2026-09-06_12-09-41.rdbg
|           |-- RobotRun_2026-09-14_09-14-21.rdbg
|           |-- RobotRun_2026-09-14_11-03-00.rdbg
|           |-- RobotRun_2026-09-14_12-27-25.rdbg
|           |-- RobotRun_2026-09-14_20-35-50.rdbg
|           |-- RobotRun_2026-09-14_20-36-51.rdbg
|           |-- RobotRun_2026-09-14_20-51-27.rdbg
|           |-- RobotRun_2026-09-14_21-55-20.rdbg
|           |-- Weight8x8.rdbg
|           |-- Weight8x8.rdbg-shm
|           |-- Weight8x8.rdbg-wal
|           `-- WeightWallTubeWall.rdbg
|-- PlatformIO\
|   |-- .gitignore
|   |-- .vscode\
|   |   |-- c_cpp_properties.json
|   |   |-- extensions.json
|   |   `-- launch.json
|   |-- ARENA_VIEW_SETUP.md
|   |-- DEBUG_GUI_CONFIG.md
|   |-- ENCODER_WIRING.md
|   |-- HX12K_WIRING.md
|   |-- INDUCTIVE_PROXIMITY_WIRING.md
|   |-- TOF8X8_WIRING.md
|   |-- ULTRASOUND_WIRING.md
|   |-- debug_config.json
|   |-- platformio.ini
|   |-- include\
|   |   |-- README
|   |   |-- config.h
|   |   `-- debug_config.generated.h
|   |-- scripts\
|   |   `-- embed_debug_config.py
|   |-- src\
|   |   `-- main.cpp
|   |-- test\
|   |   `-- README
|   `-- lib\
|       |-- README
|       |-- BluetoothDebugWorkflow\
|       |   |-- BluetoothDebugWorkflow.cpp
|       |   `-- BluetoothDebugWorkflow.h
|       |-- Bno055Imu\
|       |   |-- Bno055Imu.cpp
|       |   `-- Bno055Imu.h
|       |-- DcMotor203\
|       |   |-- DcMotor203.cpp
|       |   `-- DcMotor203.h
|       |-- DigitalInputSensor\
|       |   |-- DigitalInputSensor.cpp
|       |   |-- DigitalInputSensor.h
|       |   `-- library.json
|       |-- DualEncoder\
|       |   |-- DualEncoder.cpp
|       |   `-- DualEncoder.h
|       |-- HerkulexTeensy\
|       |   |-- HerkulexTeensy.cpp
|       |   |-- HerkulexTeensy.h
|       |   `-- library.json
|       |-- Hx12kServo\
|       |   |-- Hx12kServo.cpp
|       |   `-- Hx12kServo.h
|       |-- Ir\
|       |   |-- Ir.cpp
|       |   `-- Ir.h
|       |-- RobotDebug\
|       |   |-- RobotDebug.cpp
|       |   `-- RobotDebug.h
|       |-- Tof\
|       |   |-- Tof.cpp
|       |   `-- Tof.h
|       |-- Tof8x8\
|       |   |-- Tof8x8.cpp
|       |   `-- Tof8x8.h
|       |-- UltrasoundSensor\
|       |   |-- UltrasoundSensor.cpp
|       |   |-- UltrasoundSensor.h
|       |   `-- library.json
|       `-- DFRobot_MatrixLidar\
|           |-- DFRobot_MatrixLidar.cpp
|           |-- DFRobot_MatrixLidar.h
|           |-- LICENCE
|           |-- README.md
|           |-- README_CN.md
|           |-- keywords.txt
|           |-- library.properties
|           |-- examples\
|           |   |-- I2C\
|           |   |   |-- get4_4Data\get4_4Data.ino
|           |   |   |-- get8_8Data\get8_8Data.ino
|           |   |   `-- getFixedPoint\getFixedPoint.ino
|           |   `-- UART\
|           |       |-- get4_4Data\get4_4Data.ino
|           |       |-- get8_8Data\get8_8Data.ino
|           |       `-- getFixedPoint\getFixedPoint.ino
|           `-- python\raspberry\
|               |-- DFRobot_matrixLidar.py
|               |-- README.md
|               |-- README_CN.md
|               `-- examples\
|                   |-- get_all_data.py
|                   `-- get_fixed_point.py
`-- PROVIDED EXAMPLE CODE\
    |-- 101_IMU\101_IMU.ino
    |-- 102_Color\102_Color.ino
    |-- 103_IR_XYPosition\103_IR_XYPosition.ino
    |-- 104__105_TOF\
    |   |-- ContinuousMultipleSensors1_L0\ContinuousMultipleSensors1_L0.ino
    |   |-- ContinuousMultipleSensors1_L1\ContinuousMultipleSensors1_L1.ino
    |   |-- ContinuousMultipleSensors1_L0_L1\ContinuousMultipleSensors1_L0_L1.ino
    |   |-- ContinuousMultipleSensors2_L0\ContinuousMultipleSensors2_L0.ino
    |   |-- ContinuousMultipleSensors2_L1\ContinuousMultipleSensors2_L1.ino
    |   |-- ContinuousMultipleSensors4_L0_L1\ContinuousMultipleSensors4_L0_L1.ino
    |   |-- ContinuousMultipleSensors8_L0\ContinuousMultipleSensors8_L0.ino
    |   `-- ContinuousMultipleSensors8_L1\ContinuousMultipleSensors8.ino
    |-- 106_TOF_SerialMini\106_TOF_SerialMini.ino
    |-- 106_TOF_X8\
    |   |-- 106_TOF_X8.ino
    |   `-- DFRobot_MatrixLidar.zip
    |-- 108_IR_Distance\108_IR_Distance.ino
    |-- 110_UltrasoundDigital\110_UltrasoundDigital.ino
    |-- 111_LoadCell\111_LoadCell.ino
    |-- 112_Encoder\112_Encoder.ino
    |-- 114_LimitSwitch\114_LimitSwitch.ino
    |-- 118_RFIDX\RFIDExample.zip
    |-- 122_OpticalFlow_Sensor\
    |   |-- 122_OpticalFlow_Sensor.ino
    |   `-- ReadMe.txt
    |-- 201_SmartServo\
    |   |-- 201_SmartServo.ino
    |   |-- Blink\Blink.ino
    |   |-- BusScanner\BusScanner.ino
    |   `-- CoordinatedMove\CoordinatedMove.ino
    |-- 202_StepperMotor\202_StepperMotor.ino
    |-- 203_DCMotor\203_DCMotor.ino
    |-- 204_Servo\ServoMove.ino
    `-- 302_SmartLEDStrip\FastLEDCylon.ino

Generated/local directories intentionally excluded from the primary tree:

- .git\ — repository object database and worktree metadata.
- .venv\ — root Python virtual environment.
- Non-Teensy-Files\BluetoothDebugGUI\.venv\ — GUI-local Python environment.
- PlatformIO\.pio\ — downloaded packages and firmware build products.
- __pycache__\ directories — compiled Python bytecode.

Disposable diagnostic/reference extraction tree currently present:

tmp\
|-- analyse_tof8x8.py
|-- arena_live.png
|-- arena_raw_cone.png
|-- arena_wall.png
|-- arena_wall_working.png
|-- arena_wall_filtered.png
|-- herkulex_gui_repro.py
|-- herkulex_live_repro.py
|-- herkulex_live_repro.log
|-- pdfs\
|   |-- page13.png
|   |-- page31.png
|   |-- page31-31.png
|   |-- page31-i2cmotor.png
|   |-- page42-42.png
|   |-- page43-43.png
|   |-- page49.png
|   |-- page66-66.png
|   |-- page66-board-detail.png
|   |-- page66-levelshift-crop.png
|   |-- page67-67.png
|   `-- page68-68.png
`-- provided_matrixlidar\DFRobot_MatrixLidar-master\
    |-- DFRobot_MatrixLidar.cpp
    |-- DFRobot_MatrixLidar.h
    |-- LICENCE
    |-- README.md
    |-- README_CN.md
    |-- keywords.txt
    |-- library.properties
    |-- examples\I2C\get4_4Data\get4_4Data.ino
    |-- examples\I2C\get8_8Data\get8_8Data.ino
    |-- examples\I2C\getFixedPoint\getFixedPoint.ino
    |-- examples\UART\get4_4Data\get4_4Data.ino
    |-- examples\UART\get8_8Data\get8_8Data.ino
    |-- examples\UART\getFixedPoint\getFixedPoint.ino
    |-- python\raspberry\DFRobot_matrixLidar.py
    |-- python\raspberry\README.md
    |-- python\raspberry\README_CN.md
    |-- python\raspberry\examples\get_all_data.py
    `-- python\raspberry\examples\get_fixed_point.py

===============================================================================
37. FILE RESPONSIBILITY AND DEPENDENCY GUIDE
===============================================================================

Firmware entry/configuration:

- PlatformIO/src/main.cpp intentionally only constructs BluetoothDebugWorkflow,
  calls begin() in setup(), and update() in loop(). Keep it uncluttered.
- PlatformIO/include/config.h is the authoritative compile-time pin/UART/I2C
  assignment file.
- PlatformIO/debug_config.json is the human-edited list of GUI commands,
  parameters, digital inputs, ultrasound sensors and point-TOF definitions.
- PlatformIO/scripts/embed_debug_config.py validates/compacts that JSON before
  every build and writes PlatformIO/include/debug_config.generated.h.
- PlatformIO/platformio.ini selects Teensy 4.0, dependencies and the pre-build
  embedding step.

Firmware orchestration/protocol:

- BluetoothDebugWorkflow owns all devices, parses actions, enforces Debug/Run/
  STOP rules, publishes telemetry, sends 8x8 frames and services the compact
  keyboard-drive heartbeat.
- RobotDebug provides newline-delimited JSON receive/send primitives.
- Bluetooth serial uses a 2048-byte added receive buffer to prevent long JSON
  commands being truncated while telemetry is transmitted.

Firmware device libraries:

- Bno055Imu probes Wire1 then Wire and publishes full fusion/diagnostic state.
- DcMotor203 generates the 1.05–1.95 ms servo-style motor-driver pulses with
  1.50 ms neutral and supports forward/reverse percentages.
- DigitalInputSensor provides configurable polarity, pull-up and debounce while
  retaining both logical detection and raw-level diagnostics.
- DualEncoder wraps Encoder.h for two x4 quadrature channels and reports count,
  delta and counts/second.
- HerkulexTeensy implements packet protocol, angle read/write, zero-relative
  behavior, torque control and continuous-velocity commands.
- Hx12kServo outputs standard pulse control on level-shift pins A–D.
- Tof manages VL53L0X/VL53L1X point sensors using SX1509-controlled XSHUT and
  unique runtime addresses.
- Tof8x8 wraps the supplied DFRobot MatrixLidar library, uses Wire1, scans the
  supported addresses and returns complete row-major 64-zone frames.
- UltrasoundSensor performs timed trigger/echo ranging without blocking the
  Bluetooth and actuator service loop while waiting for an echo.
- DFRobot_MatrixLidar is the provided third-party SEN0628 driver copied into
  the PlatformIO project together with its license/examples.
- Ir exists for analogue IR presence sensing but is not the primary arena range
  source; the fitted D15 conflict also prevents right IR while motor bank 2 is
  connected there.

Desktop live application:

- DebugGUI.py constructs the main window, serial connection controls, dashboard,
  plots, parameters, command cards, logs, raw serial, 8x8 views, favorites,
  keyboard drive, recording, Wiring Guide and Arena View integration.
- BluetoothSerial.py owns the threaded COM transport, newline JSON parsing,
  Qt signals and port enumeration.
- DataRecorder.py writes .rdbg SQLite recordings with metadata, telemetry,
  events and commands.
- WiringGuide.py provides the editable/persistent CPU-centric connection table
  and diagram. Arena sensor names/ports are joined to these saved entries.
- ArenaView.py contains encoder/IMU odometry, the evidence grid, sensor layout
  editor, top/bottom comparison, raw TOF panel and 64-zone cone projection.
- communicationProtocol.md documents the serial JSON formats.
- run_gui.ps1 launches DebugGUI.py using the GUI-local environment; setup.ps1
  creates that environment and installs requirements.

Diagnostics/tests:

- diagnose_live_connection.py checks whether bytes/frames arrive without
  commanding actuators.
- diagnose_sensor_telemetry.py summarizes live IMU, encoders, point TOFs and
  matrix spans without motion commands.
- diagnose_arena_live.py uses production GUI/model code headlessly and saves a
  screenshot plus pose/cell summary.
- diagnose_reconnect_stress.py exercises repeated connection handling.
- diagnose_safe_commands.py is for explicit safe command-path checks; inspect
  it before use whenever hardware can move.
- smoke_test.py performs basic application/module checks.
- test_arena_view.py covers mapping, calibration, stationary yaw lock, spike/
  sentinel rejection, robust 8x8 column sectors, raw panel, sensor editing,
  connection state and keyboard direction mapping.
- test_wiring_guide.py covers tab creation, persistence and schema migration.

Offline recordings/analysis:

- DataVisualisationAfter/DataVisualiser.py is the graphical saved-run viewer.
- DataVisualisationAfter/DataAnalysisCLI.py produces textual/statistical run
  analysis suitable for repeatable diagnostics.
- ROBOT_DATA_WORKFLOW.md explains capture and analysis.
- latest_analysis.md contains the most recent written interpretation.
- Data/ contains the SQLite .rdbg recordings. Weight8x8.rdbg-wal and -shm are
  SQLite sidecar files and should stay with the database if it was not cleanly
  closed.

Documentation/reference priority:

1. This status file, especially sections 0 and 33–39.
2. Active source/configuration files.
3. ARENA_VIEW_SETUP.md, DEBUG_GUI_CONFIG.md, ENCODER_WIRING.md,
   HX12K_WIRING.md, TOF8X8_WIRING.md and BLUETOOTH_SETUP.md.
4. Supplied example sketches for hardware-specific comparison.
5. The generic repository README is still the original GitLab template and is
   not an accurate technical handoff.

===============================================================================
38. MATRIX STARBURST CORRECTION
===============================================================================

A live screenshot exposed that treating all 64 matrix zones as independent
horizontal rays was geometrically wrong. Y0–Y7 represent vertical field of
view, not eight sub-bearings inside an X sector. It created many long narrow
green rays, scattered red dots and nearly 8,000 persistent cells from a
stationary robot. The raw frame in that screenshot was approximately 160–214
mm while the configured mapping minimum was 200 mm, so most of the visible
long-range evidence could not describe the current scene.

The matrix mapper now works column-by-column. It filters every raw value to the
10–3500 mm range, requires at least three valid Y pixels in an X column, uses
their median distance, and requires that median to agree across two frames
before mapping it. A valid X column paints its entire one-eighth share of the
configured horizontal FOV as a filled sector: green free cells up to the
measurement and a red arc at the measured distance. Eight sectors form a
continuous cone. Current-ray overlays are limited to the two sector boundaries
and centreline rather than 64 invented bearings.

The later user-requested lower bound is 10 mm, not 200 mm. This keeps a zero
sentinel invalid while allowing the screenshot's 160–214 mm readings to paint
a short, filled cone. Restarting the GUI or pressing Reset local map removes
the already-stored starburst. This correction is desktop-only and needs no
Teensy upload. All 28 tests passed at that stage.

The lower-limit edit was checked. ArenaView.py uses 10 mm rather than literal
zero so an invalid zero return cannot paint the map; legitimate close returns
from 10 mm upward are retained. This is the intended current behavior.

===============================================================================
39. PAIRED HX12K BUMPER SERVO COMMAND
===============================================================================

The Commands page now receives one Bumper servos card in the HX12K category.
It contains two fixed-action buttons in the same block and can also be starred
onto the Dashboard as one card:

- BUMPERS ON commands level-shifter output C / D31 to 0 degrees and output D /
  D30 to 130 degrees.
- BUMPERS OFF commands C / D31 to 130 degrees and D / D30 to 0 degrees.

Both buttons send the same `set_bumper_servos` command with a Boolean enabled
argument. The firmware action is `hx12k_bumpers`. It requires Debug Mode and
Run state and rejects missing/non-Boolean arguments. STOP or leaving Debug Mode
still detaches all HX12K outputs. The generic command-card renderer now supports
a `buttons` array so multiple fixed-argument buttons can share one card.

The complete desktop suite passes all 29 tests, including a check that both
buttons appear in one widget and send the correct arguments. PlatformIO builds
the Teensy 4.0 firmware successfully; code uses 127704 bytes of flash code and
29824 bytes of RAM1 variables. The warnings are in the Teensy framework and
supplied MatrixLidar driver, not the new bumper handler. This firmware change
must be uploaded before the buttons can operate the real servos.

===============================================================================
40. SIXTH POINT TOF ON XSHUT4
===============================================================================

XSHUT4 is now configured as the sixth point sensor. Its firmware telemetry name
is `bottom_left_left`, its type is short range, and its default descriptive
label is Bottom far-left short-range TOF. The Wiring Guide display name is
TOF Short Bottom Left Left. Existing custom Wiring Guide entries are preserved:
the migration only inserts this default row when no XSHUT4 connector already
exists.

Adding the sensor to PlatformIO/debug_config.json automatically makes the
Bluetooth workflow initialise it, assign the next address in the 0x40–0x47
range, read it with normal telemetry, and expose `tof.bottom_left_left.*`.
Arena View then discovers it from the same configuration and includes it in
the raw readout, layout editor, top/bottom layer logic and evidence grid. The
live diagnostic summary also knows the new telemetry name.

The firmware compiles successfully with six point sensors. The desktop suite
passes all 29 tests, including the exact XSHUT0/1/2/3/4/5 configuration and an
existing-settings migration that retains custom entries while adding XSHUT4.
The rebuilt firmware must be uploaded before the Teensy will publish the sixth
sensor. Restart the desktop app after the upload so it requests the new
definitions and reloads the Wiring Guide/Arena layout.

===============================================================================
41. FLAT-PLANE 8X8 ARENA PROJECTION
===============================================================================

The 8x8 Arena View previously treated each column median as radial range. Equal
values were therefore plotted on a circular arc, making a flat plate look
curved even when it was moved squarely toward and away from the sensor.

Matrix values are now interpreted as forward depth along the sensor module's
centreline. For each horizontal bearing, the mapper converts that depth to ray
length using depth divided by the cosine of the bearing relative to the module
centreline. The free-space fill and red occupied endpoint use the same planar
geometry. Equal column depths consequently terminate at the same forward
coordinate and form a straight line perpendicular to the configured module
angle. Real measurements may still form small 10 mm steps because each column
has independent noise and the evidence grid uses 10 mm cells.

The arena-model regression test now explicitly feeds an equal 1000 mm frame,
checks all eight projected endpoints have the same forward coordinate, and
checks the expected 1150 mm world position after the module's 150 mm mounting
offset. The complete desktop suite passes all 29 tests. This is a desktop-only
mapping correction and requires an app restart, not a Teensy upload. Press
Reset local map after restarting to discard curved evidence stored by the old
projector.

===============================================================================
42. D21 INDUCTIVE PROXIMITY INPUT
===============================================================================

The inductive proximity sensor is assigned to Teensy D21. The obsolete D11
placeholder in config.h was corrected. D21 does not conflict with the current
Bluetooth, Herkulex, motor, encoder, HX12K, I2C or TOF assignments.

A reusable DigitalInputSensor library now owns digital input setup and
debouncing. Inputs are listed in the top-level `digital_inputs` array in
debug_config.json, so additional switches or presence sensors can be added
without cluttering main.cpp. Each entry chooses a unique name, pin, active
polarity, internal pull-up setting and debounce time. Up to eight are accepted;
invalid or duplicate definitions produce a robot-side error.

The current entry is `inductive_proximity`, pin 21, active-low, pull-up enabled,
with 20 ms debounce. It publishes the debounced logical state as
`digital.inductive_proximity.detected`, the debounced electrical level as
`digital.inductive_proximity.raw_high`, and the accepted edge count as
`digital.inductive_proximity.transitions`. These appear automatically in Live
Telemetry and recordings. The Wiring Guide adds the D21 row to both fresh and
already-saved layouts while preserving user edits.

D21 is a 3.3 V-only Teensy input. A raw industrial sensor output at 5 V, 12 V or
24 V must not be attached directly. The supplied interface cable, an
optoisolator or a suitable level shifter must limit the CPU-side output to
0–3.3 V. Full commissioning steps and polarity guidance are in
PlatformIO/INDUCTIVE_PROXIMITY_WIRING.md.

The Teensy 4.0 firmware builds successfully with this input enabled. The build
uses 127320 bytes of flash code and 30208 bytes of RAM1 variables. The complete
desktop suite passes all 30 tests, including the persistent Wiring Guide
migration and an exact check of the D21 active-low/pull-up/debounce definition.
The generated embedded configuration was refreshed by the successful build.
This change requires uploading the rebuilt firmware before live D21 telemetry
will appear.

===============================================================================
43. FRONT ULTRASOUND ON D24/D14
===============================================================================

The dual trigger/echo ultrasound interface is configured with channel A TRIG on
D14 and ECHO on D24, plus channel B TRIG on D22 and ECHO on D20 through the
8-pin cable. Live edge capture confirms this reversed-within-each-pair mapping
is the working mapping. The old D9/D10 placeholders in config.h were replaced.
D14 is also A0, so the unused IR_LEFT placeholder cannot operate at the same
time.

A reusable UltrasoundSensor library sends a 10 microsecond trigger every 100 ms
and captures both echo edges with hardware interrupts. It does not use the
example's unbounded `pulseIn()` call, so a missing echo cannot stall serial
parsing, motor deadman handling or servo service. The echo times out after 30
ms. Successful pulse duration is converted to millimetres using approximately
0.343 mm per microsecond and divided by two for the outward/return path;
20–5000 mm is the accepted range.

Ultrasound devices are now declared in the top-level `ultrasound_sensors` JSON
array. The supplied interface has A/B sockets and the workflow supports both,
but each requires its own trigger/echo pair. A scheduler permits only one
transmitter/listener pair at a time and alternates completed measurements, so
the channels cannot cross-trigger one another. Channel A publishes
`ultrasound.a.valid`, `.timed_out`, `.echo_us`, and `.distance_mm` when
valid. Channel B publishes the corresponding `ultrasound.b.*` fields. Dynamic
telemetry makes both appear in the GUI and recordings without adding
GUI-specific controls.

The Wiring Guide adds a persistent Front ultrasound board row to both new and
existing settings while retaining custom entries. Electrical and commissioning
details are in PlatformIO/ULTRASOUND_WIRING.md. D14 must never receive more than
3.3 V; condition any 5 V echo output before it reaches the Teensy.

The final Teensy 4.0 build succeeds, using 127704 bytes of flash code and 31360
bytes of RAM1 variables. All 33 desktop tests pass, including exact ultrasound JSON
configuration and migration into an existing customised Wiring Guide. The
generated embedded configuration is current. Upload this firmware before
expecting the `ultrasound.a.*` and `ultrasound.b.*` telemetry fields from the robot.

The interface-board documentation shows two HC-SR04 modules connected through
the 8-pin CPU cable, while the single-sensor example uses the smaller cable.
The initial D24-trigger/D14-echo and D20-trigger/D22-echo mapping produced only
timeouts. Exchanging the echo lines between channels also produced no response.
The current hardware mapping restores the original pair grouping but reverses
the function within each pair: D14-trigger/D24-echo for A and
D22-trigger/D20-echo for B. Live testing confirms both channels work in this
configuration.

The ultrasound driver now captures ECHO edges with hardware interrupts instead
of relying on loop polling. This prevents short echo pulses from being missed
while the workflow services I2C sensors or Bluetooth. It also publishes raw
12-bit echo ADC/current voltage, minimum and maximum ADC/voltage observed during
the last ping, trigger low/high ADC and estimated high voltage, current digital
echo level, and cumulative trigger/rise/fall counters for both channels. The
read-only live sensor diagnostic prints all of these fields. The instrumented
firmware was built and uploaded successfully for electrical diagnosis. That
build uses 128152 bytes of flash code and 31424 bytes of RAM1 variables.

The added diagnostics initially made the combined telemetry JSON exceed the
3071-byte RobotDebug transmit limit, which silently discarded every normal
telemetry frame while the separately transmitted 8x8 frames continued. Normal
telemetry and ultrasound electrical diagnostics are now sent as two telemetry
frames with the same timestamp. The GUI merges these by signal name as usual.

A successful 15-second COM10 capture after this correction received 120 signal
names with no protocol errors. Ultrasound A reported 9 triggers, 9 rising edges
and 9 falling edges, a 6768 microsecond pulse and a valid 1161 mm distance.
Ultrasound B also reported 9/9/9 edges, a 4282 microsecond pulse and a valid
734 mm distance. Both trigger outputs measured ADC 0 low and ADC 4095 high,
equivalent to 0.0 V and 3.3 V at 12-bit resolution. Both echo inputs were 0 V
when idle. The slower loop-level ADC sampling missed the echo-high interval,
but the one-for-one rising/falling interrupt counts prove that both echo inputs
crossed the Teensy digital threshold on every ping. Measuring the exact
echo-high amplitude requires a scope or multimeter; ADC conversion inside the
edge interrupt was tested and removed because it stalled runtime service.

The restored safe firmware builds and uploads successfully, using 128408 bytes
of flash code and 31424 bytes of RAM1 variables. All 33 desktop tests pass.
After the final upload, two later COM10 captures opened the PC-side CH9143 port
but received no bytes at all, including no independent 8x8 packets. This is a
Bluetooth link reconnection state after the Teensy reset rather than an
ultrasound failure; the remote Bluetooth side should be power-cycled before
opening the GUI again.

===============================================================================
END OF STATUS
===============================================================================


ONLINE CHATGPT ADDITIONS - CODEX TO READ

===============================================================================
44. ULTRASOUND A/B DESKTOP APP AND ARENA VIEW INTEGRATION
===============================================================================

The working ultrasound firmware from commit 12951a3 remains unchanged. The
robot continues to use the full BluetoothDebugWorkflow rather than any of the
temporary ultrasound-only diagnostic sketches or reduced test loops. The
current workflow therefore continues servicing Bluetooth, Herkulex, both 203
motor banks, keyboard-drive deadman handling, HX12K servos, both encoders,
BNO055, six point TOFs, the SEN0628 8x8 TOF, D21 inductive input and both
ultrasound channels together.

The desktop Arena View now treats ultrasound A/B as first-class range sources.
It reads the existing `ultrasound_sensors` entries from debug_config.json, so
the confirmed working pin mapping remains:

- Ultrasound A: TRIG D14 / ECHO D24.
- Ultrasound B: TRIG D22 / ECHO D20.

No Bluetooth protocol or firmware telemetry format change was required.
BluetoothSerial already accepts both normal telemetry packets and the second
ultrasound electrical-diagnostic telemetry packet sent with the same robot
timestamp. DataRecorder and the generic Live Telemetry table therefore continue
recording all ultrasound fields without any special transport path.

Arena View now adds:

- A dedicated raw Ultrasound sensors panel beside the point-TOF and 8x8 raw
  displays.
- Live display of valid distance, echo pulse width and rise/fall edge counts.
- Explicit TIMEOUT / NO VALID RANGE states.
- An `outside map` marker when the firmware regards a measurement as valid but
  it lies outside the Arena View 10-3500 mm mapping window.
- Numeric angle, right-offset and forward-offset controls for both ultrasound
  channels. These persist through the existing QSettings mechanism.
- Default positions of -90/+90 mm lateral and 180 mm forward for A/B
  respectively. These are only starting estimates and should be replaced with
  measured robot geometry.
- Ultrasound obstacle/free-space evidence in the Arena View map. A range must
  be valid, not timed out, within the map range, and repeat consistently before
  it is accepted. The existing temporal range filter then suppresses isolated
  jumps.
- A live `ultrasound valid: X/2` summary in the Arena View status line.

Ultrasound is deliberately excluded from the purple possible-weight
top-versus-bottom comparison because its acoustic beam is much broader than the
point TOF beams and does not provide an equivalent height-layer measurement.
For the same reason, current mapping uses the configured centreline as
diagnostic evidence rather than pretending the ultrasonic beam is a precise
laser ray.

The established graphical sensor canvas is intentionally left as the six point
TOFs plus the 8x8 module so the existing TOF layout workflow and tests are not
silently redefined. Ultrasound geometry is edited with the new numeric controls
in the same Range sensors section.

This is a desktop-only change. The current Teensy firmware should NOT be
replaced with an older pre-ultrasound image. Restart the Python application to
load the new ArenaView.py; no Teensy upload is required for this integration.

The replacement ArenaView.py supplied with this status entry was syntax-checked
with Python's AST parser before handoff. A physical live run is still required
to confirm the measured A/B mounting offsets and angles and to observe the
ultrasound map evidence alongside the already-working TOF/IMU/encoder streams.


===============================================================================
45. GRAPHICAL LEFT/RIGHT ULTRASOUND SENSOR LAYOUT
===============================================================================

The two working ultrasound channels are now represented as distinct physical
sensors throughout the desktop app:

- Ultrasound Left (A): channel A, TRIG D14 / ECHO D24.
- Ultrasound Right (B): channel B, TRIG D22 / ECHO D20.

WiringGuide.py now uses those left/right names for fresh settings. A one-time
settings migration renames existing default "Ultrasound A" and "Ultrasound B"
rows to the new physical names without renaming unrelated custom user-created
ultrasound entries. The existing confirmed pin assignments are retained.

ArenaView.py now places both ultrasound channels on the same graphical robot
sensor-layout interface used by the TOF sensors. They appear as yellow diamond
markers labelled US-A and US-B. Each marker can be dragged around the robot
diagram to set its right/left and forward/back position, and the white arrow tip
can be dragged to set its pointing angle. The corresponding numeric controls
remain available and stay synchronised with graphical dragging.

The existing six point-TOF sensors plus the 8x8 module remain the primary
`sensor_canvas.specs` list so older TOF layout assumptions remain compatible.
The two ultrasound sensors are held as auxiliary graphical specs and are
included in all drawing, hit-testing, dragging and angle-editing operations.
There are therefore nine visible draggable range-sensor markers in total:
six point TOFs, one 8x8 TOF module and two ultrasound sensors.

Arena View reads the ultrasound display names from the Wiring Guide. It also
normalises old saved default names immediately, so the first app launch after
this change shows "Ultrasound Left (A)" and "Ultrasound Right (B)" even though
the Arena tab is constructed before the Wiring Guide tab performs its saved-
settings migration.

The ultrasound map behaviour from the previous integration is retained:
`ultrasound.a.*` and `ultrasound.b.*` telemetry feed the dedicated raw
ultrasound panel and, after the existing consistency/range filtering, contribute
green free-space evidence and red obstacle endpoint evidence from the configured
sensor pose. Ultrasound remains excluded from the purple top/bottom TOF weight
comparison because its acoustic beam geometry is different.

DebugGUI.py already connects `WiringGuide.entries_changed` to
`ArenaView.reload_sensor_layout`, so editing the saved ultrasound rows in the
Wiring Guide causes the Arena View sensor layout to reload without adding any
new GUI plumbing.

This is a desktop-only change. No Teensy firmware, debug_config.json or Bluetooth
protocol change is required and no firmware upload is needed. Restart the GUI
after replacing ArenaView.py and WiringGuide.py.

Both replacement Python files pass `python -m py_compile`. The full Qt runtime
test suite could not be executed in the handoff environment because PyQt6 is
not installed there, so the existing project test suite should still be run in
the project's normal GUI virtual environment after replacement.

===============================================================================
46. AUTONOMOUS NAVIGATION CONTROLLER
===============================================================================

The desktop additions from commit 0a71ee7 were reviewed in the project virtual
environment. The complete 33-test Qt/offline suite passes, including the Arena
View mapping, graphical sensor layout, wiring persistence, Bluetooth status,
keyboard drive and ultrasound configuration tests.

An `autonomous_navigation` command is declared in debug_config.json. It appears
as one Navigation command card containing RUN NAVIGATION and STOP NAVIGATION.
It requires Debug Mode. Starting navigation also enters the run state, while
its STOP button neutralises motor bank 2 without disabling unrelated actuators.
STOP ROBOT and leaving Debug Mode cancel navigation and neutralise the drive.

Navigation runs on the Teensy rather than depending on continuous desktop
commands. It fuses valid 100-3500 mm point-TOF readings, both ultrasound
channels, the middle four SEN0628 columns for forward clearance, and the left
and right matrix halves for turn selection. Equal negative motor-bank-2 values
drive forward. Because this high-geared drivetrain does not move reliably below
75 percent, the controller uses -80 percent in clear space, -75 percent below
650 mm, and 75 percent opposing wheel commands for an in-place turn below
350 mm. A turn is latched for 650 ms to prevent rapid oscillation and chooses
the side with more measured clearance.

The controller stops at neutral if Debug Mode/run state is lost, no usable
forward range exists, or point-TOF acquisition is over one second old. Keyboard
drive and manual 203 motor commands cancel navigation before taking control.
Telemetry exposes `navigation.active`, numeric `navigation.state` (0 stopped,
1 forward, 2 left, 3 right, 4 waiting), and fused front/left/right clearance.

The first physical run produced a permanent clockwise avoidance circle. The
cause was the original fusion rule taking the single minimum measurement from
all 64 matrix zones and accepting readings down to 100 mm. A chassis/ground
self-return or one noisy near pixel could therefore hold the avoidance branch
forever. Navigation now rejects sub-200 mm values consistently with the GUI's
established raw-v2 filtering. It uses the lower quartile of the valid central,
left and right 8x8 zones instead of a single closest pixel, retaining sensitivity
to a real plate or wall while rejecting isolated outliers.

main.cpp remains unchanged and only calls BluetoothDebugWorkflow. Embedded JSON
generation and the Teensy build succeed. The build uses 130936 bytes of flash
code and 32480 bytes of RAM1 variables. The full desktop suite passes 33 tests.

===============================================================================
47. CURRENT NAVIGATION BEHAVIOUR AND COMPLETE RUN REPLAY
===============================================================================

The first high-speed navigation tests exposed that 500 ms point-sensor updates
and a 350 mm avoidance threshold did not leave enough stopping distance at the
required 75 percent minimum drive. Range polling is now independent of the
telemetry rate and runs every 100 ms. Encoder and BNO055 samples run every 50
ms. Navigation decisions execute before normal telemetry transmission so large
Bluetooth JSON frames cannot delay a motor safety response. Avoidance begins at
900 mm and the robot uses 75 percent from 900-1300 mm or 80 percent in clearer
space.

Avoidance turns are clearance-driven rather than blindly timed. The earlier
900 mm forward envelope caused the robot to treat most of the arena as blocked
and spin continuously. Forward avoidance now begins at 300 mm and clears at
350 mm. All six point TOFs are physically aimed into the forward field at their
fitted angles and therefore contribute to forward clearance along with the
SEN0628 central region. Ultrasound A faces robot-left and ultrasound B faces
robot-right; only those ultrasound channels supply side clearance. Side
avoidance begins at 200 mm and clears at 250 mm. Once a turn has run for at
least 350 ms, forward motion resumes when both the front and sides meet their
clear thresholds. If no usable forward range is available, the robot keeps
turning to search for clearance instead of stopping.

Automatic navigation shutdowns based on missing range data, IMU availability,
encoder direction or motion disagreement have been removed. Autonomous motion
is neutralised only by an explicit STOP command, leaving Debug Mode, entering
the global stopped state, or another manual drive command taking ownership.
Encoders and BNO055 heading are still checked against the commanded motion and
published as `navigation.motion_consistent`; this is diagnostic telemetry only
and never stops the motors. Existing encoder rates, full IMU state, fused
clearances, motor commands and navigation state are all recorded for diagnosis.

Recording schema version 4 adds a structured `matrix_frames` table containing
every full SEN0628 frame and a `ui_snapshots` table containing a once-per-second
snapshot of the active tab, all telemetry, parameters, robot state, latest
matrix, selected 8x8 view, favourites, commands, wiring guide and sensor
geometry. Recording metadata embeds debug_config.json, the CPU wiring guide and
initial arena sensor layout. Continuous raw serial, telemetry, commands,
parameter changes, logs, state packets, annotations and faults remain intact.

DataVisualiser has a Run Replay workspace with one synchronized timeline,
play/pause, 0.25x-5x speed and Go to End controls. Nested views reproduce
Dashboard, 8x8 TOF, Arena View, Plots, Parameters, Commands, Wiring Guide, Logs
and Raw Serial over time. A separate End View reconstructs and freezes the final
arena map. Version 3 recordings remain supported, including best-effort 8x8
recovery from raw serial, but cannot reconstruct data absent from that format.

This section supersedes the original stop conditions and thresholds documented
in section 46. The firmware builds successfully with 130392 bytes of flash code
and 32512 bytes of RAM1 variables. Python syntax checks pass. The expanded
offline suite passes 36 tests, including matrix/snapshot persistence, temporary
recording replay, all requested replay tabs, and the current non-stopping
navigation behaviour.

===============================================================================
48. WALL-FOLLOWING ARENA COVERAGE PROGRAM
===============================================================================

The recording `FirstProperRun.rdbg` was analysed after the reactive controller
again produced continuous turning. Over the 76.3 second run, navigation state
3 was the median and final state. Motor bank 2 remained near A=-75 and B=+75.
Encoder 1 reached -91573 counts while encoder 2 reached only -760 counts, which
confirms that the run was dominated by a one-sided pivot/circle rather than
forward travel. The fused forward range had a median of only 147.5 mm.

The source of that permanently short range was the lower point-sensor group.
`bottom_left_left` stayed between 110 and 245 mm with a 150 mm median, while
`bottom_mid_left` frequently read around 200 mm or returned its 8191 out-of-
range sentinel. These sensors can see one another or nearby robot structure.
They are intended to distinguish low weights from walls and are no longer used
by autonomous navigation. They remain initialised, telemetered, recorded and
available to Arena View's top/bottom weight processing.

Forward navigation now uses only `top_mid_left`, `top_mid_right`, and the
central four columns of the SEN0628. It takes the median of those independent
forward estimates, so one isolated short return cannot trap the robot in an
avoidance turn. A broad wall still affects the majority. Forward wall distance
is 300 mm. Ultrasound A is exclusively the robot-left range and ultrasound B is
exclusively the robot-right range, with 200 mm side clearance.

The former reactive obstacle wanderer has been replaced by a deterministic
coverage state machine:

1. `seek_wall`: hold the starting BNO055 heading and drive straight until the
   robust forward range reaches 300 mm.
2. `initial_turn`: make a BNO055-measured 90 degree right turn.
3. `follow_wall`: follow the wall on the left using ultrasound A, targeting
   250 mm, until the forward sensors identify the corner.
4. `corner_turn`: make another 90 degree right turn into the first long sweep.
5. `sweep`: hold heading and traverse the arena until the opposite wall.
6. `lane_turn_out`, `lane_shift`, `lane_turn_in`: alternate right and left
   U-turns, using the calibrated encoders to shift 300 mm into the next lane.
7. After eight parallel lanes (covering the supplied 2400 mm arena width), enter
   `complete` and command neutral motor pulses.

Straight sections use 80 percent drive and small 75/80 differential corrections
to hold IMU heading while respecting the drivetrain's measured 75 percent
minimum. Right/left turns use opposing 80 percent commands and finish within
seven degrees of the target heading. Side readings below 200 mm cause a forward
steering correction during a sweep rather than restarting a permanent turn.

Telemetry now records `navigation.phase`, `navigation.lane`, and
`navigation.target_heading_deg` as well as the numeric state, fused ranges and
motion-consistency diagnostic. The updated debug command description explains
the coverage routine and the generated embedded configuration is rebuilt from
the JSON automatically.

The Teensy firmware builds successfully. It uses 130520 bytes of flash code and
32512 bytes of RAM1 variables.

===============================================================================
49. ORTHOGONAL SWEEPS AND DISCOVERED-BOUNDARY MAP
===============================================================================

The subsequent recording `New run.rdbg` lasts 145.2 seconds. Navigation starts
at 18.9 seconds, reaches the first wall at 23.6 seconds, follows it to the corner
at 37.1 seconds, begins the first sweep at 39.6 seconds, and reaches lane 6 before
the manual STOP at 142.7 seconds. The basic wall/corner/lane sequence therefore
runs, but two independent problems were visible.

First, every 90 degree target was previously calculated from the measured IMU
heading at the end of the preceding leg. The seven-degree completion tolerance
was consequently inherited by the next leg, allowing a few degrees of error to
accumulate at every U-turn. Turns are now calculated from the exact commanded
heading of the previous leg. Forward legs also retain that exact target rather
than adopting the measured completion heading. All headings therefore remain
on the four-direction lattice established when navigation starts, with no
lane-to-lane accumulation.

Second, the recording has regular telemetry through 19.4 seconds and then a
34-second gap during autonomous motion, followed by widely spaced samples. The
normal telemetry JSON had grown close to the former 3072-byte transmit limit,
and the full-rate ultrasound electrical diagnostic packet plus 8x8 frames left
little capacity on the 115200-baud link. RobotDebug now provides a 4096-byte
transmit buffer. Motion, pose, navigation, IMU, encoder, range and matrix data
retain their normal rates; only low-level ultrasound electrical diagnostics are
reduced to one packet per second. This prevents long pose-history holes while
still recording those diagnostics.

Arena View no longer draws or clips to the supplied 2400 by 4900 mm rectangle.
The old width/length controls remain internally for recording compatibility but
are hidden and have no rendering effect. The canvas begins as an unbounded dark
local frame, automatically fits around the robot, trail, current rays and known
cells, and expands as observations accumulate. Only measured green free cells,
red occupied cells and purple weight candidates are painted. Detected red wall
cells therefore form the arena boundary naturally; there is no artificial
outline or filled unknown rectangle. The same rendering is used by live Arena
View, recording replay and End View.

Map odometry now uses exact constant-curvature arc integration between pose
samples instead of a midpoint approximation. This materially improves turns
when Bluetooth samples are sparse or the drivetrain pivots around one wheel.
An offline regression test covers a 90-degree sparse-sample arc.

The updated Teensy firmware builds successfully with 130904 bytes of flash code
and 33536 bytes of RAM1 variables. The complete offline suite passes all 38
tests, including exact sparse-turn integration, absence of a fixed arena
outline, orthogonal navigation targets, recording replay, sensor mapping and
wiring persistence.

===============================================================================
50. TIGHTER WALL FOLLOWING AND CLOSER COVERAGE LANES
===============================================================================

The first-wall following phase now targets 200 mm from the left wall instead of
250 mm. A narrow 10 mm deadband prevents needless twitching. Outside that band,
the steering correction grows proportionally with ultrasound distance error:
the motor pair uses a differential from 5 to 10 percentage points while both
wheels continue forward at or above the drivetrain's reliable 75 percent
minimum. If ultrasound A reports increasing separation, the left wheel is
slowed relative to the right wheel to arc back toward the wall. If it reports
less than 190 mm, the opposite differential arcs away. Inside the deadband the
BNO055 resumes exact parallel-heading hold, preventing the range correction
from becoming a permanent curve.

Parallel coverage lane spacing is reduced from 300 mm to 250 mm. The program
now performs ten lanes rather than eight, preserving coverage of the supplied
2400 mm arena width while producing greater overlap between adjacent sensor
passes.

The command description and embedded JSON configuration reflect the 200 mm wall
target and 250 mm lane spacing. The firmware builds successfully with 131032
bytes of flash code and 33536 bytes of RAM1 variables. All 38 offline tests
pass.


ONLINE CHAT ADDITION:

===============================================================================
50. WALL-FOLLOWING SMOOTHING AND FINER COVERAGE SWEEPS
===============================================================================

The coverage controller was tuned after the latest physical run showed that the
basic wall/corner/lane sequence works, but the first wall-following leg wanders
more than desired and the 300 mm sweep spacing leaves coverage bands wider than
wanted.

Only PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp changes.
No desktop GUI, Bluetooth protocol, debug_config.json, sensor driver, pin
assignment or recording format change is required.

Wall following:

- The left wall target remains 250 mm and still uses ultrasound A.
- The previous wall follower used coarse threshold steering: one fixed motor
  imbalance when closer than target-30 mm, the opposite imbalance when farther
  than target+50 mm, and otherwise straight heading hold.
- The new controller filters completed ultrasound-A echoes before steering.
  It keys new samples from the ultrasound fall-edge count so repeated reads of
  the same echo do not get over-weighted.
- Large single-sample distance changes are rate limited to 120 mm before the
  low-pass update. The accepted filter moves 35 percent of that limited step per
  fresh echo.
- A recent good ultrasound measurement is retained for 650 ms across a missed
  echo/timeout. If no recent valid wall range exists, the robot holds the known
  wall-parallel BNO055 heading rather than making a blind distance correction.
- Wall-distance error is converted into a small heading offset around the exact
  wall-parallel heading instead of directly switching between two motor pairs.
  A +/-15 mm deadband is used around the 250 mm target. Outside it, the heading
  offset is 0.06 degree/mm and is limited to +/-10 degrees.
- Wall-follow forward power is 78 percent.

Straight-line heading control:

- The previous +/-4 degree bang-bang 80/75 steering has been replaced by
  continuous proportional differential steering.
- Heading error is multiplied by 0.65 and limited to +/-5 percentage points.
- Errors below 0.75 degree are treated as zero.
- The existing measured drivetrain minimum is respected by constraining each
  requested drive channel to 75-85 percent.
- Sweep side-clearance corrections now request a temporary +/-7 degree heading
  offset instead of directly forcing a fixed 80/75 motor imbalance.

Turns:

- BNO055 turn completion tolerance is reduced from 7 degrees to 3 degrees.
- Turns use 80 percent while more than 15 degrees from target and 75 percent
  near the target. This is intended to reduce the angular error handed into the
  following straight leg while remaining above the drivetrain's usable minimum.
- The previously-added exact four-direction heading lattice is retained, so
  turn error still does not accumulate lane to lane.

Finer sweeps:

- Lane spacing is reduced from 300 mm to 200 mm.
- SWEEP_LANE_COUNT is increased from 8 to 11.
- The routine therefore targets substantially tighter parallel passes while
  keeping approximately the same overall arena-width coverage.
- Encoder-based lane shift distance now naturally uses the new 200 mm constant.

The current six point TOFs, SEN0628 8x8 sensor, both ultrasound sensors, BNO055,
encoders, Bluetooth debug workflow, telemetry/recording, GUI and safety/manual
ownership behaviour remain intact.

This source was generated from the current testing-branch
BluetoothDebugWorkflow.cpp. A Teensy build and physical run are still required.
For the next run, specifically inspect the first follow_wall segment for
oscillation around 250 mm, verify that 200 mm lane shifts are physically close
to 200 mm, and confirm that the tighter 3 degree turn threshold does not cause
a turn to hunt indefinitely at the drivetrain's 75 percent minimum.


===============================================================================
51. ULTRASONIC ARENA-WIDTH SWEEP TERMINATION AND IMU-CONFIRMED MAP ODOMETRY
===============================================================================

This change is based on testing-branch commit 92cffc0 ("online chat made
better") and supersedes the fixed eleven-lane completion rule described in the
second section 50. The improved 200 mm lane spacing and proportional wall/
heading control from that commit are retained, but the robot no longer assumes
a known arena width or a set number of sweep passes.

Dynamic sweep width / opposite-wall detection:

- The 200 mm value is now only the nominal separation between adjacent long
  sweep passes. There is no `SWEEP_LANE_COUNT` completion condition.
- The robot begins the sweep while already running close to the first side wall
  after the 250 mm left-wall-follow phase. It then works laterally toward the
  opposite side of the arena.
- Both side ultrasound sensors are used to determine how much unmapped arena
  remains. Because the robot reverses its long-sweep heading after every lane,
  the sensor looking toward the still-unmapped side alternates naturally:
    - when the next lane shift is to robot-right, Ultrasound B/right is the
      remaining-width sensor;
    - when the next lane shift is to robot-left, Ultrasound A/left is the
      remaining-width sensor.
- Side ultrasound readings used for arena width accept the driver's full useful
  range up to 5000 mm. Each side has a filtered sweep reference. The filter is
  updated only when that ultrasonic channel completes a new echo, not once per
  main-loop iteration, so a stale sample cannot be counted repeatedly.
- A new sweep-side sample is rate-limited by 200 mm before the filtered value
  moves one third of the limited difference. This reduces the chance that one
  broad-beam reflection or spike causes premature arena completion.
- At the end of every long pass, when the forward sensors reach the end wall,
  the controller examines the ultrasonic sensor facing the still-unmapped side.
  If no valid filtered side range is available, the drivetrain remains neutral
  and waits rather than blindly making another lateral move.
- The target final side clearance is 250 mm. A +/-75 mm completion allowance is
  used, so when the remaining-side ultrasonic reports 325 mm or less at the end
  of a long pass the coverage routine enters `complete` and commands neutral.
- If more arena remains, the next shift is normally 200 mm. When less than
  200 mm of lateral travel remains before the 250 mm edge target, that final
  shift is shortened to exactly the measured remaining amount. The following
  long pass therefore runs close to the opposite wall rather than overshooting
  it. In effect the pattern goes from brushing one side of the arena to
  brushing the other without knowing the arena width in advance.
- `navigation.lane` is retained as a diagnostic count only; it no longer
  decides when mapping is finished.
- Added compact telemetry:
    - `navigation.remaining_width_mm` while a valid remaining-side reference is
      available during a sweep;
    - `navigation.next_shift_mm` for the dynamically selected lateral move.

Encoder/IMU cross-check for the virtual map:

The BNO055 does not provide a trustworthy absolute translational velocity
signal. Its linear acceleration can, however, independently confirm that the
physical chassis actually started moving. A firmware-side motion-event detector
therefore runs at the existing 50 ms IMU sample rate:

- motion start threshold: 0.22 m/s^2 for two consecutive samples (~100 ms);
- motion stop threshold: 0.08 m/s^2 for three consecutive samples (~150 ms);
- each confirmed stationary-to-moving transition increments a persistent
  `imu.motion_event_count`;
- `imu.motion_active` and `imu.motion_event_count` are included in normal
  telemetry. The event counter allows the GUI to see a short launch
  acceleration even when it occurred between slower telemetry packets.

Arena View now cross-checks encoder translation against this IMU evidence:

- encoder counts are always consumed immediately, including rejected motion,
  so false wheel movement can never be accumulated and applied later;
- when a new encoder-motion bout starts, map translation is accepted only when
  the IMU is currently valid and either `imu.motion_active` is true, the motion
  event counter has incremented, or the fused BNO055 heading has changed by at
  least 0.5 degrees (independent evidence for a real turn);
- once a bout has been confirmed, the confirmation remains latched while the
  encoders continue moving. This is necessary because a robot travelling at
  steady velocity can legitimately have approximately zero linear
  acceleration;
- when the encoders stop, the latch clears. The next encoder-motion bout must be
  independently confirmed again by the IMU;
- if the encoders move while the current IMU is invalid or supplies no motion
  evidence, Arena View leaves x/y and travelled distance unchanged, reports
  zero virtual linear speed for that sample, and displays
  `ENCODER/IMU DISAGREEMENT: map translation held.`;
- older recordings/firmware that do not contain the new `imu.motion_*` fields
  retain the legacy odometry path for replay compatibility.

Files changed:

- PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp
- Non-Teensy-Files/BluetoothDebugGUI/ArenaView.py

No header, pin assignment, debug_config.json, Bluetooth command format, sensor
wiring or GUI control layout change is required.

Verification performed for this handoff:

- ArenaView.py passes Python bytecode compilation.
- Synthetic ArenaModel checks confirm: encoder-only movement is held; a new IMU
  motion event permits translation; constant-speed encoder movement remains
  accepted after the event; stopping resets the latch; a later wheel-only bout
  is held again; and an invalid IMU holds translation even if stale motion
  fields claim activity.
- The modified C++ has balanced braces, contains no `SWEEP_LANE_COUNT`, and the
  expected dynamic-width / IMU-motion telemetry paths are present.

Not verified in this environment:

- PlatformIO/Teensy compilation, because the project PlatformIO toolchain is not
  available here.
- Physical dynamic-width behaviour. The first robot test should watch
  `navigation.remaining_width_mm` and `navigation.next_shift_mm`, confirm that
  the remaining-side sensor alternates B/A correctly between lanes, verify a
  shortened final shift near the opposite wall, and confirm the robot stops
  after making the final wall-adjacent pass rather than after a predetermined
  lane count.
- The encoder/IMU map gate should also be tested with the drive wheels spinning
  while the chassis is physically restrained: the virtual robot should remain
  stationary and show the disagreement warning.


===============================================================================
52. UNEXPECTED-WALL DETOURS THAT RETURN TO THE ORIGINAL SWEEP PATH
===============================================================================

This change builds directly on section 51's dynamic arena-width sweep logic and
IMU-confirmed Arena View odometry. The robot still maps the arena with 200 mm
nominal lane spacing, still decides the final arena width from the side
ultrasound sensors, and still has no fixed sweep-lane count. The new behaviour
only changes what happens when a forward wall/obstacle appears where a normal
arena end wall is not expected.

Unexpected-wall detection:

- Completed wall-to-wall sweep legs now teach the firmware an expected long
  sweep length from the calibrated encoders.
- `navigation.sweep_progress_mm` accumulates only while the robot is actually
  travelling on its long sweep line. Temporary obstacle-detour travel is
  deliberately excluded so a bypass cannot make the learned arena length grow.
- The first clean completed sweep establishes the expected sweep length. Later
  completed sweeps low-pass update it with 75 percent previous length and
  25 percent current length.
- Once that expected length exists, a forward wall reached more than 450 mm
  before the expected end of the lane is classified as an unexpected obstacle
  rather than an arena boundary.
- The SEN0628 8x8 also provides a geometry-based obstacle cue. Each horizontal
  X column uses the median of at least three valid Y samples. An outer left or
  right flank is considered open when at least two of its three outer columns
  see farther than 550 mm.
- The 8x8 flank rule allows a localised obstacle to be recognised even before
  an expected arena length has been learned. A broad wall that fills the whole
  first-sweep field cannot be distinguished safely from the true arena end wall
  until at least one clean wall-to-wall sweep has established the arena length.

Choosing a bypass side:

- A detour is attempted only when the chosen side ultrasound reports at least
  450 mm of lateral room.
- If both sides are possible, the robot prefers the side with the greater
  measured ultrasound clearance. The 8x8 flank distance is used only as a
  tie-breaker.
- If the controller knows the wall is unexpectedly early but neither side has
  enough verified room, autonomous navigation stops rather than guessing a
  route or driving into the obstruction.

Detour path:

1. The current exact sweep heading is saved. The lane number and the normal
   B/A alternating arena-width direction are not changed.
2. The robot turns 90 degrees toward the selected bypass side.
3. It drives laterally while the ultrasound sensor facing back toward the
   obstacle tracks the obstacle edge. A right detour therefore tracks the wall
   with Ultrasound A/left; a left detour tracks it with Ultrasound B/right.
4. A side return at 650 mm or less confirms that the obstacle is actually being
   tracked. The edge is considered cleared only after two fresh ultrasonic
   measurements are clear: either timeout/open space or a valid range of at
   least 800 mm.
5. After the edge is cleared the robot continues another 220 mm for body
   clearance, records the total lateral encoder offset, then turns back onto the
   exact original sweep heading.
6. It drives forward past the obstacle, again using the obstacle-side
   ultrasound to determine when the far edge has passed. It then adds another
   220 mm forward clearance margin.
7. The robot turns 90 degrees back toward the original sweep lane and drives the
   exact measured lateral offset back using both calibrated encoders.
8. It turns back to the stored original sweep heading and resumes the same
   sweep path from beyond the obstacle. The lane counter is not incremented and
   the detour does not consume a 200 mm coverage lane shift.
9. On returning to the sweep path, the side-width filters are deliberately
   invalidated and reacquired so measurements taken while the robot was rotated
   around the obstacle cannot corrupt the remaining-arena-width estimate.

Safety limits during a detour:

- Maximum lateral search for an obstacle edge: 1200 mm.
- Maximum forward travel while waiting to pass the obstacle: 2500 mm.
- If the lateral route becomes front-blocked before the obstacle edge is found,
  the forward bypass path becomes blocked, the return path becomes blocked, or
  the obstacle never ends within the above limits, navigation stops and logs an
  explicit abort reason.
- Broad returns at approximately the learned sweep-end distance still follow
  the existing normal arena end-wall sequence and dynamic 200 mm/final-shortened
  lane-shift logic.

New navigation phases:

- `obstacle_turn_out`
- `obstacle_offset`
- `obstacle_turn_forward`
- `obstacle_pass`
- `obstacle_turn_back`
- `obstacle_return`
- `obstacle_turn_in`

New compact telemetry:

- `navigation.sweep_progress_mm`
- `navigation.expected_sweep_length_mm` (0 until learned)
- `navigation.detour_active`
- `navigation.detour_side` (`1` = right, `-1` = left, `0` = none)
- `navigation.obstacle_count`
- `navigation.detour_offset_mm`

Files changed:

- PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp
- PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.h

No ArenaView.py, debug_config.json, pin mapping, sensor driver, Bluetooth command
format, recording schema or desktop control changes are required. The generic
telemetry path will expose the new navigation fields automatically, and the
existing Arena View will naturally draw the physical detour because its pose is
still derived from the encoders plus the section 51 IMU cross-check.

Verification performed for this handoff:

- The generated C++ source has balanced braces and parentheses.
- Every new NAV_OBSTACLE_* state used by the implementation is declared in the
  replacement header, and every new navigation member referenced by the source
  is present in that header.
- Lightweight logic checks confirm the right/left detour heading sequence
  returns to the exact original heading, the 450 mm early-wall rule separates a
  clearly premature wall from a near-end wall, and the 8x8 flank rule requires
  at least two agreeing outer columns rather than one noisy pixel.
- The section 51 dynamic arena-width logic and IMU motion-event telemetry remain
  present in the complete replacement source.

Not verified in this environment:

- PlatformIO/Teensy compilation and physical obstacle testing. After replacing
  both files, run the normal PlatformIO build before uploading.
- The first physical obstacle test should use a finite wall/box placed well
  inside a known sweep. Watch `navigation.phase`, `navigation.detour_side`,
  `navigation.detour_offset_mm`, `navigation.sweep_progress_mm` and
  `navigation.expected_sweep_length_mm`. Confirm the robot goes around the
  object, returns to the same line beyond it, then continues the normal sweep.
- Test both left and right bypasses and also place an obstruction where neither
  side has 450 mm clearance; that case should stop safely rather than force a
  detour.


===============================================================================
53. 2026-09-22 RUN DIAGNOSIS: TURN OSCILLATION AND MISSED CLOSE WALL
===============================================================================

The newest recording, `RobotRun_2026-09-22_13-17-44.rdbg`, was inspected
directly rather than judging the behaviour from the Arena View drawing. It
contains 162.3 seconds, 17,084 telemetry rows and 125 signals. Autonomous
navigation ran from 6.87 seconds until the dashboard STOP command at 94.43
seconds.

The recording does not contain the newer sweep-progress, expected-length or
detour telemetry fields now present in the workspace. Therefore the current
dynamic-width/detour implementation was either added after this run or had not
yet been uploaded to the Teensy. The faults below are still directly visible in
the recorded controller and the fixes have been applied to the newer workspace
implementation so they are not lost when that version is uploaded.

Two independent faults were found in the recorded control timeline.

Corner-turn oscillation:

- At 29.52 seconds the robot entered `corner_turn` with a target heading of
  48.44 degrees. It did not begin sweep lane 1 until 78.52 seconds, so this one
  turn consumed almost exactly 49 seconds.
- The BNO055 trace shows repeated overshoot across the same target: examples
  include about 34, 58, 36 and 55 degrees. The motor commands reversed directly
  between the drivetrain's 75 percent minimum usable turn commands. Rotational
  inertia therefore carried the chassis through the target on every reversal.
- Heading turns now coast for 180 ms before reversing direction. Outside 25
  degrees they retain the existing 80 percent turn. Inside 25 degrees they use
  90 ms pulses at the known-working 75 percent motor command followed by the
  remainder of a 300 ms neutral interval.
- Entering the target band no longer immediately starts the next forward leg.
  Both motors remain neutral and the BNO055 heading must stay within 4 degrees
  for 300 ms. This prevents residual rotation from bending the start of a
  sweep.

Missed 8x8 wall:

- At 88.75 seconds, 25 of the 31 usable centre zones were below 450 mm and the
  centre median was about 415 mm. At 89.94 seconds, every one of the 32 centre
  zones reported the same close wall between 153 and 188 mm.
- The old navigation path discarded all matrix values below 200 mm before
  fusion. It consequently converted that exceptionally strong, coherent wall
  frame into no matrix reading. `navigation.front_mm` became zero at 90.9
  seconds and the robot continued forward. It did not enter the lane turn until
  approximately 92.1 seconds, when a separate point sensor reported 42 mm.
- Navigation now has an independent broad-wall detector over the 32 central
  matrix zones. At least 20 zones must be usable and at least 75 percent of
  those zones must be below 450 mm. A coherent surface satisfying that rule is
  authoritative even when its ranges are below 200 mm; isolated close noise
  remains rejected.
- Replaying the recorded pixel values through the new rule would declare the
  wall at 88.75 seconds, over three seconds earlier than the old state change.
  The 450 mm matrix threshold compensates for the observed roughly 1.2 second
  interval between full matrix frames; ordinary point-sensor avoidance remains
  at 300 mm.
- New recorded telemetry exposes `navigation.matrix_close_zones`,
  `navigation.matrix_usable_zones` and `navigation.matrix_broad_wall`. A future
  run can therefore show the complete evidence behind each forward-wall
  decision.

Review of the intervening workspace changes:

- Arena View now cross-checks encoder translation against BNO055 linear-motion
  events, suppressing false map travel when the wheels turn without moving the
  chassis.
- Firmware now publishes IMU motion activity/event telemetry, learns expected
  wall-to-wall sweep length, measures remaining arena width dynamically and
  contains a seven-phase rectangular bypass for unexpected internal obstacles.
- The fixed sweep count was removed and nominal lane spacing is now 200 mm.
- The wall-follow target had accidentally returned to 250 mm. It has been
  restored to the requested 200 mm and protected by the regression suite.

Files changed for this diagnosis and fix:

- `PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp`
- `PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.h`
- `Non-Teensy-Files/BluetoothDebugGUI/test_recording_replay.py`
- `status.txt`

Verification:

- PlatformIO Teensy 4.0 release build succeeds. The image uses 133,912 bytes of
  flash code and leaves 325,792 bytes of RAM1 available for local variables.
- All 38 desktop regression tests pass in offscreen mode.
- Regression checks now require the 200 mm wall-follow target, dynamic 200 mm
  lane spacing, broad-matrix wall consensus, settled/pulsed heading turns and
  the absence of a fixed sweep-lane count.
- Physical verification still requires uploading the new firmware. The next
  recording should show a short, monotonic corner turn and a broad-wall verdict
  before the front of the robot reaches 300 mm from a flat wall.


===============================================================================
54. NEW RUN CONFIRMED TO BE USING THE PRE-FIX TEENSY BINARY
===============================================================================

`RobotRun_2026-09-22_13-35-43.rdbg` was captured after section 53's source-code
fix, but its telemetry proves that the corrected firmware was not running on
the Teensy.

- It still contains exactly 125 signals and does not contain
  `navigation.matrix_close_zones`, `navigation.matrix_usable_zones`,
  `navigation.matrix_broad_wall`, `navigation.sweep_progress_mm`,
  `navigation.expected_sweep_length_mm` or the obstacle-detour fields.
- At the first wall the old controller was still commanding approximately
  -79/-81 percent while its front range changed from 436 mm at 20.4 seconds to
  210 mm at 21.6 seconds. Residual motion brought the reported range to 77 mm.
- During the following turn, the recording shows uninterrupted -80/+80 motor
  output followed by direct, uninterrupted +/-75 percent reversals around the
  316.06 degree target. Example BNO055 headings were 339, 273, 330, 302, 328,
  313 and 322 degrees. This is precisely the old turn implementation; the new
  implementation cannot generate that trace because it inserts neutral coast
  time and 90 ms near-target pulses.
- The robot remained in navigation state 2 (`initial_turn`) for the remainder
  of the 44.4 second recording.

No further controller constants were changed from this run because it did not
exercise the current source. A connected-device check showed only COM10, the
CH9143 Bluetooth adapter, plus the computer's two standard Bluetooth serial
ports. No Teensy programming USB device was available, so the corrected binary
could not be uploaded. The next required action is to connect the Teensy USB
programming port and upload the already-successful PlatformIO build before
performing another physical run.


===============================================================================
55. 90 PERCENT CRUISE, U-TRAP ESCAPE AND FALSE-MAP-TRAVEL REJECTION
===============================================================================

The lower horizontal Arena View trail in the 13:35 recording was not real
chassis travel. The robot was physically against the wall while its wheels and
turn commands continued accumulating encoder counts. Motor vibration could
also satisfy the BNO055 linear-acceleration event latch, so encoder/IMU motion
agreement alone was insufficient to reject this wheel slip.

Arena View now applies two additional kinematic checks:

- Opposite-sign bank-2 commands of at least 50 percent are a commanded point
  turn. Unequal encoder counts during that command may rotate the chassis, but
  they cannot be treated as centre translation. The map continues consuming
  BNO055 heading while holding X/Y position.
- When both bank-2 channels command forward at 50 percent or more and
  `navigation.front_mm` is 300 mm or closer, encoder motion is treated as
  forward wheelspin against a wall. Translation and travelled distance are
  held rather than drawing the robot through the observed wall.
- The Arena View status explicitly reports `WHEEL SLIP / POINT TURN: map
  translation held` when either rule is active.

Navigation speed and wall approach:

- Open-space forward travel now requests 90 percent. Heading correction may
  adjust the two channels between 85 and 95 percent.
- Inside 700 mm of a forward return, travel drops to the drivetrain's known
  usable 75 percent minimum to reduce stopping distance. Point turns retain
  the separately controlled 75/80 percent coast-and-pulse algorithm.
- The broad 8x8 wall rule now uses a 550 mm threshold and requires at least 20
  usable centre zones with at least two thirds agreeing. In the 13:35 run this
  would have detected the first wall at 19.45 seconds, when 22 of 32 central
  zones were below 550 mm and their median was about 468 mm. The old controller
  continued until the next close frame.

U-shaped enclosure recovery:

- A U-trap is declared only when forward navigation has a confirmed front wall
  and both side ultrasound ranges are below 350 mm.
- The controller enters the new `escape_reverse` phase and commands both drive
  channels backward at 80 percent.
- It backs at least 350 mm and continues until either side ultrasound reaches
  350 mm or more. A 900 mm encoder-distance fallback prevents indefinite
  reverse travel if a side reading never recovers.
- It then enters `escape_turn`, chooses the side with more verified clearance,
  performs the controlled 90 degree turn, clears learned sweep-length state and
  restarts wall seeking on the new heading.
- Bottom TOFs do not participate in this rule; they remain reserved for weight
  sensing.

The Navigation command description now accurately states 90 percent open-space
travel, 200 mm wall following, 200 mm sweep spacing and U-trap recovery. The
generated embedded configuration was refreshed by the PlatformIO build.

Verification:

- Teensy 4.0 release firmware builds successfully: 134,488 bytes of code and
  324,736 bytes of RAM1 remaining for local variables.
- All 40 desktop regression tests pass. New tests prove that commanded point
  turns and forward wheelspin at a close wall do not translate the Arena View.
- The programming USB was still unavailable at the time of this build, so this
  firmware still requires upload before physical testing.


===============================================================================
56. FULL AUDIT OF ROBOTRUN_2026-09-22_14-08-10
===============================================================================

The 112.02 second recording was audited from the raw newline-delimited serial
packets, SQLite telemetry, command/log tables, matrix frames and recorded UI
snapshots. It did not run the controller represented by the current source.
The embedded command description had been refreshed, but the executable motor
logic was still the previous build.

Commands and actuator outputs:

- The only motion command from the app was autonomous-navigation RUN at 1.82
  seconds. STOP was sent at 107.54 seconds. No Herkulex or HX12K position command
  was issued; their commanded values remained zero/disabled.
- Open-space motor output was -80/-80 percent, not the requested -90/-90. The
  corresponding recorded PPM pulses were 1140/1140 microseconds. Positive 80
  percent produced 1860 microseconds, negative 75 percent produced 1163
  microseconds and positive 75 percent produced 1838 microseconds. These values
  are consistent with the motor driver's configured PPM conversion.
- During straight motion Encoder 1 was negative and Encoder 2 positive, matching
  the calibrated forward polarity. During point turns the wheel response and
  BNO055 heading both changed, so neither drive channel was electrically dead.

Navigation timeline:

- Wall seek began around 2.9 seconds on an 86.625 degree heading. Forward range
  fell through 1578, 1156, 605 and 259 mm. The initial turn began at 9.0 seconds.
- The requested first-turn target was 176.625 degrees. Continuous -80/+80 output
  drove the heading through 157 to 233 degrees, an overshoot of about 56 degrees.
  Reverse torque then brought it back to 179 degrees.
- The old controller declared the turn complete immediately at 19.16 seconds.
  Inertia continued rotating the chassis to approximately 121 degrees while
  wall-follow output was already driving forward. The robot therefore started
  wall following about 56 degrees off its intended heading.
- The corner was declared at 24.23 seconds. Because of the bad preceding leg,
  the next nominal 90 degree target was 266.625 degrees while measured heading
  was only about 110 degrees: an effective 156 degree correction.
- That turn oscillated repeatedly through approximately 212 to 340 degrees.
  At 50.17 seconds it was still in `corner_turn`. When telemetry returned at
  110.08 seconds it was in `lane_turn_in`, still commanding -80/+80.

Sensor audit over the 32 intact full telemetry frames:

- Top middle left TOF: available but timed out in 32/32 frames; no distance was
  produced. This sensor is not currently providing navigation redundancy.
- Top middle right TOF: available and not timed out in 32/32 frames; 31 recorded
  distances ranged from 77 to 3008 mm.
- Ultrasound A and B: both valid and not timed out in 32/32 frames. A ranged
  from 136 to 2264 mm; B ranged from 263 to 3145 mm.
- SEN0628 matrix: 32/32 frames valid at address 0x33. The matrix itself was not
  the cause of the failed turns.
- Bottom middle right frequently returned the 8191 invalid sentinel. Bottom
  far right timed out in every intact frame. Several other bottom sensors were
  available but produced no usable distance. These remain excluded from
  navigation as intended, but their weight-sensing coverage is incomplete.
- BNO055 fusion remained in status 5 and passed self-test. Gyro calibration was
  3/3, accelerometer 1/3 and magnetometer 1-2/3; whole-system calibration stayed
  0/3. Short relative turns are observable, but absolute magnetic heading is
  not fully calibrated.

Telemetry and map faults:

- There was a complete 58.29 second raw-stream gap from 50.39 to 108.68 seconds
  and a 59.91 second gap between full telemetry frames.
- One returning line was malformed. A later syntactically valid packet had lost
  byte spans inside field names, producing keys such as
  `ultrasound.b.trigger_counasound.b.rise_count`. Those corrupt keys then
  appeared in the saved UI snapshots.
- The transport sent each JSON line in 20-byte pieces with a 3 ms delay between
  every piece and then called `flush()`. A normal 3 KB frame could block the
  firmware loop for hundreds of milliseconds. This invalidated 90 ms steering
  timing and contributed to control latency and link saturation.
- Replaying the recording through the corrected map gives a final local pose of
  approximately X=310 mm, Y=1285 mm, heading=-180.4 degrees and 1525 mm accepted
  travel. Point-turn and close-wall wheelspin are now rejected, reducing the
  impossible trail, but the minute-long telemetry hole cannot be reconstructed.

Transport and controller corrections:

- Serial1 now has an additional 8192-byte hardware TX ring. Complete JSON lines
  are queued to the UART without per-chunk delays and without `flush()`, allowing
  the hardware UART to drain asynchronously while the control loop continues.
- Telemetry now includes `system.navigation_controller_version=4` and
  `system.max_loop_gap_ms`. The Arena View warns clearly if controller version 4
  is absent, preventing another stale/mixed binary from being mistaken for the
  current controller.
- Heading turns now stop continuous 80 percent torque at 55 degrees remaining,
  rather than 25 degrees, and use the existing short 75 percent pulse/coast
  control for the remainder. This prevents the roughly 45 degree/second chassis
  from entering the target region with maximum accumulated inertia.
- The full PlatformIO output directory was cleaned before rebuilding, ensuring
  every library and the workflow source were recompiled.

Verification:

- Clean Teensy 4.0 release build succeeds: 134,808 bytes of flash code and
  316,544 bytes of RAM1 remain available for local variables.
- Firmware image SHA-256:
  `D89DCCEBFA905A60B56BFBB41ED17C3E40DFB39948B86F9FEFB650A9D8CA93FB`.
- All 40 desktop regression tests pass. Tests now also require the 8 KB TX ring,
  absence of the old delay/flush transport, controller-version telemetry and
  the 55 degree early transition to pulsed turning.
- No Teensy programming USB port was visible after the audit. Only the CH9143
  COM10 link and two computer Bluetooth COM ports were present, so the clean
  image has not been uploaded.


===============================================================================
57. FULL AUDIT OF ROBOTRUN_2026-09-22_14-24-40 AND CONTROLLER V5
===============================================================================

The 260.70 second recording contains 45,587 telemetry rows, 398 SEN0628 matrix
frames and 262 complete GUI snapshots. Navigation found the first wall, followed
it to a corner and started eleven sweep lanes before the operator stopped the
run. The physical sequence was substantially better than the preceding run,
but it exposed two remaining design faults: the displayed map remembered noisy
ray cells instead of physical walls, and completed turns were allowed to enter
a forward state even when the newly facing sensors already saw a close wall.

Recording findings:

- Navigation RUN was received at 7.51 seconds. The first wall was found at
  12.65 seconds, wall following began at 15.25 seconds, the first corner was
  detected at 23.76 seconds and sweep lane 1 began at 40.56 seconds.
- Later lane starts were logged at approximately 50.91, 65.31, 81.52, 102.93,
  121.26, 142.07, 162.95, 185.18, 207.33 and 241.19 seconds. STOP was received
  at 244.48 seconds.
- Multiple turns ended with a wall already directly ahead. The first sweep
  began with a forward range near 108 mm and immediately entered a lane turn;
  the next began near 121 mm and again changed state almost immediately. The
  controller had no mandatory post-turn clearance check.
- The recorded motor outputs still topped out at 80-85 percent. No telemetry
  field named `system.navigation_controller_version`, `system.max_loop_gap_ms`,
  `navigation.matrix_broad_wall` or `navigation.sweep_progress_mm` exists in
  this recording. It therefore came from an intermediate/stale executable, not
  the clean controller-v4 image documented in section 56.
- Of 1,047 raw packets, ten were malformed around 197-213 seconds. Several
  syntactically valid frames also contained truncated or joined signal names.
  This is consistent with the old blocking/chunked serial transport still being
  present in the executable that produced the recording.
- Both top point TOFs were available in 392 of 393 intact telemetry frames and
  had no recorded timeouts. Ultrasound A had 383 valid samples and Ultrasound B
  had 362. The SEN0628 supplied 398 frames. These sensors were producing useful
  data; the main problem was how geometry and state transitions used it.
- BNO055 fusion stayed in running status 5 and gyro calibration remained 3/3.
  Whole-system calibration remained 0/3 and magnetic calibration varied from
  1/3 to 3/3, so relative turns are useful but long-term global heading should
  not be treated as an exact absolute reference.
- The bottom sensors remain unsuitable for navigation. Several frequently
  returned the 8191 sentinel or no useful distance. They remain reserved for
  paired top/bottom weight detection.

Persistent wall map:

- ArenaModel now keeps a separate collection of persistent wall tracks rather
  than asking the red/green occupancy grid to represent permanent geometry.
- Repeated returns from top point TOFs, both side ultrasounds and each 8x8
  horizontal sector are classified into one north, south, east or west arena
  boundary. Each confirmed wall uses the median perpendicular coordinate and
  extends its along-wall limits as more of it is observed.
- A track is displayed only after at least three agreeing observations. Once
  confirmed its perpendicular position is locked for the rest of the run, so
  later odometry noise cannot relocate it or spawn stacks of parallel copies.
  Confirmed perpendicular walls meet at their measured intersections, producing
  a clean discovered outline only after all four sides have actually been seen.
  The latest run replays as exactly four retained boundaries rather than the 64
  duplicate candidate lines produced by the first clustering attempt.
- Bottom TOFs never vote for persistent walls. They continue contributing only
  ordinary display evidence and repeated top/bottom weight candidates.
- Reset Map clears the persistent walls along with pose, trail and occupancy
  evidence.

Controller version 5:

- Normal autonomous translation now uses a 100 percent base command in wall
  seeking, wall following, sweep lanes, lane shifts and obstacle-detour legs.
  IMU heading correction may reduce one wheel slightly while the other remains
  at full command; this is necessary to steer rather than repeat a curve.
- Point turns now also use 100 percent during their driven pulses. The existing
  pulse/coast approach, reversal coast and 300 ms settled-heading requirement
  are retained to prevent continuous full-output ping-pong around the target.
- Every turn that would lead to forward motion now checks the current combined
  top-TOF/8x8 forward result first. If it is still blocked, the robot turns an
  additional 90 degrees toward the ultrasound side with more room and checks
  again. It cannot enter the next forward state until a clear heading exists.
- If a completed turn sees the front blocked and both side ultrasounds below
  350 mm, the existing U-shaped enclosure recovery is entered immediately. The
  robot reverses until a side clears before choosing another turn.
- An unexpected additional clearance turn invalidates the old sweep-lane
  geometry. When a clear direction is found, navigation deliberately returns
  to wall acquisition rather than blindly continuing an obsolete lane.
- Telemetry reports `system.navigation_controller_version=5` and phase
  `clearance_turn`; Arena View warns if version 5 is not actually running.

Verification:

- All 44 desktop regression tests pass, including persistent-wall confirmation,
  exclusion of bottom sensors from wall geometry, full replay, transport and
  navigation source checks.
- Teensy 4.0 release firmware builds successfully: 135,384 bytes of code and
  316,544 bytes of RAM1 remain available for local variables.
- Firmware image SHA-256:
  `25333683570BBCCD19410E9BE7EA631D6DC3F552A38F371583C643F6FC2DA46A`.
- This section documents a compiled but not yet physically trialled controller.
  The next recording must show controller version 5 before its behaviour can be
  compared with the stale executable used for the 14:24 run.


===============================================================================
58. ARENA VIEW LIVE MOTOR AND NAVIGATION CONTROLS
===============================================================================

The live Arena View now has a compact control strip above the map containing:

- RUN NAVIGATION: sends the existing `autonomous_navigation` command with
  `enabled=true`.
- STOP NAVIGATION: sends the same command with `enabled=false`, stopping only
  the autonomous navigation routine.
- RUN MOTORS: sends the existing global `run` command, taking the robot out of
  stopped mode so drive commands are permitted.
- STOP MOTORS: sends the existing global `stop` command, immediately entering
  stopped mode and neutralising robot outputs.

The controls emit through RobotDebugGUI.execute_command rather than talking to
the serial layer directly. They therefore use the same command format, debug
mode enforcement, transmit log and recording command history as the Dashboard
and Commands tabs. Run buttons are green and stop buttons are red.

Offline Run Replay and End View instances deliberately hide these controls so a
recording cannot present buttons that look capable of controlling live hardware.
No Teensy firmware change is required for this feature because all four commands
already existed in the protocol.

Verification:

- All 45 desktop regression tests pass.
- A new GUI test clicks all four buttons and verifies the exact command names
  and arguments emitted in order.


===============================================================================
59. NEAR-CONTACT 8X8 WALL AND U-TRAP CORRECTION
===============================================================================

Recording `RobotRun_2026-09-22_14-57-55.rdbg` captured the robot continuously
driving into the wall at the upper-right of the discovered arena. The failure
was reproduced directly from the recorded actuator, range and controller data.

Exact stuck condition near 75.7 seconds:

- Navigation remained in `follow_wall` and commanded approximately -80/-76
  percent forward output. Encoder 1 and Encoder 2 continued reporting about
  -1018 and +1057 counts/second, respectively, while the wheels pushed against
  the wall.
- Ultrasound A measured 160 mm and Ultrasound B measured 248 mm. Both sides
  therefore satisfied the requested U-enclosure threshold of 350 mm.
- Both top point TOFs were timed out, leaving no point-sensor forward range.
- The 8x8 frame at 64.19 seconds showed all 32 centre zones below 550 mm with a
  median of 34 mm. Every subsequent frame contained a coherent surface only
  12-21 mm away across almost the complete array.
- The firmware's broad-wall calculation accepted values only from 30 mm upward.
  Once the wall was closer than that, all 12-21 mm zones were discarded and
  `navigation.front_mm` became zero, meaning unknown rather than close. Because
  `frontBlocked` became false, U-trap recovery could not start despite both side
  ranges being close.
- This recording also lacks `system.navigation_controller_version`,
  `system.max_loop_gap_ms`, `navigation.matrix_broad_wall` and the newer sweep
  fields. Its motor output never reached the requested 100 percent. It was made
  by the older intermediate firmware rather than controller version 5.

Controller version 6 correction:

- Broad 8x8 wall consensus now accepts centre-zone measurements from 10 through
  3500 mm. Zero remains invalid. A coherent 10-29 mm image is treated as an
  authoritative near-contact wall rather than disappearing from navigation.
- Ordinary matrix ranging still rejects values below 200 mm for general gap and
  distance estimation. Only the independent broad-wall consensus receives the
  near-contact allowance, so isolated low pixels cannot trigger the behaviour.
- The existing consensus still requires at least 20 usable centre zones and at
  least two thirds of them below 550 mm. Re-evaluating every final matrix frame
  from this run with the new rule produces 32 usable close zones and therefore
  a broad wall. Combined with the recorded 160/248 mm side ranges, controller
  version 6 enters `escape_reverse` instead of continuing forward.
- Arena View now expects and reports controller version 6 so an old upload is
  immediately visible.

Verification:

- All 45 desktop regression tests pass.
- Teensy 4.0 release firmware builds successfully: 135,384 bytes of code and
  316,544 bytes of RAM1 remain available for local variables.
- Firmware image SHA-256:
  `FD7C7B8D6546A9947E7868A7FF368475DF5145FAED39397F959499836B99DF31`.
- No Teensy programming port was connected after the build. Only COM3 and COM4
  computer Bluetooth ports were visible, so controller version 6 has not yet
  been uploaded to the robot.


===============================================================================
60. NON-ABORTING NAVIGATION RECOVERY AND SELF-CORRECTING INTERNAL WALL MAP
===============================================================================

This change is based on the current `testing` branch at commit
`d8216d4de23f8864e39983f9ce27c74f9b1b8c94`
("Better smoother navigation code, just got stuck in a loop at one point. But
way better"). GitHub was read only while preparing these files.

Affected files:

- PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp
- PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.h
- Non-Teensy-Files/BluetoothDebugGUI/ArenaView.py

No pin allocation, debug_config.json setting, Bluetooth command format, sensor
driver or recording format was changed.

Navigation controller version:

- `system.navigation_controller_version` is now 7.
- New telemetry phases are `recovery_reverse` and `recovery_turn`.
- New diagnostic telemetry:
  - `navigation.recovery_count`
  - `navigation.clearance_turn_count`

Non-aborting recovery:

- Failed obstacle-detour conditions no longer call `stopNavigation()`.
- These conditions now enter a generic recovery path while autonomous
  navigation remains enabled:
  - no immediately safe detour side;
  - lateral detour path blocked;
  - obstacle edge not found within the lateral search limit;
  - forward bypass path blocked;
  - obstacle extends beyond the forward detour limit;
  - return path to the original sweep lane blocked.
- Recovery first reverses away from the failed local plan.
- The normal recovery back-off is 350 mm once at least one side has opened.
- A recovery reverse is bounded at 800 mm before a new heading is attempted.
- The new turn is chosen toward the side ultrasound reporting the most room.
  If neither side is available, successive recoveries alternate direction.
- If the new recovery heading is still blocked, the robot does not immediately
  chain another in-place turn. It reverses again first, flips the intended turn
  direction, then reassesses.
- Once a clear recovery heading exists, stale sweep/detour geometry is
  invalidated and the robot returns to `seek_wall` so it can reacquire the
  arena and continue exploring rather than aborting autonomy.

End-of-run / repeated-spin correction:

- `NAV_CLEARANCE_TURN` is now explicitly counted.
- At most two blocked extra quarter-turn exits are allowed without translation.
- If those turns still end facing a wall, the controller enters
  `recovery_reverse` instead of continuing to rotate in a circle.
- A clear turn exit resets the clearance-turn count.
- Existing STOP, Debug Mode and global stopped-state safety behaviour is
  unchanged; those conditions can still intentionally stop navigation.

Internal-wall mapping:

- Arena View now keeps two distinct persistent geometry collections:
  - outer arena boundaries;
  - internal walls/obstacles.
- Confirmed outer boundaries remain solid red.
- Confirmed internal walls are drawn as dashed orange segments.
- Internal wall tracks require at least three agreeing wall observations before
  they are shown persistently.
- A range return substantially inside an already-established farther boundary
  is retained as internal-wall evidence instead of being discarded.
- If a farther coherent wall is later observed in the same cardinal direction,
  it can replace the earlier boundary candidate. The earlier candidate is
  demoted to internal-wall evidence rather than silently lost.
- The status line now reports the number of confirmed internal wall tracks.

Map self-correction from physical traversal:

- The existing encoder/IMU agreement gate is retained. If encoders report
  movement but the IMU has not confirmed the movement bout, virtual x/y does
  not advance.
- Wall self-correction is performed only after that existing translation gate
  has accepted the chassis movement.
- The confirmed robot path marks an approximately 210 mm-radius corridor as
  strong free-space evidence.
- If the confirmed path crosses a mapped internal wall, approximately a
  440 mm-wide corridor is carved out of that wall.
- A short false wall can therefore disappear completely.
- A real wall containing a traversable opening is split into the remaining
  segments on either side instead of the complete wall being deleted.
- If the robot physically crosses geometry previously labelled as an outer
  arena boundary, that boundary label is removed. Any surviving side pieces
  are retained as internal geometry and a later farther observation can
  establish the true arena boundary.
- Possible-weight votes inside a physically traversed corridor are also cleared.

Behaviour retained:

- Dynamic ultrasound-determined sweep width remains in place.
- Nominal sweep spacing remains 200 mm.
- Existing 8x8/top-TOF forward obstacle detection remains in place.
- Existing rectangular local obstacle detour remains in place.
- Existing near-contact 8x8 broad-wall handling and U-trap escape remain.
- Existing BNO055/encoder map-motion cross-check and point-turn/wall-slip
  suppression remain.
- Existing Arena View controls, raw sensor panels, replay support and telemetry
  remain.

Verification performed while preparing this change:

- The source baselines were checked against the current GitHub blob SHAs before
  modification:
  - BluetoothDebugWorkflow.cpp:
    `c265d65dfc6d042b0400b970562b8c7e0462fa4e`
  - BluetoothDebugWorkflow.h:
    `8dfc6d942445ffdd19ba8d1f9d66987ab0fe0714`
  - ArenaView.py:
    `aef26c046f83fc6986c555151174b656b909f766`
- `python -m py_compile ArenaView.py` passes.
- ArenaModel logic tests pass for:
  - nearer wall retained as internal geometry;
  - farther wall replacing an earlier false boundary;
  - physical traversal splitting an internal wall around the proven corridor;
  - physical traversal invalidating a false outer boundary while preserving
    untraversed pieces as internal geometry.
- C++ structural checks pass for balanced brackets/braces/parentheses, the new
  recovery states and telemetry, and removal of all obstacle-detour abort
  messages.
- Inside `updateNavigation()`, the only remaining `stopNavigation()` call is the
  existing global safety exit when Debug Mode is unavailable or the robot is
  globally stopped.
- A PlatformIO/Teensy build was not run in this environment because the project
  toolchain/repository checkout is not available here.
- The full PyQt GUI regression suite was not run here because PyQt6 is not
  installed in this environment.

Required local verification:

1. Build the firmware:
   `C:\Users\alex\.platformio\penv\Scripts\platformio.exe run`
2. Upload it and confirm Arena View reports navigation controller version 7.
3. Test a deliberately blocked turn and verify phase changes from
   `clearance_turn` to `recovery_reverse` rather than spinning indefinitely.
4. Test an internal wall/box and verify it is drawn dashed orange while the
   outer arena remains solid red.
5. Drive through a deliberately false mapped wall location and verify the
   crossed section is removed/split while unrelated wall sections remain.
6. Keep the physical STOP control ready during the first autonomous trial.


===============================================================================
61. SENSOR-FUSED WALL MAPPING / TOF NOISE REJECTION / WALL RECLASSIFICATION
===============================================================================

This change is based on the current testing branch commit:

- 4c6459844c4bf76db0783eb0aaca71bd3bfb78bb
  "Added new internal wall recognition, not working but yk"

Affected file:

- Non-Teensy-Files/BluetoothDebugGUI/ArenaView.py

No Teensy firmware, navigation state machine, pin allocation, Bluetooth packet,
debug_config.json setting, sensor driver or recording format was changed.
Navigation controller version remains 7.

Problem addressed:

The previous wall mapper promoted range endpoints into persistent wall geometry
too quickly. A few noisy point-TOF or 8x8 returns could therefore become dashed
orange internal-wall segments. Large numbers of those false segments could make
the map look like a maze even when later free-space evidence disagreed.

The mapper now uses a staged sensor-fusion pipeline:

  raw range -> temporal validation -> occupancy/free-space evidence
            -> pending wall evidence -> confirmed wall geometry
            -> internal/external classification

1. STRONGER TEMPORAL FILTERING

- Top point-TOF sensors no longer allow their first valid sample to become wall
  geometry immediately.
- A new top-TOF baseline must be supported by three mutually-consistent samples
  before that distance is accepted for wall mapping.
- The rolling median history was increased from 3 to 5 accepted samples.
- Bottom TOFs retain their immediate behaviour because they are primarily used
  for near-floor/weight logic rather than persistent wall creation.
- Ultrasound and 8x8 columns continue to require initial confirmation before
  contributing persistent geometry.

2. PENDING WALL-EVIDENCE FUSION

- A valid range endpoint is no longer converted directly into a wall track.
- Candidate wall endpoints are accumulated into spatially-compatible evidence
  clusters first.
- Approximate geometry matching for a pending cluster requires:
  - the same horizontal/vertical orientation;
  - perpendicular coordinate agreement within roughly 120 mm;
  - longitudinal continuity within roughly 220 mm.
- Source weights are intentionally unequal:
  - temporally-confirmed point TOF: 2.0 wall-confidence units;
  - coherent 8x8 sector: 2.5 wall-confidence units;
  - ultrasound: 0.75 wall-confidence units.
- A pending cluster needs at least 4.0 confidence units and at least two accepted
  observations before becoming persistent wall geometry.
- This means ultrasound mainly corroborates geometry rather than defining a
  precise wall by itself.
- Weak stale unconfirmed clusters decay instead of remaining forever.
- Free-space rays crossing a pending endpoint reduce its confidence before it
  can become a wall.
- The last approximately 100 mm of a point-TOF ray, and approximately 140 mm of
  an 8x8 sector, are excluded from this contradiction test so centimetre-level
  endpoint quantisation cannot cancel a genuine wall.

3. 8x8 SPATIAL SENSOR FUSION

The 8x8 matrix now has two separate spatial-consensus tests before it can create
persistent wall geometry.

Vertical consistency inside one matrix column:

- At least four valid pixels are required in the column.
- A median is calculated.
- At least four pixels must remain within max(80 mm, 12 percent of range) of
  that median.
- A few isolated flying pixels therefore cannot define the complete angular
  sector.

Horizontal consistency between columns:

- A matrix column is allowed to create persistent wall evidence only when an
  immediately adjacent column reports a similar depth.
- Adjacent depths must agree within max(100 mm, 15 percent of the nearer range).
- An isolated valid-looking 8x8 column still contributes low-confidence
  occupancy information, but it cannot create an orange/red wall line.
- Coherent neighbouring columns are given stronger occupied-cell evidence and
  wall-confidence weight.

4. OCCUPANCY-GRID CONFIDENCE

- Point-TOF endpoints now add less occupied evidence per sample than before.
- Ultrasound endpoints add only weak occupied evidence because of their broad
  acoustic beam.
- Coherent 8x8 sectors add strong occupied evidence; non-coherent sectors add
  only weak evidence.
- Every valid ray still adds free-space evidence in front of the measured
  endpoint.
- The visual occupancy layer no longer paints every +1 cell bright red or every
  -1 cell green:
  - red requires occupancy evidence >= +4;
  - green requires free-space evidence <= -3;
  - low-confidence cells are rendered as the dark map background.
- This is deliberately a visual confidence threshold; the underlying evidence
  values are still retained and can strengthen or weaken later.

5. INTERNAL WALLS ARE NOW THE DEFAULT CONFIRMED GEOMETRY

- A newly-confirmed wall is initially classified as an internal wall.
- It is drawn as a dashed orange segment only after the fused evidence stage has
  confirmed it.
- A single wall seen in one direction is no longer immediately declared to be
  one of the four arena boundaries.
- This prevents a genuine internal obstacle from becoming a false outer wall
  simply because it was encountered before the real arena boundary.

6. COLLINEAR INTERNAL WALL MERGING

- Nearby internal chunks with the same orientation are merged when:
  - their perpendicular coordinates are within approximately 220 mm; and
  - the gap along the wall is no more than approximately 350 mm.
- The merged line uses the median observed perpendicular coordinate and extends
  across the combined supported longitudinal range.
- This specifically addresses runs where the same physical wall appeared as a
  long sequence of separate orange dashes.

7. AUTOMATIC INTERNAL -> EXTERNAL WALL PROMOTION

A long merged internal line can now become a solid red arena boundary when the
explored geometry supports that classification.

Current promotion requirements:

- supported wall span >= 800 mm;
- at least 6 confirmed wall observations;
- at least 5 recorded robot-trail points on one side of the line;
- no more than 1 qualifying trail point on the opposite side.

This makes external-wall classification depend on BOTH wall evidence and known
free/explored space. For example:

- a long horizontal wall with explored trajectory only below it -> north arena
  boundary;
- a long horizontal wall with explored trajectory only above it -> south;
- a long vertical wall with explored trajectory only left of it -> east;
- a long vertical wall with explored trajectory only right of it -> west.

Once a line becomes an arena boundary:

- it is drawn solid red;
- later collinear confirmed chunks within roughly 200 mm extend the same red
  boundary instead of creating more orange segments beside it;
- a farther, better-supported outer line can supersede an earlier boundary.

8. ROBOT TRAJECTORY STILL OVERRIDES RANGE EVIDENCE

The existing encoder/BNO055 translation-validation logic remains intact.
Only IMU-confirmed chassis motion can correct wall geometry.

When the robot physically traverses a location:

- the approximately 210 mm radius chassis corridor receives strong free-space
  evidence;
- pending wall evidence in the traversed corridor is deleted;
- a crossed internal wall is shortened or split around the approximately
  440 mm proven-open corridor;
- a crossed outer boundary is demoted/invalidated;
- surviving pieces of crossed geometry are marked so they cannot immediately
  promote themselves back into an arena boundary.

Therefore a noisy TOF wall that the robot later physically drives through is
still removed, while a real wall with only one genuine gap retains the wall
sections on either side of that gap.

9. MAP STATUS / DISPLAY

- Header now describes the map as sensor-fused.
- Solid red = promoted external arena boundary.
- Dashed orange = confirmed internal wall.
- Low-confidence range speckle is not drawn as a wall.
- Status now reports:
  - confirmed external boundary count;
  - confirmed internal wall count;
  - current pending/unconfirmed wall-cluster count.

Verification performed while preparing this change:

- The source baseline was verified against current GitHub ArenaView.py blob:
  571eccc16b6b30abdf27b98a0b416e66af51d035
- `python -m py_compile ArenaView.py` passes.
- Synthetic ArenaModel tests pass for:
  - rapidly changing point-TOF noise producing no wall geometry;
  - repeated consistent point-TOF data becoming a confirmed internal wall;
  - one isolated 8x8 column NOT creating wall geometry;
  - adjacent coherent 8x8 columns creating wall evidence;
  - separated collinear internal segments merging into one long line;
  - a long one-sided internal line promoting to an external arena boundary;
  - later collinear segments extending the promoted external boundary instead
    of remaining separate internal-wall dashes;
  - confirmed chassis traversal carving/splitting false wall geometry.
- Full PyQt GUI regression tests were not run in this environment because PyQt6
  is not installed here.

Physical verification recommended:

1. Run Arena View with the robot stationary in front of an ordinary wall and
   confirm scattered TOF noise no longer immediately creates orange dashes.
2. Sweep the 8x8 across a broad flat wall and confirm coherent adjacent sectors
   still build wall geometry.
3. Put a genuine internal wall/box in the arena and confirm it first appears as
   dashed orange after repeated evidence.
4. Drive/sweep alongside a long arena edge and confirm its orange chunks merge
   and then promote into one solid red external boundary.
5. Deliberately drive through a previously false mapped wall location and verify
   that the contradicted section disappears/splits.
6. Keep the physical STOP control ready during autonomous testing.


===============================================================================
61. SENSOR-FUSED WALL MAPPING / TOF NOISE REJECTION / WALL RECLASSIFICATION
===============================================================================

This change is based on the current testing branch commit:

- 4c6459844c4bf76db0783eb0aaca71bd3bfb78bb
  "Added new internal wall recognition, not working but yk"

Affected file:

- Non-Teensy-Files/BluetoothDebugGUI/ArenaView.py

No Teensy firmware, navigation state machine, pin allocation, Bluetooth packet,
debug_config.json setting, sensor driver or recording format was changed.
Navigation controller version remains 7.

Problem addressed:

The previous wall mapper promoted range endpoints into persistent wall geometry
too quickly. A few noisy point-TOF or 8x8 returns could therefore become dashed
orange internal-wall segments. Large numbers of those false segments could make
the map look like a maze even when later free-space evidence disagreed.

The mapper now uses a staged sensor-fusion pipeline:

  raw range -> temporal validation -> occupancy/free-space evidence
            -> pending wall evidence -> confirmed wall geometry
            -> internal/external classification

1. STRONGER TEMPORAL FILTERING

- Top point-TOF sensors no longer allow their first valid sample to become wall
  geometry immediately.
- A new top-TOF baseline must be supported by three mutually-consistent samples
  before that distance is accepted for wall mapping.
- The rolling median history was increased from 3 to 5 accepted samples.
- Bottom TOFs retain their immediate behaviour because they are primarily used
  for near-floor/weight logic rather than persistent wall creation.
- Ultrasound and 8x8 columns continue to require initial confirmation before
  contributing persistent geometry.

2. PENDING WALL-EVIDENCE FUSION

- A valid range endpoint is no longer converted directly into a wall track.
- Candidate wall endpoints are accumulated into spatially-compatible evidence
  clusters first.
- Approximate geometry matching for a pending cluster requires:
  - the same horizontal/vertical orientation;
  - perpendicular coordinate agreement within roughly 120 mm;
  - longitudinal continuity within roughly 220 mm.
- Source weights are intentionally unequal:
  - temporally-confirmed point TOF: 2.0 wall-confidence units;
  - coherent 8x8 sector: 2.5 wall-confidence units;
  - ultrasound: 0.75 wall-confidence units.
- A pending cluster needs at least 4.0 confidence units and at least two accepted
  observations before becoming persistent wall geometry.
- This means ultrasound mainly corroborates geometry rather than defining a
  precise wall by itself.
- Weak stale unconfirmed clusters decay instead of remaining forever.
- Free-space rays crossing a pending endpoint reduce its confidence before it
  can become a wall.
- The last approximately 100 mm of a point-TOF ray, and approximately 140 mm of
  an 8x8 sector, are excluded from this contradiction test so centimetre-level
  endpoint quantisation cannot cancel a genuine wall.

3. 8x8 SPATIAL SENSOR FUSION

The 8x8 matrix now has two separate spatial-consensus tests before it can create
persistent wall geometry.

Vertical consistency inside one matrix column:

- At least four valid pixels are required in the column.
- A median is calculated.
- At least four pixels must remain within max(80 mm, 12 percent of range) of
  that median.
- A few isolated flying pixels therefore cannot define the complete angular
  sector.

Horizontal consistency between columns:

- A matrix column is allowed to create persistent wall evidence only when an
  immediately adjacent column reports a similar depth.
- Adjacent depths must agree within max(100 mm, 15 percent of the nearer range).
- An isolated valid-looking 8x8 column still contributes low-confidence
  occupancy information, but it cannot create an orange/red wall line.
- Coherent neighbouring columns are given stronger occupied-cell evidence and
  wall-confidence weight.

4. OCCUPANCY-GRID CONFIDENCE

- Point-TOF endpoints now add less occupied evidence per sample than before.
- Ultrasound endpoints add only weak occupied evidence because of their broad
  acoustic beam.
- Coherent 8x8 sectors add strong occupied evidence; non-coherent sectors add
  only weak evidence.
- Every valid ray still adds free-space evidence in front of the measured
  endpoint.
- The visual occupancy layer no longer paints every +1 cell bright red or every
  -1 cell green:
  - red requires occupancy evidence >= +4;
  - green requires free-space evidence <= -3;
  - low-confidence cells are rendered as the dark map background.
- This is deliberately a visual confidence threshold; the underlying evidence
  values are still retained and can strengthen or weaken later.

5. INTERNAL WALLS ARE NOW THE DEFAULT CONFIRMED GEOMETRY

- A newly-confirmed wall is initially classified as an internal wall.
- It is drawn as a dashed orange segment only after the fused evidence stage has
  confirmed it.
- A single wall seen in one direction is no longer immediately declared to be
  one of the four arena boundaries.
- This prevents a genuine internal obstacle from becoming a false outer wall
  simply because it was encountered before the real arena boundary.

6. COLLINEAR INTERNAL WALL MERGING

- Nearby internal chunks with the same orientation are merged when:
  - their perpendicular coordinates are within approximately 220 mm; and
  - the gap along the wall is no more than approximately 350 mm.
- The merged line uses the median observed perpendicular coordinate and extends
  across the combined supported longitudinal range.
- This specifically addresses runs where the same physical wall appeared as a
  long sequence of separate orange dashes.

7. AUTOMATIC INTERNAL -> EXTERNAL WALL PROMOTION

A long merged internal line can now become a solid red arena boundary when the
explored geometry supports that classification.

Current promotion requirements:

- supported wall span >= 800 mm;
- at least 6 confirmed wall observations;
- at least 5 recorded robot-trail points on one side of the line;
- no more than 1 qualifying trail point on the opposite side.

This makes external-wall classification depend on BOTH wall evidence and known
free/explored space. For example:

- a long horizontal wall with explored trajectory only below it -> north arena
  boundary;
- a long horizontal wall with explored trajectory only above it -> south;
- a long vertical wall with explored trajectory only left of it -> east;
- a long vertical wall with explored trajectory only right of it -> west.

Once a line becomes an arena boundary:

- it is drawn solid red;
- later collinear confirmed chunks within roughly 200 mm extend the same red
  boundary instead of creating more orange segments beside it;
- a farther, better-supported outer line can supersede an earlier boundary.

8. ROBOT TRAJECTORY STILL OVERRIDES RANGE EVIDENCE

The existing encoder/BNO055 translation-validation logic remains intact.
Only IMU-confirmed chassis motion can correct wall geometry.

When the robot physically traverses a location:

- the approximately 210 mm radius chassis corridor receives strong free-space
  evidence;
- pending wall evidence in the traversed corridor is deleted;
- a crossed internal wall is shortened or split around the approximately
  440 mm proven-open corridor;
- a crossed outer boundary is demoted/invalidated;
- surviving pieces of crossed geometry are marked so they cannot immediately
  promote themselves back into an arena boundary.

Therefore a noisy TOF wall that the robot later physically drives through is
still removed, while a real wall with only one genuine gap retains the wall
sections on either side of that gap.

9. MAP STATUS / DISPLAY

- Header now describes the map as sensor-fused.
- Solid red = promoted external arena boundary.
- Dashed orange = confirmed internal wall.
- Low-confidence range speckle is not drawn as a wall.
- Status now reports:
  - confirmed external boundary count;
  - confirmed internal wall count;
  - current pending/unconfirmed wall-cluster count.

Verification performed while preparing this change:

- The source baseline was verified against current GitHub ArenaView.py blob:
  571eccc16b6b30abdf27b98a0b416e66af51d035
- `python -m py_compile ArenaView.py` passes.
- Synthetic ArenaModel tests pass for:
  - rapidly changing point-TOF noise producing no wall geometry;
  - repeated consistent point-TOF data becoming a confirmed internal wall;
  - one isolated 8x8 column NOT creating wall geometry;
  - adjacent coherent 8x8 columns creating wall evidence;
  - separated collinear internal segments merging into one long line;
  - a long one-sided internal line promoting to an external arena boundary;
  - later collinear segments extending the promoted external boundary instead
    of remaining separate internal-wall dashes;
  - confirmed chassis traversal carving/splitting false wall geometry.
- Full PyQt GUI regression tests were not run in this environment because PyQt6
  is not installed here.

Physical verification recommended:

1. Run Arena View with the robot stationary in front of an ordinary wall and
   confirm scattered TOF noise no longer immediately creates orange dashes.
2. Sweep the 8x8 across a broad flat wall and confirm coherent adjacent sectors
   still build wall geometry.
3. Put a genuine internal wall/box in the arena and confirm it first appears as
   dashed orange after repeated evidence.
4. Drive/sweep alongside a long arena edge and confirm its orange chunks merge
   and then promote into one solid red external boundary.
5. Deliberately drive through a previously false mapped wall location and verify
   that the contradicted section disappears/splits.
6. Keep the physical STOP control ready during autonomous testing.
2026-09-22 navigation gap and floor-return update
-------------------------------------------------
- Reviewed the latest 19:19 recording and the current post-19:00 source before editing. The recording contains malformed/torn telemetry signal names, so some individual signal names in that file are not trustworthy; the navigation and mapping changes below do not depend on those damaged names.
- Added a Navigation tuning tab immediately above the raw range readouts in Arena View. APPLY NAVIGATION TUNING sends every value to the Teensy, stores it in the desktop settings, and records the change in an active .rdbg recording.
- Added three selectable navigation behaviours:
  - Balanced coverage: passes a confirmed opening only when it is approximately centred.
  - Gap explorer: can steer toward an off-centre opening when its measured width is sufficient.
  - Conservative wall-safe: never lets an apparent opening override a detected wall.
- Added live parameters for front stopping distance, side clearance, wall-follow distance, sweep spacing, physical robot width, gap safety margin, minimum open depth, number of ignored lower 8x8 rows, and number of frames required to confirm a gap.
- Gap recognition now searches all eight horizontal columns, not only the far-left and far-right flanks. It finds contiguous open columns, projects their angular width into millimetres at the nearest measured depth, and requires width >= robot width + safety margin. It also requires repeated frames before declaring the opening passable.
- The firmware publishes navigation.gap_passable, navigation.gap_width_mm, navigation.gap_centre_column_x2, navigation.strategy and navigation.matrix_floor_rows so recordings show exactly why a gap was or was not accepted.
- Bottom point TOFs no longer paint red/free occupancy into the arena map. They remain available in raw telemetry and are still paired with top measurements for purple possible-weight detection. This removes floor/chassis returns from the persistent wall map.
- The lower rows ignored from the 8x8 are adjustable and apply to both firmware navigation and desktop mapping. The default is two rows; use zero only if the module is level and confirmed not to see the floor.
- The firmware compiles successfully for Teensy 4.0. The generated embedded debug configuration includes the new navigation parameters.
2026-09-22 Bluetooth error-count diagnosis
------------------------------------------
- Investigated the reported 14 protocol errors using the newest recording and a live COM10 connection.
- The Bluetooth byte stream itself was valid JSON. The apparent protocol-error count was made from robot-generated error messages, not malformed packets.
- The Teensy had been running an incompatible firmware build with a different telemetry schema such as drive.left_percent, imu.heading and tof.array.r0c0. It rejected the current GUI handshake, parameter messages and autonomous_navigation command as Unknown command. Each rejection incremented the GUI counter, producing the reported total.
- Built and uploaded the current PlatformIO BluetoothDebugWorkflow firmware to the connected Teensy through its programming USB connection.
- Ran the read-only live reconnect diagnostic on COM10. It received 249 valid messages across an initial connection and reconnection, populated 116 telemetry rows, and reported zero link or robot errors.
- Renamed the GUI health counter from Protocol errors to Link/robot errors because the counter includes both serial-worker failures and valid error messages deliberately reported by the robot.
2026-09-22 Arena button command rejection diagnosis
---------------------------------------------------
- Reproduced the Arena View failures without moving the robot. Six RUN NAVIGATION clicks in the 19:44 recording each received a valid JSON error response saying Unknown command.
- Captured the live command definitions from COM10. The controller connected to Bluetooth currently advertises only drive_set, stop, set_debug_mode, servo_set, encoders_reset, bluetooth_probe and set_text_mode. It advertises only telemetry.interval_ms, drive.max_percent and servo.pulse_us parameters. It therefore cannot accept autonomous_navigation, run, or any navigation tuning parameter.
- The command-line upload was changed to the verifiable teensy-cli protocol. The programming USB Teensy entered its HalfKay bootloader and accepted the project image after a retry, but COM10 continued serving the unrelated generic command set. The USB Teensy and Bluetooth link are therefore not currently reaching the same running firmware/controller.
- No motor movement or autonomous navigation was commanded during diagnosis. Only debug-mode enable, run/stop command validation and one parameter lookup were tested; unsupported messages were rejected before actuation.
- To restore Arena controls, the programming USB cable must be attached to the same Teensy whose Serial1 connector carries the robot-side CH9143 Bluetooth board. Once that physical pairing is confirmed, upload the current PlatformIO firmware with teensy-cli and verify that COM10 advertises autonomous_navigation and the navigation.* parameters.
2026-09-22 verified Arena firmware upload
-----------------------------------------
- After the Teensy programming button was pressed, loaded the exact image at PlatformIO/.pio/build/teensy40/firmware.hex with teensy_loader_cli. The loader confirmed that it found the HalfKay bootloader, programmed the image and booted it.
- Queried COM10 after boot. The robot now advertises the complete project command set, including autonomous_navigation, run, stop, all Herkulex/DC motor/HX12K commands and TOF commands.
- Confirmed that COM10 advertises all navigation tuning parameters and produced ten live tof_8x8 frames during the verification window.
- Exercised Debug Mode, Run Motors, navigation.front_avoid_mm parameter update and Stop Motors without commanding wheel movement. The robot returned the expected state transitions and zero errors.

2026-09-22 latest navigation recording diagnosis for web handoff
----------------------------------------------------------------
Recording analysed:

- `Non-Teensy-Files/DataVisualisationAfter/Data/GAPEXPLOREREAGGRESSIVE.rdbg`
- Recorded from 20:44:55 to 20:46:40 NZST; 104.12 seconds of telemetry.
- Navigation ran from approximately 3.24 seconds until STOP at 101.51 seconds.
- This was not actually a Gap Explorer run despite the filename. The saved
  parameter snapshot has `navigation.strategy = 0` (Balanced), and the
  recording contains no navigation parameter-change events. The GUI tuning
  selection was therefore not applied to the controller for this run.

Main observed behaviour:

- The robot entered nine separately sampled reverse segments, totalling about
  16.94 seconds and approximately 3.55 metres of encoder-estimated reverse
  travel. This directly explains the visibly excessive backwards movement.
- Full reverse was deliberately commanded by the navigation controller:
  motor-bank-2 channels A and B were both +100 percent in recovery reverse.
  Normal forward travel was approximately A=-100, B=-100. The motor driver did
  not spontaneously reverse and the encoder directions match the commands.
- The reverse/recovery sequences were concentrated at approximately 24-32 s,
  42-45 s, 68-69 s, 84-85 s, and 94-102 s. The final sequence alone repeatedly
  alternated reverse and nominal recovery-turn states until STOP was pressed.
- Logs explicitly reported `Recovery heading still blocked; backing away
  again` at about 25, 27, 29, 42, 94, 96, 97, 99 and 100 seconds.
- Five recovery episodes were counted. Only about one second of real sweep
  operation occurred before an obstacle detour and recovery reset the robot to
  wall-seeking again. The algorithm therefore spent most of the test repeatedly
  reacquiring a first wall instead of mapping in lanes.

Confirmed recovery-state defect:

- `beginRightAngleTurn()` constructs its target from
  `navigationHeadingReferenceDeg_ +/- 90 degrees`, meaning the heading of the
  last completed forward leg, rather than from the robot's current heading.
- When an initial/corner turn ends facing another wall, no new forward leg has
  begun, so the old heading reference has not been updated. Recovery then asks
  for the same 90-degree target the robot has already reached.
- This is visible directly in the recording. Around 25-29 s the IMU heading is
  about 262-268 degrees and the recovery target remains about 266.4 degrees.
  Around 94-102 s the heading is about 353-355 degrees and the target remains
  about 356.4 degrees. The nominal `recovery_turn` therefore performs almost no
  useful rotation, sees the same wall again, and commands another reverse.
- `NAV_RECOVERY_REVERSE` also recalculates the turn side from the latest
  ultrasound values before every attempt. That can overwrite the intended
  alternating direction and repeatedly choose the same apparently open side.
- Recommended correction: recovery and clearance turns must calculate the next
  target from the current valid IMU heading at recovery entry, retain that
  target/direction for the whole attempt, and force a materially different
  heading after a blocked attempt. Do not reuse the stale forward-leg heading
  reference. Add a maximum recovery-attempt count and choose a 180-degree exit
  or stop/replan after repeated blocked headings rather than accumulating
  multiple 350-800 mm reverse legs.

Why the almost-boxed-in escape was unreliable:

- The special U-shaped escape requires front blocked AND both side ultrasound
  distances below 350 mm at the same update. That condition was usually not
  satisfied. During failed recoveries the left side was commonly 211-324 mm,
  but the right side was often reported between roughly 564 mm and 4.3 m, or
  briefly as zero/unknown. The controller consequently treated this as an
  ordinary blocked turn instead of a U-shaped enclosure.
- One right-ultrasound sample was only 36 mm while neighbouring values were far
  larger. The side data therefore contains at least one extreme transient.
  Single side samples should be validity/range checked and median-filtered
  before they select a recovery direction.
- The forward 8x8 classified a broad wall for 50 of 105 telemetry samples
  (47.6 percent), including the end of the run. A passable gap was confirmed in
  only 7 of 105 samples (6.7 percent), and the final gap state was false. The
  robot was genuinely being told that the forward view remained blocked; the
  failure was the recovery response, not absence of forward-wall detection.
- Gap Explorer still needs a separate later test after applying strategy 1.
  This recording tests Balanced strategy and must not be used as evidence that
  the Gap Explorer selection itself worked or failed.

Motion and communication evidence:

- `navigation.motion_consistent` was true for 98 of 105 samples (93.3 percent).
  Encoder rates changed consistently with the commanded forward, reverse and
  turn directions, so the strange path is principally commanded behaviour.
- The IMU heading followed the turns and was close to the saved target during
  the repeated failed recovery attempts. This further confirms that the robot
  was not merely ignoring turn motor commands; the target itself was stale.
- Bluetooth dropped-message and dropped-byte counters remained zero for the
  full recording. Maximum main-loop gap stayed tightly between 40.59 and
  43.64 ms. This run was not caused by a telemetry stall or overloaded loop.

Arena-view geometry issue:

- The recording embeds `track_width_mm = 40.0` while the wheels are about
  80 mm diameter and the photographed chassis is much wider than 40 mm. If
  `track_width_mm` is intended to be drive-wheel centre-to-centre spacing, 40 mm
  is physically implausible and likely missing a zero. This must be measured
  directly; do not blindly change it to 400 mm without measurement.
- A track width ten times too small makes encoder-derived yaw roughly ten times
  too large. Even with IMU correction this can severely distort pose history,
  sensor projection and the drawn arena during turns/reversals. Correcting the
  recovery controller will not by itself repair the displayed map until the
  real wheel-centre spacing is entered.

Recommended next order of work:

1. Measure and enter the true centre-to-centre drive-wheel track width.
2. Fix recovery targets to be relative to the current IMU heading and prevent
   the side-choice logic from overwriting the selected alternating direction.
3. Bound the number and total distance of consecutive recovery reverses.
4. Filter/validate side ultrasound data and allow a boxed-area classification
   to persist briefly instead of requiring three simultaneous raw readings.
5. Explicitly APPLY strategy 1 in Arena View, verify a recorded parameter event
   and `navigation.strategy = 1`, then perform a new Gap Explorer test.
6. Reassess arena mapping only after odometry geometry and recovery motion are
   corrected; the present map is being fed a physically incorrect pose path.


2026-09-22 navigation recovery v8 fix from GAPEXPLOREREAGGRESSIVE diagnosis
----------------------------------------------------------------------------
Basis for this change:

- Re-read the current testing branch at commit
  `7e6b6bcfc4efa5c9a50dd6a3915166445f291a01` and the latest status.txt before
  editing.
- Reconstructed the three edited source baselines and verified the firmware
  source/header against the exact current Git blob SHAs before applying the
  fix:
  - BluetoothDebugWorkflow.cpp: 68aad83d56c96f71f963c357f99890d12b8b128f
  - BluetoothDebugWorkflow.h:   5136fa7c9251f7c2d5ee3843d97024915920a210
  - ArenaView.py:               75d12b53fcead82d223eba7c7a3f2ced5abb4b48
- The fix is specifically based on the recorded failure in
  GAPEXPLOREREAGGRESSIVE.rdbg. The recording showed repeated reverse/recovery
  cycles where the recovery target was almost the same heading the robot had
  already reached.

Confirmed root cause addressed:

- Normal coverage turns still use the exact navigation heading lattice. This is
  retained because it prevents lane-to-lane angular error from accumulating.
- Recovery, U-trap escape and clearance turns no longer use the stale
  navigationHeadingReferenceDeg_ from the previous forward leg.
- A new beginRelativeTurnFromCurrent() helper constructs those targets from the
  current valid BNO055 heading. If the IMU is temporarily unavailable it falls
  back to the current navigation target rather than the stale forward-leg
  reference.
- This directly addresses the recorded cases where the chassis was already at
  about 266 degrees while recovery repeatedly targeted about 266.4 degrees, and
  later was already near 354 degrees while the target stayed near 356.4 degrees.

Recovery direction locking:

- startRecovery() now chooses the first escape side once and keeps that choice
  locked throughout that reverse/turn attempt.
- NAV_RECOVERY_REVERSE no longer recalculates navigationRecoveryTurnRight_ from
  whichever raw ultrasound sample happens to arrive immediately before the
  turn.
- If a genuine 90-degree recovery exit is still blocked, the next attempt is
  explicitly forced to the opposite side. The reverse state cannot overwrite
  that alternation.

Side-ultrasound filtering:

- Recovery/U-trap decisions now use a separate three-sample side-ultrasound
  filter instead of one raw reading.
- Samples are added only when the sensor fallCount changes, so the fast main
  loop cannot count the same echo repeatedly.
- Only 30-5000 mm samples are accepted.
- At least two fresh completed echoes are required. Two samples use their mean;
  three samples use the median.
- Filtered side data is retained for up to 800 ms. This also gives the boxed-in
  classification short persistence instead of requiring both side readings to
  happen to be close on exactly the same raw update.
- This is intended to reject the isolated approximately 36 mm right-ultrasound
  transient seen in the failed recording while preserving genuine close walls.

Bounded recovery motion:

- Normal recovery reverse target reduced from 350 mm to 300 mm.
- Maximum single recovery reverse leg reduced from 800 mm to 450 mm.
- Consecutive recovery attempts are counted and total reverse distance is
  accumulated.
- Maximum ordinary recovery budget is three attempts or 1200 mm total reverse,
  whichever is reached first.
- Once that budget is reached the controller commands a 180-degree turn from
  the CURRENT IMU heading rather than continuing an unbounded reverse/90-degree
  cycle.
- If the forced 180-degree heading still reports a wall, the controller does
  not immediately begin another long reverse loop. It hands control back to
  wall acquisition on the materially different heading so navigation can
  re-plan from there.

Existing U-shaped escape / clearance behaviour:

- The front+left+right trapped test now uses the filtered side distances.
- NAV_ESCAPE_REVERSE uses filtered side-open evidence and its turn is generated
  from the current IMU heading.
- Extra clearance turns are also generated relative to current IMU heading.
- Existing front 8x8 broad-wall detection, physical-width gap detection,
  Gap Explorer behaviour, obstacle detours, dynamic arena-width sweeps and the
  exact normal sweep heading lattice are retained.

Arena View navigation tuning fix:

- Clicking RUN NAVIGATION now first calls the same Apply Navigation Tuning path
  used by the manual APPLY NAVIGATION TUNING button, then sends
  autonomous_navigation enabled=true.
- This means the currently visible Algorithm selection and every visible
  navigation tuning value are sent before autonomy starts.
- This prevents a recording named/tested as Gap Explorer from silently running
  the previous Balanced strategy because APPLY was forgotten.
- Arena View now expects navigation controller version 8.

New diagnostic telemetry:

- system.navigation_controller_version = 8
- navigation.recovery_logic_version = 2
- navigation.recovery_attempt
- navigation.recovery_total_reverse_mm
- navigation.recovery_force_half_turn

Deliberately NOT changed:

- Arena View's current track_width_mm value/migration has not been changed.
  The latest recording contains track_width_mm = 40.0, which appears physically
  implausible for this chassis if it represents drive-wheel centre-to-centre
  spacing, but the real value must be measured. Do not replace it with a guessed
  400 mm value. Enter the actual measured centre-to-centre drive-wheel spacing
  before judging the map geometry during turns.

Verification performed here:

- Python syntax compilation of the final ArenaView.py passed.
- C++ braces, brackets and parentheses were checked after stripping comments and
  strings; all were balanced with no negative nesting.
- Verified the new header members are referenced by the firmware.
- Verified controller version 8, current-heading recovery helper, filtered side
  median, three-attempt/1200-mm bound and forced 180-degree path are present.
- Verified the old recovery pattern that recalculated the side from raw
  left/right inside NAV_RECOVERY_REVERSE is absent.
- Verified Arena View applies tuning before emitting the RUN NAVIGATION command.
- Full PyQt interaction tests were not run in this environment because PyQt6 is
  not installed here.
- Full PlatformIO Teensy compilation was not run here because the complete repo
  toolchain/project is not mounted in this container. Build locally before
  uploading.

Recommended next test:

1. Measure the real drive-wheel centre-to-centre track width and enter it in
   Arena View before evaluating map shape.
2. Build/upload this firmware and verify telemetry reports
   system.navigation_controller_version = 8.
3. Select Gap Explorer in Arena View and click RUN NAVIGATION directly. Confirm
   navigation.strategy becomes 1 in telemetry/recording without separately
   pressing APPLY NAVIGATION TUNING.
4. Recreate the previously difficult boxed/internal-wall section with STOP ready.
5. Watch navigation.phase, navigation.recovery_attempt,
   navigation.recovery_total_reverse_mm and navigation.target_heading_deg.
6. Every recovery_turn target should now be about +/-90 degrees from the actual
   heading at that attempt, rather than approximately equal to it.
7. If repeated exits remain blocked, the recovery should be bounded and move to
   the forced 180-degree re-plan instead of accumulating metres of reverse.

2026-09-22 22:12 run analysis and permanent-wall mapping update
---------------------------------------------------------------
Newest recording reviewed:

- `Non-Teensy-Files/DataVisualisationAfter/Data/RobotRun_2026-09-22_22-12-08.rdbg`
- Duration 117.37 seconds. Navigation ran from 2.76 seconds to STOP at 116.24
  seconds.
- The broken ultrasound period was deliberately excluded from the algorithm
  assessment below. Conclusions use navigation state, IMU, encoders, top TOF,
  8x8 forward data and motor commands.
- This run correctly applied Conservative wall-safe strategy 2 before starting.
  It also applied 150 mm front clearance, 100 mm side clearance, 100 mm
  wall-follow distance, 250 mm lane spacing, 430 mm robot width, 40 mm gap
  margin, three ignored lower matrix rows and 60-degree matrix FOV.

What improved and what remains:

- Navigation controller version 8's bounded recovery is a major improvement
  over the previous recording. Only one recovery episode occurred. It reached
  attempt 2 and about 756.6 mm total recorded reverse, then found a clear
  current-heading exit. It did not enter the former multi-metre endless reverse
  loop and did not need its forced half-turn limit.
- Motion consistency was true in 71 of 80 navigation telemetry samples (88.75
  percent). The controller, motor commands, encoders and IMU were generally
  describing the same motion.
- The remaining weakness is exploration policy, not simply collision sensing.
  The controller repeatedly cycles through seek-wall, right-angle turn and
  clearance-turn states. It only completed brief sweep phases, so it does not
  maintain a durable concept of which free regions have already been searched.
- The 8x8 reported a broad wall in 35 of 80 samples (43.75 percent). A passable
  gap was confirmed in only 6 samples (7.5 percent). Conservative strategy is
  therefore behaving as designed, but it is inherently reluctant to explore
  uncertain openings and is a poor primary search policy for complete coverage.
- The recording contains mangled duplicate signal names, although the core
  navigation signals above remain readable. Three transmit frames/1783 bytes
  were already counted as dropped before or at the start and did not increase
  during the run. Main-loop timing remained stable at 40.58-43.65 ms. Preserve
  the transport diagnosis as a separate issue when interpreting fine timing.

Recommended searching algorithm:

- Replace the top-level reactive wall-seek/sweep loop with frontier-based
  exploration on a coarse occupancy grid, using A* for global paths and the
  existing TOF/8x8 collision logic as the local safety/controller layer.
- A 100 mm planning grid is sufficient for the supplied 2.4 x 4.9 m arena: only
  about 24 x 49 = 1176 cells, small enough for deterministic A* on the Teensy.
  Keep the 10 mm GUI grid for display, but downsample it for planning.
- Maintain three planning states per cell: unknown, confirmed free and occupied.
  Inflate occupied cells by half the robot width plus a configurable clearance
  margin before path planning. Never plan through an unknown cell unless the
  chosen target is its adjacent frontier.
- A frontier is a confirmed-free cell adjacent to unknown space. Score reachable
  frontiers by expected new area minus A* travel cost and turn cost. Drive to
  the best score, rescan, update the grid and repeat. Mark unreachable frontiers
  temporarily rather than falling back into an unbounded recovery loop.
- Use IMU heading for turn control, calibrated encoders for translation and the
  8x8/top TOFs for local wall confirmation. Re-plan immediately when the local
  sensor layer blocks the next path segment.
- Once no reachable frontiers remain, run boustrophedon/lawnmower coverage only
  inside the confirmed reachable free-space mask. This gives orderly close
  passes without assuming that the entire arena is one unobstructed rectangle.
- This hybrid is preferred over pure wall following, Bug algorithms or the
  current fixed sweep state machine because the task requires remembering where
  the robot has searched, deliberately entering sufficiently wide openings and
  handling internal obstacles/dead ends without repeatedly reacquiring the same
  first wall.

Arena painting change implemented:

- Wall geometry now records independent verification tokens. Adjacent 8x8
  columns in one frame count as one verification, not multiple independent
  confirmations.
- A wall locks after agreement from three distinct sensor frames. This is a
  deliberate compromise: isolated speckle cannot become permanent, while a real
  wall seen repeatedly becomes a fixed landmark quickly.
- Once locked, a wall's perpendicular coordinate is frozen. Later agreeing
  observations may extend its length and increase confidence but cannot slide
  the whole wall as pose estimates drift.
- Locked internal walls and locked arena boundaries are never deleted, split or
  demoted by chassis-path carving. A later outward parallel candidate cannot
  replace an already locked boundary.
- Provisional geometry remains correctable. Before three-frame lock it may still
  be weakened by free-space rays or carved when confirmed chassis translation
  proves that location traversable.
- Rendering now distinguishes map maturity: thick solid lines are permanent
  three-frame-locked landmarks; thin/dashed lines are provisional geometry.
  Arena status shows the number of locked permanent walls.

Verification:

- `ArenaView.py` passes Python syntax compilation.
- Focused tests pass for three independent frames causing lock, locked geometry
  surviving a false odometry crossing, locked coordinates remaining fixed while
  their tangent span extends, and provisional geometry remaining carveable.
- The full historical `test_arena_view.py` suite currently contains unrelated
  failures from older tests that initialise only the retired single
  `mm_per_count` calibration while current mapping requires separate encoder
  scales, plus tests that predate the current initial range-confirmation filter.
  The three new permanence tests pass independently.

Frontier grid navigation option implemented (2026-09-22):

- Added a fourth navigation strategy in the GUI named `Frontier grid + A*`.
  The three existing reactive strategies remain available and unchanged.
- Added the `FrontierExplorer` Teensy library. It maintains a 64 x 64 local
  occupancy grid at 100 mm per cell, centred on the robot's starting pose.
- Encoder translation and BNO055 heading update the planner pose. Point TOF,
  left/right ultrasound and all eight horizontal columns of the 8x8 TOF update
  free and occupied evidence. Each 8x8 column uses a vertical median so one
  isolated bad pixel does not define an obstacle.
- Planning traverses confirmed-free cells, inflates occupied cells by half the
  configured robot width plus the configured gap margin, finds reachable
  frontiers next to unknown space and builds a shortest grid path to the best
  frontier. The score favours frontiers exposing more unknown cells while
  penalising longer travel and unnecessary heading change.
- The robot follows the resulting cell waypoints with BNO055 heading control at
  full configured navigation power. A newly blocked forward route invalidates
  the current path and triggers a re-plan instead of continuing into it.
- If the initial map does not yet contain a reachable frontier, the robot turns
  in place to scan. Completion is latched only after the explored map is large
  enough and no reachable unknown boundary remains; a new Run Navigation
  command starts a fresh map.
- Added planner telemetry for local pose, known-cell count, frontier count,
  path length, re-plan count and completion. The navigation controller version
  reported to the GUI is now 9.
- The GUI and firmware both accept navigation strategy value 3. Select
  `Frontier grid + A*` on Arena View before pressing Run Navigation.
- Removed the temporary programming-USB boot and one-second heartbeat prints
  used during the earlier Bluetooth diagnosis.

Verification of the frontier option:

- Teensy 4.0 release firmware compiles successfully. Final build size is
  150040 bytes of code plus 28024 bytes of data; 289536 bytes of RAM1 remain
  available for local variables.
- `ArenaView.py` passes syntax compilation.
- The three focused permanent-wall regression tests pass after the navigation
  option was added.

Frontier controller smoothing update (2026-09-22):

- The initial frontier controller was physically erratic because it discarded
  and regenerated a valid route every second, used each adjacent 100 mm grid
  cell as a precision target and applied immediate full-power opposite turns
  around a small heading threshold.
- A valid route is now held until its frontier is reached or live obstacle data
  genuinely invalidates it. Sensor-map updates continue while following it.
- The waypoint follower now uses a 130 mm rolling look-ahead, allowing it to
  pass through the immediately adjacent grid cell without stopping or making a
  fresh steering decision every 100 mm.
- Heading changes now enter at 20 degrees and use the established IMU
  pulse/coast/settle turn controller. It coasts before reversing turn torque,
  pulses the required 100 percent motor command near the target and must settle
  inside 4 degrees before forward travel resumes.
- The turn state and route state reset cleanly on every new navigation run and
  whenever a real forward blockage invalidates the path.
- The smoothed Teensy firmware compiles successfully: 150104 bytes code,
  28024 bytes data and 289504 bytes RAM1 free for local variables.

Frontier run diagnosis and mapping correction (recordings ending 22:49 and
22:51 on 2026-09-22):

- The short final recording selected strategy 3 at 7.75 seconds and started at
  8.32 seconds, but incorrectly announced exploration complete at 19.56
  seconds. The longer named recording showed the same defect repeatedly, with
  several runs ending after roughly one to six seconds.
- This was a planner defect rather than successful coverage. `No reachable
  frontier right now` was being treated as `the arena is fully explored` after
  only a small number of sensor rays.
- Sensor rays were also projected from the chassis centre. The physical forward
  sensors are about 245 mm ahead of the centre and the side ultrasounds are
  about 195 mm left and 190 mm right. A wall 170 mm beyond the front sensor was
  consequently inserted 245 mm too close and obstacle inflation could cover
  the robot's entire starting free patch.
- Frontier ray projection now includes those sensor origins before applying
  sensor angle and measured range. Forward point/8x8 rays originate at about
  right 25 mm, forward 245 mm; left and right ultrasound rays originate at
  their respective side positions.
- Failure to find a current route no longer declares completion. The robot now
  makes one controlled 45 degree IMU turn, settles, collects a new view and
  tries planning again. It does not perform an unbounded full-power scan.
- A close front reading during an intentional scan/route turn is expected and
  no longer repeatedly cancels that turn. It only invalidates a route while the
  robot is actually in the forward-follow phase.
- Occupancy known-cell accounting now correctly decreases if contradictory
  evidence returns a cell to unknown.
- Corrected firmware compiles successfully: 150360 bytes code, 28024 bytes
  data and 289504 bytes RAM1 free for local variables.

Automatic arm sorting added (2026-09-22):

- Added an `Arm sorting` command block with `ARM SORTING ON` and
  `ARM SORTING OFF` buttons. It requires Debug Mode and Run Robot.
- Turning sorting on reads Herkulex ID 4 at its current mechanical position and
  stores that as the software zero. It then commands the sorting gate to -30
  relative degrees and sets the paired HX12K bumpers OFF (C=130, D=0).
- The sorting decision uses the configured D21 input's debounced logical
  `digital.inductive_proximity.detected` value, including its active-low
  configuration. It does not use the raw electrical level.
- A new detection must remain continuously true for 500 ms. If it is still
  detected, the bumpers turn ON (C=0, D=130) and Herkulex 4 moves to +30
  relative degrees.
- When detection clears, the bumper deliberately remains ON for another two
  seconds so the weight can move fully off it. The gate independently remains
  at +30 degrees for five seconds, then returns to -30. The periodic timer is
  restarted when detection clears.
- While nothing is detected, the bumpers pulse ON for one second every 20
  seconds. The gate remains at -30 degrees during this mode.
- The automation is non-blocking apart from the short established Herkulex
  command transaction. The 500 ms confirmation and 20 second period use loop
  timestamps, so Bluetooth, sensing and navigation continue updating.
- STOP ROBOT disables the sorting automation and disables the HX12K outputs.
  Sorting must be explicitly turned on again after a stop.
- Added recording telemetry: `sorting.enabled`, confirmation pending/confirmed,
  bumper state, gate target, both remaining hold times and milliseconds to the
  next periodic pulse.
- Firmware and embedded command definitions compile successfully. Final build:
  152408 bytes code, 29048 bytes data and 288480 bytes RAM1 free for local
  variables.

Reactive exploration option and 8x8 orientation correction (2026-09-22):

- Added navigation strategy 4, shown in Arena View as `Reactive gap wander (no
  map planning)`. It does not build or consult the Teensy's frontier occupancy
  grid. The desktop may still show live sensor rays, pose and observed evidence
  so the operator can see what the robot sees.
- Normal wandering drives at 100 percent with BNO055 heading hold. Every five
  to nine seconds it makes a controlled 30 degree exploration turn when both
  sides are clear, preventing a permanent straight wall-to-wall shuttle.
- A broad frontal obstacle causes a controlled approximately 85 degree turn
  toward the clearer side. A confirmed, physically passable off-centre 8x8 gap
  instead causes a smaller approximately 35 degree correction toward the gap.
- A single close side ultrasound causes a 25 degree steer away. If front, left
  and right form a U-shaped trap, the robot reverses until at least one side is
  clear after 200 mm, with a hard 650 mm reverse cap, then turns toward the
  clearer exit.
- Direction ties alternate rather than always selecting clockwise. Turns use
  the existing IMU pulse/coast/settle controller, and bottom TOFs remain absent
  from navigation decisions.
- Added `navigation.wander_avoidance_count` telemetry and explicit
  `wander_drive`, `wander_turn` and `wander_reverse` phases for recordings.
- Expanded the navigation strategy parameter range to 0-4 and embedded the new
  definition in the firmware. Firmware compiles successfully: 153816 bytes
  code, 30072 bytes data and 287424 bytes RAM1 free for local variables.
- Corrected the installed 8x8 TOF left/right display orientation. The previous
  app default applied a mirror; existing saved orientation is inverted once by
  settings migration version 2 and new settings default to unmirrored.
- The 8x8 tab and Arena View receive the same corrected orientation. The manual
  `Flip left/right for robot POV` checkbox remains available for verification
  or a future physical remount.
- `ArenaView.py` and `DebugGUI.py` pass Python syntax compilation, and the three
  focused permanent-wall regression tests still pass.

Outer top long-range TOFs added (2026-09-22):

- Added `top_right_right`, a long-range point TOF on XSHUT6, and
  `top_left_left`, a long-range point TOF on XSHUT7. This fills all eight
  supported XSHUT channels while retaining the six existing point sensors.
- Both sensors initialise through the existing SX1509 XSHUT/address-assignment
  workflow and publish separate availability, timeout and distance telemetry as
  `tof.top_right_right.*` and `tof.top_left_left.*`.
- They are top-layer navigation sensors. XSHUT7 contributes to robot-left
  clearance and XSHUT6 contributes to robot-right clearance. They are not
  folded into the straight-ahead median, so seeing an angled side wall does not
  falsely report a wall directly in front.
- Balanced, gap, conservative and reactive-wander modes receive the new ranges
  through their common left/right avoidance field. Reactive wander therefore
  uses them when choosing the clearest turn and when steering away from a close
  side.
- Frontier mode records the two sensors as independent rays from their actual
  outer positions at default bearings of -45 degrees for XSHUT7 and +45 degrees
  for XSHUT6. It no longer projects their combined minimum as a false 90 degree
  ultrasound ray.
- Added both sensors to the editable Wiring Guide with a one-time migration
  that preserves all existing custom rows. Arena View creates separate raw
  readouts and sensor-layout controls for both.
- Default Arena View geometry is top layer, approximately 180 mm to each side,
  120 mm forward and +/-45 degrees. These values remain editable and persisted
  because the precise fitted angles should be measured on the robot.
- Firmware compiles successfully: 154328 bytes code, 30072 bytes data and
  287424 bytes RAM1 free for local variables. GUI syntax checks and all seven
  Wiring Guide tests pass.

Weight detection and visited-area wandering memory (2026-09-22):

- Added firmware weight detection using four matched vertical point-TOF pairs:
  bottom/top far left, middle left, middle right and far right.
- A low object is present when the bottom sensor returns 30-1200 mm and its
  matching top sensor sees at least 150 mm farther. This distinguishes a short
  weight from a wall visible at both heights.
- Each sector requires three consecutive fresh 100 ms point-TOF polls (about
  300 ms) before confirmation. Re-reading one sample in the fast control loop
  cannot create a false multi-sample confirmation.
- Added `weight.detected`, `weight.sector_mask`, `weight.nearest_mm` and signed
  `weight.direction` telemetry. Sector mask bits run far-left through far-right;
  direction values are -2, -1, +1 and +2.
- Reactive gap wander biases once toward each newly confirmed weight sighting:
  about 18 degrees for a middle sector and 35 degrees for an outer sector. Top
  TOFs and the 8x8 remain authoritative for wall avoidance, so a bottom-only
  weight is not incorrectly handled as a wall.
- Added a separate 32 x 32 visited-cell memory at 200 mm resolution for reactive
  wander. It stores breadcrumbs only, not obstacles or walls. Encoder distance
  plus BNO055 heading update the local pose.
- Cells are counted when entered rather than on every control-loop iteration.
  Re-entering an old cell triggers a 45 degree choice toward the less-visited
  side. Live clearances still override the preference when retracing is the
  only safe way out, avoiding a self-imposed trap.
- Periodic exploration and equal-clearance turn ties also prefer the projected
  side with fewer prior visits. The breadcrumb grid resets on every new
  navigation run.
- Added wander local pose and visited-cell count to recorded telemetry.
- Firmware compiles successfully: 155864 bytes code, 30072 bytes data and
  286368 bytes RAM1 free for local variables.




2026-09-22 pre-laid mission strategy and graphical arena planner
----------------------------------------------------------------
Baseline and scope:

- Built on the current testing-branch navigation/GUI work plus the recovery-v8
  handoff, preserving the current gap-explorer tuning, sensor-fusion mapper,
  non-aborting recovery changes, Bluetooth transport changes and raw sensor UI.
- Navigation controller telemetry version is now 9.
- Files changed in this handoff:
  - PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp
  - PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.h
  - Non-Teensy-Files/BluetoothDebugGUI/ArenaView.py
- No GitHub write, commit or push was performed.

New strategy 3: Pre-laid mission

- Arena View now exposes a fourth navigation strategy named Pre-laid mission.
- The operator lays out the mission before starting the robot. RUN NAVIGATION
  first applies the visible navigation tuning, plans the route, uploads the
  route to the Teensy and only then starts autonomous navigation.
- Firmware accepts two built-in GUI protocol commands without requiring new
  debug_config command definitions:
  - mission_plan_set
  - mission_plan_clear
- A mission plan contains the selected start position and start heading, a
  compact waypoint polyline, flags identifying real-weight visit waypoints and
  the roaming strategy to use after all real weights have been visited.
- A maximum of 64 uploaded mission waypoints is enforced in both the GUI and
  firmware so the command remains bounded for the Bluetooth receive buffer.

Graphical pre-laid arena editor:

- A dedicated Pre-laid mission tab now sits beside the live discovered map.
- The pre-laid arena is a fixed horizontal rectangle, 4900 mm wide by 2400 mm
  high by default.
- Two fixed home bases are drawn on the left short side:
  - green home base at the top-left;
  - blue home base at the bottom-left.
- The operator selects My home as Green or Blue. The other home base is drawn
  with a red dashed overlay and labelled NO-GO ZONE. The path planner treats
  that whole base plus robot clearance as forbidden space.
- The operator can place and drag:
  - the robot start position;
  - real weight locations;
  - dummy weight locations;
  - walls;
  - ramps;
  - tubes.
- Walls and ramps can be rotated 90 degrees with Rotate selected.
- Selected non-start items can be deleted.
- Start heading is selectable as East, South, West or North.
- The whole editable layout is persisted in QSettings under
  navigation/mission_layout so it survives GUI restarts.

Path planning and obstacle handling:

- PLAN ROUTE runs an A* grid planner in arena coordinates.
- Robot radius is derived from the visible robot-width tuning value and the
  planner safety inflation follows the visible gap-margin value.
- The arena border, opponent home base, walls, ramps, tubes and dummy weights
  are inflated by the robot footprint/safety margin and treated as no-go
  geometry.
- Real weights are goals, not obstacles.
- The planner repeatedly evaluates the remaining real weights and chooses the
  reachable next target with the lowest A* path cost, producing a complete
  route through every real weight.
- Grid paths are line-of-sight simplified before upload. This keeps waypoint
  count low while retaining obstacle clearance.
- If a start or real weight cannot be reached safely, the GUI refuses to upload
  the mission and displays a ROUTE ERROR instead of producing a dangerous path.
- Dummy weights are never marked as visit targets; they only influence path
  clearance.
- The planned route is drawn in cyan. Real-weight visit waypoints are numbered
  on the route so the planned visit order is visible before the run.

Robot path following / self correction:

- When strategy 3 starts, the Teensy aligns the user-selected arena start
  heading to the current valid BNO055 heading. It will hold the motors stopped
  until a valid IMU sample exists.
- Mission coordinates use +X to the right/east, +Y downward/south and clockwise
  positive heading, matching the graphical editor and BNO055 yaw convention.
- The Teensy maintains its own mission pose from calibrated encoder travel plus
  BNO055 heading. Encoder 1 uses the currently established inverted sign and
  encoder 2 the forward sign from the straight-run calibration.
- For every update it recomputes the bearing from the estimated robot pose to
  the active waypoint. This means the robot does not merely replay fixed motor
  timings: lateral/heading error causes the requested heading to change and the
  robot steers back toward the planned polyline.
- Heading errors above 18 degrees use a point turn. Smaller errors use the
  existing proportional differential driveOnHeading controller. Speed is
  reduced near the waypoint.
- Transit waypoints use a 120 mm arrival radius. Real-weight visit waypoints use
  a tighter 100 mm arrival radius.
- Live forward ranging remains a safety layer over the pre-laid map. If the
  robot sees an unexpected blocked path and no confirmed matrix opening, it
  holds position without aborting the mission. Once the live path clears it
  resumes and again aims at the active waypoint.

Mission completion and roaming handoff:

- Arena View includes an After weights selector with Balanced, Gap explorer and
  Conservative as fallback roaming modes.
- Every uploaded waypoint corresponding to a real weight carries a target flag.
- Firmware counts visited real-weight targets. As soon as every real weight has
  been visited, it stops the mission follower, switches navigation.strategy to
  the selected fallback roaming mode, enters seek_wall and continues with the
  existing autonomous coverage behaviour.
- The mission tab displays the live firmware mission pose and heading over the
  pre-laid arena while a run is active.
- Live status shows weights visited/total, waypoint progress and whether live
  ranging is currently holding the route.

New telemetry:

- mission.plan_valid
- mission.active
- mission.waypoint_index
- mission.waypoint_count
- mission.targets_visited
- mission.target_count
- mission.pose_x_mm
- mission.pose_y_mm
- mission.heading_deg
- mission.blocked
- system.navigation_controller_version = 9

Verification performed in this environment:

- ArenaView.py passes Python py_compile.
- The non-Qt MissionLayout/A* planner was executed directly with a synthetic
  horizontal arena containing two real weights, one dummy weight and a wall.
  It produced a valid obstacle-avoiding route containing exactly two target
  visit waypoints.
- A synthetic target inside unreachable border clearance correctly caused the
  planner to reject the mission.
- Home-base switching was checked: Green home makes Blue the no-go zone and
  Blue home makes Green the no-go zone.
- Firmware/header brace and parenthesis counts are balanced and the controller
  version, mission state and mission command paths are present.
- PlatformIO is not installed in the generation environment, so an actual
  Teensy compile and physical mission run are still required.

Required verification before a physical mission:

1. Run the normal PlatformIO Teensy build and upload controller v9.
2. Measure/set the real drive-wheel centre-to-centre track width before trusting
   the drawn live/discovered map. This handoff deliberately does not guess a
   replacement for the currently stored 40 mm value.
3. In the Pre-laid mission tab select the correct home colour, drag the start to
   the real starting position and set the actual starting cardinal heading.
4. Place at least one real weight plus any dummy weights/walls/ramps/tubes, then
   press PLAN ROUTE and visually inspect every cyan segment before running.
5. Keep STOP ready for the first physical test. Begin with one or two weights
   and a simple wall before testing a dense obstacle layout.
6. During the run verify mission.pose_x_mm / mission.pose_y_mm and the cyan live
   robot marker broadly agree with the physical chassis. Odometry calibration
   errors will directly affect waypoint following.
7. Confirm the final real weight changes mission.targets_visited to target_count
   and that navigation then changes to the selected fallback roaming mode.



2026-09-22 Pre-laid Mission Planner v10: placement guides, wall dimensions and live weight visibility
---------------------------------------------------------------------------------------------------

Base used:

- This change was applied on top of the previously delivered/working pre-laid
  mission v9 files, not on top of the older GitHub testing navigation source.
- The existing v9 mission upload protocol, A* planner, waypoint follower,
  live-obstacle hold behaviour, opponent-home no-go zone and fallback roaming
  behaviour were retained.

Mission Planner display/editor changes:

- The pre-laid editor is now presented as its own `Mission Planner` sub-tab in
  Arena View. Selecting it hides the live-map tuning panel, raw TOF panel and
  right-side Arena inspector so the planner gets the full Arena View working
  area.
- The Mission Planner canvas now supports:
  - mouse-wheel zoom;
  - middle-button or right-button drag to pan;
  - left-drag to move START, weights, dummy weights, walls, ramps and tubes;
  - a Reset view button;
  - live cursor x/y coordinates in arena millimetres.
- Seven evenly-spaced dashed vertical placement reference lines are drawn across
  the 4900 mm long arena. The lines divide the long dimension into eight equal
  bays and are labelled L1..L7 with their x-coordinate. They are graphical
  placement guides only and do not affect path planning.

Obstacle dimensions and A* behaviour:

- A selected wall/ramp/tube now has editable dimensions in the Mission Planner
  inspector.
- Wall and ramp dimensions are independently editable as length and
  thickness/depth. Range limits are 50..4000 mm for length and 50..2400 mm for
  thickness/depth.
- Tube size is edited as its diameter; its two stored dimensions stay equal.
- Wall/ramp rotation by 90 degrees is retained.
- Obstacle dimensions are saved in the persisted mission layout.
- Changing an obstacle dimension invalidates the previous route. If a route was
  already present, the GUI immediately replans it.
- The existing A* occupancy test already uses each obstacle's real rectangle or
  tube radius. It also inflates walls/ramps/tubes, dummy weights, the arena
  border and the opponent home by robot radius + configured safety margin.
  Therefore the displayed route now explicitly recalculates around different
  wall lengths/thicknesses rather than treating every wall as a fixed-size icon.
- Synthetic planner verification passed with a resized 180 x 800 mm wall and a
  tube between START and two real weight targets. The returned route deviated
  around the inflated wall rather than crossing it.

Live weight-visibility confirmation:

- Navigation controller telemetry version is now 10.
- Firmware now publishes:
  - `weight.detected`
  - `weight.sector_mask`
  - `weight.nearest_mm`
  - `weight.direction`
- Weight visibility is calculated from four matched bottom/top point-TOF pairs:
  far-left, mid-left, mid-right and far-right.
- A sector becomes a weight candidate when its bottom TOF sees an object from
  30..1200 mm and the matching top TOF sees at least 150 mm farther away.
- A sector must satisfy that condition for three fresh 100 ms point-TOF polls
  before it becomes visible. Re-reading one sample in the fast loop cannot
  produce a three-sample confirmation.
- Direction telemetry values are -2 far-left, -1 mid-left, +1 mid-right and +2
  far-right. The nearest confirmed sector supplies `weight.nearest_mm`.
- Mission Planner displays an always-visible status card:
  - WEIGHT VISIBLE with range/direction/active sectors; or
  - NO WEIGHT CURRENTLY VISIBLE.
- While mission pose telemetry is available, the planner also draws the four
  weight-sensor sector rays from the live robot pose. Active sectors are green.
- If a visible sector geometrically agrees with a planned real weight, that
  planned weight receives a green highlight ring. This is an operator aid only;
  it does not silently move or reclassify a planned weight.

Files changed:

- `PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.cpp`
- `PlatformIO/lib/BluetoothDebugWorkflow/BluetoothDebugWorkflow.h`
- `Non-Teensy-Files/BluetoothDebugGUI/ArenaView.py`

Verification completed here:

- `ArenaView.py` passes Python `py_compile`.
- MissionLayout was extracted and exercised independently of PyQt6.
- A* successfully planned around a resized wall/tube layout and retained seven
  placement reference lines.
- C++ and header brace/parenthesis counts are balanced.
- Expected v10 firmware tokens and weight-detection function/members are present.

Not verified here:

- PyQt6 is not installed in this environment, so the complete GUI regression
  suite could not be run.
- PlatformIO/Teensy toolchain is not installed here, so firmware v10 was not
  compiled in this environment.
- Physical weight visibility should be checked against the actual fitted outer
  and middle TOF angles before relying on the green planned-weight highlight.



2026-09-23 encoder distance calibration update
-----------------------------------------------
- Recalibrated both drive encoders from two new powered straight-line runs.
- Run 1: 2180 mm; encoder 1 changed by 25,450 counts and encoder 2 by 25,282 counts.
- Run 2: 2010 mm; encoder 1 changed by 24,210 counts and encoder 2 by 24,237 counts.
- Used combined measured distance divided by combined absolute counts rather than averaging the two per-run scale factors.
- New encoder 1 calibration: 4190 / 49660 = 0.08437374 mm/count.
- New encoder 2 calibration: 4190 / 49519 = 0.08461399 mm/count.
- Encoder sign/inversion is unchanged; encoder 1 still uses its existing inverted direction and encoder 2 its existing normal direction.
- BluetoothDebugWorkflow.cpp now uses the new constants for all encoder-distance navigation calculations.
- ArenaView.py now uses the same values for desktop odometry and includes a one-time `arena/dual_encoder_calibration_20260923` settings migration so an existing installation does not silently retain the older saved calibration.
- No wheel-track/yaw calibration was changed. The physical drive-wheel centre-to-centre track width still needs its own measurement.
- ArenaView.py passes Python syntax compilation after the change.

2026-09-23 known-arena live avoidance and landmark correction
------------------------------------------------------------
- The pre-laid mission now uploads a compact physical-feature map separately
  from its A* waypoints. The map contains the opposite home as a forbidden
  area and rectangles for configured walls, ramps, tubes and dummy weights.
- Arena View checks both command sizes against the Teensy's 768-byte receive
  buffer before sending; an oversized layout does not start navigation.
- During a mission, a frontal obstacle on the current travel leg can trigger
  a two-leg detour: move sideways into a verified clear ultrasound side, then
  pass the obstacle. Both legs must remain within the arena and clear of
  inflated known features. The robot then resumes the uploaded route. If no
  verified bypass exists, it stops and reports that the leg is blocked.
- Mission pose still integrates the BNO055 heading and calibrated drive
  encoders. Fresh side-ultrasound echoes are now compared with predicted ranges
  to the nearest uploaded physical feature or arena boundary. Three separate
  matching echoes allow a small, bounded position correction; mismatched or
  ambiguous echoes cannot make a large pose jump. This compensates gradual
  position error, including displacement caused by heading drift, but does
  not directly recalibrate the BNO055 yaw itself.
- Mission telemetry now includes `mission.avoid_phase`,
  `mission.landmark_corrections` and `mission.last_landmark_id`; Arena View
  shows detour/blockage state and the number of landmark corrections.
- New hardware-free `test_mission_navigation.py` covers feature uploads,
  command ordering, and rejection of an oversized upload.
- Verified: PlatformIO Teensy 4.0 firmware build succeeded; focused mission
  GUI tests passed (3 tests). The broader arena GUI test file has 15 existing
  failures/errors against mapping and sensor-count expectations and remains
  out of sync with current implementation. Physical navigation/landmark tuning
  has not yet been performed and the new firmware has not been uploaded.

2026-09-23 arm-sorting command repair
------------------------------------
- Root cause: `debug_config.json` exposed ARM SORTING ON/OFF, but the firmware
  had no handler for its `arm_sorting_toggle` action. The Teensy answered
  `Command action has no firmware handler` and no sorting sequence ran.
- Implemented ON/OFF handling and a nonblocking timing state machine using the
  debounced `inductive_proximity.detected()` value from D21, not raw pin level.
- Arming requires Debug Mode, Run state, a configured inductive input and a
  valid Herkulex 4 angle response. It stores the current angle as software
  zero, checks that both relative gate positions fit the absolute range, sets
  HX12K bumpers C=130/D=0 (OFF) and moves the gate to -30 degrees.
- A continuous detection for 500 ms sets bumpers C=0/D=130 (ON) and moves the
  gate to +30 degrees. After detection clears, bumpers remain ON for 2 seconds
  and the gate remains at +30 for 5 seconds. A renewed detection reasserts the
  bumper ON state. With no confirmed target, bumpers pulse ON for one second
  every 20 seconds. Turning sorting OFF restores bumpers OFF and gate -30.
- Stop Robot and leaving Debug Mode disarm sorting before output shutdown.
  Conflicting manual gate/HX12K commands are rejected while sorting is armed.
- Telemetry now includes `sorting.armed`, `sorting.detected`,
  `sorting.confirmed`, `sorting.bumpers_on`, `sorting.gate_angle_deg` and
  `sorting.idle_pulse` for checking the live sequence and recordings.
- Teensy firmware compiled successfully. Static app-to-firmware contract tests
  were added in `test_sorting_contract.py`. Physical actuation has not been
  tested or uploaded from this workstation in this change.

2026-09-23 navigation responsiveness repair after Morerun.rdbg
------------------------------------------------------------
- The latest recorded known-arena run contained repeated ~1.5-second main-loop
  gaps during synchronous SEN0628 8x8 data reads. The fixed 350 mm odometry
  rejection limit then discarded genuine encoder travel of roughly 0.8 m per
  delayed update, so the recorded mission pose stayed nearly stationary while
  the chassis drove toward a wall. The motor/heading trace also showed
  repeated high-power turns after the pose diverged.
- The 8x8 I2C frame acquisition now sends the same command as the supplied
  DFRobot library but polls for completion over successive loop iterations.
  A read no longer waits in the navigation/motor-control loop for the full
  sensor frame. Complete 64-zone frames retain the prior GUI wire format.
- Navigation now accepts only a recent 8x8 frame from a compatible heading
  and projects its ranges forward by measured encoder travel since that frame.
  This lets an approaching wall become nearer between slow matrix frames
  without changing the raw 8x8 values shown in the GUI and recordings.
- Mission odometry validation is proportional to elapsed time and checks both
  wheels; genuine travel through a long interval is integrated instead of
  silently dropped. Rejected steps are counted in
  `mission.odometry_rejected_steps` telemetry.
- An 8x8 broad-wall consensus is now an early confidence check, not an
  unconditional stop at its early-warning threshold. Actual front stopping
  uses the configured `navigation.front_avoid_mm`; a separate
  `navigation.matrix_wall_mm` GUI control/default of 400 mm sets the broad
  consensus threshold. Forward-facing top point TOFs are preferred, with
  outer top sensors used only when the forward pair is unavailable.
- Verified: Teensy 4.0 firmware compiles, and focused navigation/sorting GUI
  regression tests pass. These changes have not yet been uploaded or driven
  on the physical robot; verify loop gap, matrix freshness, odometry rejection
  count, and front-stop distance on a supervised run before relying on them.

2026-09-23 21:47 run review, pre-laid replay and mission controls
--------------------------------------------------------------
- Reviewed `RobotRun_2026-09-23_21-47-22.rdbg` (about 45 seconds). Its mission
  plan uploaded start (294,301), target visits (1876,544), (2471,1662), and
  (4590,355), plus non-target route corners (3100,700) and (3200,600). The
  local saved editor layout matches the three uploaded target positions and
  has three physical wall objects. The recording itself did not contain the
  original mission editor layout; the new visualiser reconstructs the older
  run from the actual `mission_map_set` and `mission_plan_set` commands and
  labels that approximation clearly.
- The far-left bottom sensor reported roughly 120-165 mm throughout metres
  of travel. It was angled outward toward the left wall, not proof of a weight.
  The old detector compared it with a differently aimed top sensor and marked
  the far-left sector continuously, including before navigation started.
  Weight visibility is now gated to within a 300 mm radius of a planned real
  weight, with a compatible bearing for that bottom sensor. The mission map
  upload carries the editable bottom sensor offsets/angles; a return matching
  a mapped physical wall or arena perimeter is excluded. `weight.map_wall_mask`
  records sectors excluded by the map. No planned weight means no TOF weight
  announcement, as requested. This affects TOF visibility, not the independent
  inductive sorting input.
- The first target was marked visited by odometry at about (1823,468), only
  about 93 mm from its (1876,544) waypoint, but the inductive sorting sensor
  never confirmed a pickup in the recording. The second was likewise marked
  visited around (2467,1564). Thus firmware's target counter means waypoint
  reached by estimated pose, not independently verified collection.
- On the approach near the third route corner, forward range fell from about
  485 mm to 209 mm, 160 mm and finally 87 mm while the old 150 mm front-stop
  tuning allowed continued mission drive. Mission tracking now uses a 400 mm
  minimum forward stop distance even when roaming is tuned closer. A blocked
  route with no map-checked safe detour stops instead of driving into the wall.
- The run's worst main-loop intervals were about 13-30 ms and no odometry
  steps were rejected, so the prior blocking 8x8 read fault was not present.
  However, 60 side-ultrasound landmark corrections occurred. The previous
  expected-range calculation incorrectly cast from the robot centre although
  both ultrasound transducers sit near the sides. The mission map upload now
  includes their editable offsets and uses each physical transducer location.
  Correction size is capped at 3 mm and at most once per side every 500 ms to
  limit cumulative pose drift from an imperfect wall match.
- New recordings embed the exact pre-laid editor layout and route in metadata
  and UI snapshots. DataVisualiser's new `Planned Arena` replay tab shows the
  planned cyan route, orange estimated actual trajectory, current blue pose,
  planned obstacles and target weights. The older 21:47 run was loaded and
  verified to display three target positions, three obstacles, five waypoints
  and its estimated travelled path without modifying the original recording.
- The Arena View has a `RETURN HOME` button. It plans a clearance-checked A*
  route from the latest valid mission pose to the centre of the selected home,
  uploads the map and route, and starts mission navigation. Firmware stops the
  motors at home instead of switching into roaming. It refuses to start if the
  pose/IMU is missing, robot is stopped, the pose lies inside mapped clearance,
  no safe path exists, or the command exceeds the serial buffer.
- The latest recording had 3-4 reported Bluetooth TX message drops and a few
  malformed telemetry signal names. Its trajectory and sensor conclusions
  above use intact named samples. No physical run of the new firmware has yet
  verified weight gating, map-aided localisation, collision margin or return
  home.
- Planned Arena playback now projects the recorded forward range (red ray)
  and the next expected physical wall on the pre-laid map (cyan ray) from the
  recorded pose and matrix mounting location. In the 21:47 run near the wall
  hit, it shows 87 mm observed versus approximately 638 mm expected. This is
  strong evidence of an unmodelled obstacle or pose/map disagreement, but
  the recording alone cannot distinguish which physical cause is correct.

2026-09-23 late runs: bottom TOF weight-detection correction
-----------------------------------------------------------
- Reviewed `RobotRun_2026-09-23_22-23-13.rdbg` and
  `RobotRun_2026-09-23_22-25-15.rdbg`. All four bottom TOFs stayed available
  without timeout. However, three channels often produced 8191 (an unusable
  range), while far-left repeatedly returned about 110-200 mm even away from
  planned weights. The earlier run had only three brief weight confirmations;
  the later run had none, despite two waypoint locations being marked visited.
  The waypoint counter is based on estimated pose, not TOF or pickup proof.
- The old detector required the robot centre to be within 300 mm of a planned
  weight and the centre-to-weight bearing to match a bottom sensor angle within
  28 degrees. This ignored the editable sensor mounts approximately 255-305 mm
  ahead of the robot centre, rejecting plausible returns. It now transforms
  each bottom TOF mounting point and its measured ray into mission coordinates
  and requires the measured hit point to land within 300 mm of a planned real
  weight. The paired top/bottom 150 mm height gap, three 100 ms confirmation
  polls, and mapped physical-wall rejection remain in place. A mission still
  needs uploaded bottom sensor geometry and a valid pose before announcing a
  weight; no geometry or plan does not fall back to an unlocated detection.
- New telemetry masks `weight.valid_mask`, `weight.gap_mask`, and
  `weight.target_mask` expose which sectors pass each stage; existing
  `weight.map_wall_mask` and `weight.sector_mask` show wall rejection and final
  confirmation. `weight.geometry_valid` shows whether the upload succeeded.
  Arena View displays a concise reason when no weight is currently visible
  and labels the mission counter as locations reached, not sensor-confirmed
  weights.
- Teensy firmware builds successfully. The focused mission/weight GUI tests
  pass (6 tests). The broader existing arena test file has 15 unrelated
  failures/errors from outdated expectations about current mapping and extra
  XSHUT6/7 sensors; those tests were not changed in this correction. The fix
  has not yet been flashed or validated with a physical weight run.

2026-09-23 22:48 Test.rdbg mission-stall and weight-bearing review
----------------------------------------------------------------
- The newest run `Non-Teensy-Files/DataVisualisationAfter/Data/Test.rdbg`
  lasts about 76 seconds. Its mission map has three real weights and three
  physical wall rectangles. At roughly 45-62 seconds the robot remained near
  (2859,1151) mm with heading about 311 degrees, front range around 340 mm,
  8x8 centre/near-side ranges around 275-400 mm, and both commanded motor
  percentages zero. The event log says the mission leg was blocked, but the
  old local detour tested only fixed 500 mm side steps, required a valid side
  ultrasound return, and found neither side map-clear. The left ultrasound
  was invalid/zero through most of the stall, so the old code never tried
  the visible left-side opening as a turning route.
- The uploaded non-target waypoints (3100,700) and (3200,600) lie inside
  the firmware's inflated clearance around a mapped wall, even though the
  next real weight at (4590,355) is reachable. A visibility-graph check from
  the recorded stall pose finds a clear path via about (3066,572) before
  continuing to the weight. Firmware now skips non-target routing corners
  inside its actual clearance and, when a map segment is blocked, inserts
  map-checked intermediate corners from the current pose. It preserves the
  real target, and logs/counts both reroutes and skipped invalid corners.
  The old live-obstacle side-step remains as a fallback for unmapped objects.
  A needed point turn is allowed while the forward wall is close, but the
  wall is checked again before the next forward move.
- The run briefly reported far-left low-object sectors near the first two
  planned weights. Those measured hit points were about 244 and 284 mm from
  the corresponding target centres, consistent with the persistent far-left
  background echo. The later mid-right return at about 70 seconds had a
  bottom range near 99 mm, top range near 468 mm, and a measured hit about
  149 mm from the last weight. Previously mission steering ignored all
  `weight.direction` and continued aiming only at the planned coordinate.
  It now uses a confirmed hit associated with the active weight waypoint if
  its map-space error is <=170 mm, the observed hit and path are map-clear,
  and the reading is fresh (<=2.5 seconds). The robot point-turns toward the
  measured hit, then still obeys forward-wall avoidance. Broad 300 mm weight
  visibility remains separate from the tighter steering gate.
- New telemetry includes live current waypoint coordinates, mission reroute
  and skipped-corner counts, and whether measured weight-bearing steering is
  active, with the hit coordinates/error while fresh. Arena View and Planned
  Arena replay show the active waypoint in yellow and an active measured
  weight bearing in purple. The mission status explains reroutes/skips. The
  GUI now calls a broad 300 mm sector return a possible low object and only
  labels the tighter map-checked approach as a verified weight bearing.
- The Teensy build succeeds and the 6 focused mission GUI tests pass,
  including off-screen rendering of the new overlays. The replay tests pass
  their 5 assertions but their PyQt process exits with a Windows access
  violation during teardown; that issue remains separate. The new firmware
  has not been uploaded or physically tested, so the reroute and weight
  approach still require a supervised run with the current arena layout.

2026-09-23 23:37 Test2.rdbg frontal-weight approach correction
--------------------------------------------------------------
- The newest ~120-second recording `Non-Teensy-Files/DataVisualisationAfter/Data/Test2.rdbg`
  shows the first weight waypoint (2147,517) stalled near pose (1735,410),
  heading 6.5 degrees, with a 332-335 mm frontal return and both drive motors
  commanded zero. Its target is about 426 mm from the robot centre; the
  bottom-pair target mask is zero. A similar ~20-second stall occurs near
  (2283,1693), heading 72.5 degrees, on the second waypoint (2445,2042):
  the front return is ~347-349 mm, both motors are zero, and a far-left low
  return is visible but its estimated hit misses the narrower steering gate.
  This is not simply a missing bottom TOF: the fixed 400 mm mission front
  stop also prevents driving the last few hundred millimetres to a weight.
- The final leg at ~119 seconds is a useful counterexample: the third target
  is ~705 mm away while the forward return is 105 mm. This should stay
  blocked, because that echo does not match the target's expected range.
- Firmware now allows a close target leg only for a real, current weight
  waypoint within 700 mm and a map-clear direct route. That final route uses
  a 225 mm centre clearance (215 mm chassis half-width plus 10 mm), rather
  than the general rerouting clearance of 305 mm. The first planned target
  lies about 257 mm from a mapped wall, so the general clearance incorrectly
  considered its endpoint obstructed even though the chassis fits.
- It compares a forward return with the expected target range and checks
  the mapped physical wall beyond it. A matching target or a mapped-wall
  echo enables an 80% drive approach. The robot can drive through a short
  weight return but must stop if the mapped front wall is under 100 mm away;
  an early, unmatched forward echo keeps the usual 400 mm mission stop.
  The target arrival radius is reduced from 100/130 mm to 70 mm. This is
  commanded power, not a measured motor speed or proof of pickup.
- New telemetry `weight.front_target_visible`, `weight.target_commit_active`,
  `weight.front_target_range_error_mm`, and
  `weight.front_target_mapped_wall_mm` exposes the new gate. Arena View labels
  the approach distinctly from bottom-TOF confirmation. Teensy build and
  six focused Arena View tests pass. No firmware upload or physical pickup
  verification has occurred yet; monitor these four telemetry fields and
  actual front-wall clearance during a supervised test.

2026-09-24 RobotRun_2026-09-23_23-55-08.rdbg review and mission changes
-----------------------------------------------------------------------
- The latest ~283-second recording contains one weight mission and several
  manually restarted return-home routes. The first and second weight sites
  were marked visited at ~26 and ~38 seconds solely because the estimated
  pose entered their arrival radii. There were zero confirmed bottom-TOF
  `weight.detected` samples during either approach, so the old logs were
  not pickup evidence. The third planned target at (4590,355) stalled at
  pose roughly (4505,691), heading 287 degrees, with forward readings
  ~331-345 mm and motors commanded zero. Its forward target-range error
  was ~134-148 mm, just outside the prior 130 mm match gate; that gate is
  now 170 mm while still requiring a map-clear route and a mapped wall
  behind the possible target.
- After a return-home restart, the robot's estimate settled at about
  (863,663) mm, heading 205 degrees; the observed physical position supplied
  by the operator is about (315,878) mm, a roughly 590 mm discrepancy.
  Front range was ~350 mm, left ultrasound ~92 mm, right ultrasound
  ~880 mm, and both drive commands were zero for over two minutes. The
  uploaded physical wall rectangles did not explain a direct obstruction
  on the estimated straight line to home. Existing side-landmark trimming
  could not correct this: it excludes the 92 mm left reading, rejects side
  residuals above 110 mm, and adjusts by at most 3 mm per match. The live
  detour tried only a 500 mm lateral then 650 mm forward offset, which did
  not fit inside the map clearance at this arena edge.
- Firmware now performs a bounded search at each real weight site before
  counting it as checked: a first rear-left/rear-right sweep and return
  through the site, plus a shorter second sweep when a confirmed target
  persists or coverage is insufficient. It dwells to gather readings at
  each reachable sample position, stops after 20 seconds, and records both
  checked and incomplete sites separately. It does not claim a pickup
  without an actual pickup sensor. Telemetry includes search state, step,
  samples and valid-range coverage. Return-home waypoints do not invoke
  weight search.
- For a blocked return-home leg, firmware holds still briefly and checks
  whether a coherent front 8x8 wall return AND an independent valid side
  ultrasound can both match known physical surfaces at a nearby candidate
  pose. Three consistent candidate scans are required before correcting
  the estimated pose. This provides a larger-drift recovery in addition
  to the existing small side-landmark trims; it cannot guarantee absolute
  location when the measurements or uploaded wall map are wrong. New
  telemetry reports recovery count and match residual.
- Live obstacle detours now try map-clear 250/350/500 mm lateral and
  250/400/650 mm forward combinations instead of only 500/650 mm. Return
  home permits up to six attempts. The firmware's map clearance for return
  home uses half the configured robot width plus 10 mm, matching the close
  physical corridor rather than an arbitrary 305 mm inflation; live range
  blocking still applies. Arena View offers an explicit checkbox to use
  a manually marked START position for RETURN HOME when the robot's actual
  centre is known to differ from odometry. Its IMU heading remains live.
  The checkbox is off by default; do not mark a position without observing
  the robot there.
- Controller version is now 11. The Teensy firmware builds successfully
  and seven focused Arena View tests pass, including a manual return-home
  start at the reported (315,878) mm location. These changes have not been
  flashed or physically validated. In the next supervised run, check
  `mission.search_*`, `mission.landmark_recovery_*`, actual pickup and
  wall clearance; do not infer successful collection from the site counter.

2026-09-24 00:23 and 00:26 run review; post-target pose and approach alignment
--------------------------------------------------------------------------
- Reviewed RobotRun_2026-09-24_00-23-08.rdbg and
  RobotRun_2026-09-24_00-26-42.rdbg. The first recording explicitly uploads a
  `return_home:true` mission plan at about 142 seconds. The second has no
  return-home command. Both runs mark the last weight site checked without
  independent pickup verification and then switch from strategy 3 (mission)
  to fallback strategy 0 (roaming). That switch was not an automatic home trip.
  The GUI's `return_home:true` plan is emitted by its RETURN HOME handler;
  the recording cannot establish whether the button was clicked deliberately
  or inadvertently.
- At the switch, `mission.pose_x_mm`, `mission.pose_y_mm`, and
  `mission.heading_deg` stopped changing while encoder counts continued to
  change. For example, the second recording stayed at approximately
  (4520,315) mm from 100 to 120 seconds while encoder 1 moved from roughly
  -352308 to -398476 counts. The cause was that encoder/IMU pose integration
  existed only inside the mission-navigation branch.
- `updateMissionPose()` now runs after sensor sampling on every control loop,
  including fallback roaming, navigation stopped, and manual driving. It
  retains the calibrated encoder scales, IMU heading, and impossible-step
  rejection. It only initialises from the uploaded start pose when a planned
  mission actually begins. `mission.pose_live` reports whether the pose is
  initialised and the IMU sample is valid. Existing landmark trimming still
  applies only during the planned mission; post-mission coordinates remain an
  odometry estimate, not guaranteed absolute localisation.
- On a real weight approach within 700 mm, the controller now centres the
  planned weight/bottom-TOF bearing to within 4 degrees, holds still for
  250 ms to settle, then advances at the existing 80% collection speed. It
  stops and re-aligns if heading error exceeds 7 degrees. Small turns use
  motor pulses because below-75% output is unreliable. The state appears in
  `mission.target_aligned`. The recordings did not provide a trustworthy
  bottom-pair bearing or an independently verified pickup for the last site;
  this alignment is to the mapped target unless a fresh sensor-derived
  bearing is available. A supervised physical run is required to verify
  centring and collection.
- When a current weight-site hit is confirmed by the lower-vs-upper TOF
  height gap, matches that site's 170 mm association tolerance, and has a
  mapped-clear approach, firmware captures the measured hit as a fixed vector
  from the current robot pose. It turns to that bearing and drives along it
  through the hit, aiming 180 mm past it where the mapped chassis clearance
  permits, otherwise trying 140 or 100 mm. If none of those extensions fit,
  it may drive only to the hit. Front live ranging still blocks an unsafe
  approach. It then resumes the site sweep; crossing the estimated hit does
  not prove the weight was collected. Recording telemetry adds
  `weight.vector_*` fields for the hit, end, heading, progress, and extension.
- This environment cannot run the Teensy build (PlatformIO unavailable) or
  the GUI regression suite (PyQt6 unavailable). Firmware has not been
  uploaded from this change. Controller version is now 12. Compile/upload and inspect the live pose after
  the final site before relying on the new behavior.

2026-09-24 00:51 and 00:55 weight-approach review; inner-pair centring
--------------------------------------------------------------------
- Reviewed RobotRun_2026-09-24_00-51-55.rdbg (159 s) and
  RobotRun_2026-09-24_00-55-06.rdbg (189 s), including logs, commands,
  bottom TOFs, frontal range, pose, alignment flags, and motor commands.
  These runs used the sensor-vector firmware above. Most confirmed weight
  returns came from the far-left (mask 1) or far-right (mask 8) bottom TOF;
  the two inner sensors rarely confirmed a low object simultaneously.
- A far-left-only vector at about 19 s in the first run aimed at approximately
  (2064,378) mm, although the planned weight was (2147,517) mm. The 162 mm
  discrepancy passed the old 170 mm association gate but steered left of the
  weight, matching the reported right-side miss. The last vector of that run
  similarly placed its hit roughly 150 mm from the planned site. Outer-only
  hits are therefore treated as candidates, not precise drive-through lines.
- At about 89-91 s in the first run, the last weight vector remained active
  but the motor commands were zero: forward range was about 345-362 mm and
  the predicted mapped wall about 516-517 mm away. At about 139-140 s in the
  second run, the last vector was only 165 mm through its planned travel and
  the motors again sat at zero with a 364 mm front return. The old 400 mm
  transit stop was winning when its strict target/wall range match failed.
- Controller v13 now allows a confirmed low object near the current site to
  proceed through a map-backed front corridor when the mapped wall is behind
  the measured return, retaining the live 100 mm wall stop. An already
  acquired vector tolerates up to 180 mm front-vs-map residual while staying
  aligned. This is less likely to strand an approach 350-400 mm from a weight,
  but still requires supervised verification of map accuracy and wall margin.
- A close inner pair is now the collection centring cue. Within 350 mm of
  the planned site (or 500 mm with fresh confirmed low-object evidence),
  when both inner bottom TOFs read at most 200 mm, the
  robot pulse-turns until their ranges agree within 30 mm. It then freezes
  the current heading, estimates the weight's forward position using their
  uploaded sensor offsets/angles, and sends straight through that location
  at 80%, aiming up to 180 mm beyond it where the mapped footprint clears.
  It no longer retargets from subsequent side-sensor readings during this
  short send; the IMU still holds that frozen heading against drivetrain
  drift. Centring is bounded to 2.5 s; non-convergence falls back to
  the site search instead of spinning indefinitely. Search travel is reduced
  from its prior 78-88% range to 76%, above the drivetrain's observed 75%
  minimum usable command.
- Arena View now retains a marker for each sensor-estimated weight location.
  Orange means possible outer-sensor candidate; purple means committed
  drive-through location. Markers remain on the planned-layout canvas after
  live detection disappears and are also projected into the discovered-map
  view at the time of detection. A new pre-laid RUN NAVIGATION clears prior
  run markers; RETURN HOME does not. Recording telemetry includes inner left and
  right ranges, the lock state, bearing sector/freshness, and the vector's
  waypoint index; the GUI test verifies marker persistence.
- Verified: PlatformIO Teensy 4.0 v13 build succeeds (177080 bytes code), and all eight focused
  Arena View navigation tests pass. The physical robot has not been flashed
  or driven with v13 yet; validate inner-pair signs, centred entry, front
  wall clearance, and actual weight pickup in a supervised run.

2026-09-24 evening: two first-site runs and turn oscillation
- `RobotRun_2026-09-24_22-12-19.rdbg`: autonomous mission began at 9.5 s,
  reached the first target vicinity, entered its 20 s site search at 18.5 s,
  and marked that search incomplete at 38.5 s. During the search the requested
  bearing sat near 204 degrees while the IMU hovered on the opposite side of
  the circle (roughly 18-35 degrees). A target almost exactly 180 degrees away
  has two equally short turn directions; small pose changes made the old
  controller alternate full-power A/B turn commands. Estimated X also drifted
  about 300 mm while turning, shifting the search target still further.
- `RobotRun_2026-09-24_22-16-51.rdbg`: inner-bottom centring and a weight
  drive-through occurred at 16.6 s; the same site search started at 18.8 s and
  timed out at 38.8 s. After about 17 s the Bluetooth telemetry became
  fragmented (only partial roughly 900-byte telemetry fragments are decoded),
  so later motor/pose readings from this recording cannot independently prove
  the precise oscillation. TX-drop counters were already rising by one frame
  per second; this is a separate link/recording issue still to investigate.
- Firmware now latches the chosen turn direction across the ambiguous
  180-degree boundary, coasts before reversing it, and pulses smaller turns
  rather than continuously commanding high torque. Encoder translation is
  suppressed while the motor bank is commanded to point-turn, preventing
  unequal encoder slip from inventing forward travel during the search.
- After a completed sensed-weight drive-through, the robot now stops for one
  second, makes a slow 30-degree-left/right/centre scan in place, and retries
  the sensed location if it still sees a valid weight bearing. Retries are
  bounded to two; if still seen but a retry is unsafe it falls back to the
  existing site search. With no weight seen, it advances to the next site.
- Normal Arena View mission uploads now include the chosen home centre.
  After every planned weight site has been checked, firmware appends a
  return-home leg using its live pose and existing map rerouting. On arrival
  it turns toward the arena interior and starts the selected fallback
  exploration strategy. The separate manual RETURN HOME command still stops
  at home. Existing plans lacking home coordinates retain their old fallback.
- The updated firmware compiles for Teensy 4.0 and all eight focused mission
  GUI tests pass. These are not hardware validation: upload the new firmware
  and run under supervision before relying on the collection check or
  automatic return-home behaviour.

2026-09-24 late run and sorting/collection tuning
- `RobotRun_2026-09-24_22-58-23.rdbg` ran for 163 s with arm sorting on.
  It made several blocked-leg detours between about 16 and 57 s, searched
  the first weight site from 84 to 104 s and the second from 117 to 137 s.
  Neither site produced a confirmed bottom-TOF weight sector/inner-pair lock
  in recorded telemetry; both searches timed out. The inductive sensor did
  confirm collection at 124 s during the second search, however. The plan
  then initiated automatic return home at 137 s and inserted a map reroute
  at 143 s; recording ended before arrival. The stop/go jitter aligns with
  front-ranging near the 400 mm mission threshold and transitions between
  point turns and forward driving. This run did not exercise a centred
  drive-through, so its pickup geometry still needs a physical check.
- Mission steering now has point-turn hysteresis (start above 24 degrees,
  continue until within 12 degrees), and front blocking remains latched
  until a range at least 80 mm clearer has remained stable for 300 ms.
  This reduces flip-flopping on a noisy threshold without reducing the hard
  100 mm wall limit. Both inner bottom TOFs may now differ by up to 50 mm
  before collection locks, instead of 30 mm. Close target heading settling
  is relaxed from 4/7 degrees to 10/14 degrees and 180 ms dwell.
- Once the inner pair centres a weight, the frozen-heading approach uses
  75% motor command. After reaching the vector end it continues at 75% for
  up to two seconds to feed the item into the mechanism, but stops sooner
  if mapped clearance ahead or a valid front return of at least 150 mm is
  lost. A newly confirmed inductive detection completes the current weight
  site immediately instead of continuing its search or post-pass scan.
- Arm sorting assumes the physical gate is at reject when armed, reads
  Herkulex 4 there as relative zero, commands reject at 0 degrees and
  accept at +60 degrees, and verifies that +60 is inside servo range.
  Explicit STOP NAVIGATION disarms sorting, turns bumpers off and commands
  reject. STOP ROBOT stops propulsion/other servo torque first, parks the
  gate at reject, then releases gate torque after its 500 ms motion. These
  positions and stop sequencing need a supervised physical check. The
  updated firmware builds successfully; it has not been uploaded here.

2026-09-24 turn-smoothness follow-up
- `RobotRun_2026-09-24_23-20-02.rdbg` completed the two planned site checks;
  the first search timed out, but an inductive pickup at 97 s immediately
  completed the second site. It returned toward home and then made a live
  detour at about 126 s. The recording ended during that return. The turn
  from about 126-141 s took a long sequence of brief motor-on/motor-off
  steps while target heading remained near 250-270 degrees.
- `RobotRun_2026-09-24_23-23-35.rdbg` repeated route planning several times
  near 24-30 s. During its first site search the requested heading changed
  sharply between search legs; a roughly 45-degree correction from about
  44-54 s took 10 seconds of pulsed rotation. These are deliberate point
  turns made visibly jerky by the old 75 ms on / 225 ms off pattern starting
  as far as 55 degrees from target. Route detours and search-step changes
  also create some genuinely large heading requests.
- Firmware now uses continuous minimum-reliable turn power for most of a
  heading change. It coasts for 350 ms on entering the final 35-degree
  region, then uses short 65 ms finishing pulses only near the target.
  A target-locked collection leg bypasses a stale generic point-turn state
  and drives on its locked heading. The Teensy 4.0 build passes; turn quality
  still requires an uploaded firmware and a supervised physical run.

2026-09-26 weight approach, missed-pickup recovery, and live mission state
- The two latest examined recordings are `RobotRun_2026-09-26_15-27-13.rdbg`
  and `RobotRun_2026-09-26_15-30-15.rdbg`. Neither recorded an inductive
  pickup during the examined collection pass. In the first run, a single
  bottom-TOF bearing caused a vector drive at about 86 s; the inner-pair lock
  stayed false while the robot's stored path diverged from the sensed hit by
  over 100 mm. The second run repeated this at about 42 s: the vector hit was
  near (1664,1966) mm; by 46-47 s the pose ran about 70-80 mm sideways of
  that frozen line, despite `mission.target_aligned` remaining true. The old
  14-degree heading tolerance and weak steering at 75% allowed that miss.
- A pair of similarly short inner TOF ranges is no longer automatically
  treated as one centred weight. Both height/target-qualified inner sectors
  must identify projected hit points within 160 mm of each other before
  fused centring can lock; the two inner sensor rays point outward and may
  be seeing different objects at equal distances. Sensor vectors now apply
  lateral cross-track correction and a 4/5-degree heading tolerance rather
  than continuing a long collection pass while 8-14 degrees off heading.
- After a drive-through with no inductive confirmation, the robot waits one
  second for sorting, then (if the uploaded map shows a clear rear corridor)
  backs up to 120-240 mm before the sensed hit and re-approaches. Reverse
  distance is encoder-bounded and time-limited to 4.5 seconds; retries are
  limited to two. If there is no clear reverse corridor it falls back to the
  existing scan/search. STOP NAVIGATION still stops propulsion immediately.
- The latest run had repeated right-ultrasound readings of 30-59 mm and
  top far-right TOF readings around 150-200 mm near 160-164 s, while stored
  mission pose was approximately (2065,1860) mm and the photographed robot
  was against the corresponding side wall. The old landmark trim discarded
  side-ultrasound readings below 180 mm and never recovered that drift.
  Valid side-ultrasound ranges now remain usable at close distance. When a
  repeated close return geometrically matches an uploaded perimeter wall,
  the pose can be corrected in bounded steps; a nearby top TOF shortens the
  confirmation but is not required. This changes map association, not the
  reliability assigned to the measured ultrasound distance.
- Firmware telemetry now includes `mission.phase` (following path, searching,
  centring, collecting, checking pickup, reversing for retry, avoiding,
  returning home, etc.) and reports navigation-controller version 14. Arena
  View shows this as a prominent state banner on both live-map and mission
  planner views. The Teensy 4.0 build succeeds and the focused navigation/
  replay tests report 16 passing tests. The new firmware has not yet been
  uploaded or checked with a physical collection run.

2026-09-26 — Mission pickup attribution, home docking and run controls
- Analysed RobotRun_2026-09-26_15-54-44.rdbg and
  RobotRun_2026-09-26_15-58-54.rdbg. The first had no inductive pickup. In
  the second, the first site search timed out at about 64.3 s after repeatedly
  being blocked, while the successful inductive pickup arrived at 81.3 s.
  Firmware had attributed that delayed pickup to the *next* site because its
  route point was within 500 mm, incremented the visited count, and returned
  home without searching that second site. The inductive sensor is direct
  evidence of successful collection, not a false detection.
- Firmware now remembers a recently incomplete site. A later inductive
  confirmation is credited to that site unless an active collection/verify
  pass establishes the new one. It no longer advances past the next planned
  site merely because that site's waypoint is within 500 mm. After pickup it
  attempts a 1.2 s straight 75% roll-on, only if the forward sensor and
  uploaded corridor are clear. A blocked weight-site search now tries another
  sweep after 1.2 s instead of waiting motionless for the entire timeout;
  the search window has been extended to 28 s.
- Mission travel paths now round A* corners into short, map-checked curved
  polylines. Weight collection's final straight approach is not rounded.
- GUI manual return-home and firmware automatic return-home now look for a
  map-clear staging point with turning clearance and a straight corridor to
  home. At that point the robot turns to face outward, then reverses into
  home at 75% while checking IMU heading and mapped corridor. If staging,
  heading, IMU or the corridor is invalid, navigation stops. The robot has no
  rear range sensor, so reverse docking remains dependent on map accuracy;
  verify at low risk before trusting it near movable obstacles.
- Mission Planner has START RUN / STOP RUN and a visible elapsed timer. START
  RUN uploads the planned mission, enables robot run mode, arms sorting,
  commands both first-bank motor channels to +100%, then starts navigation.
  STOP RUN sends STOP (which disarms sorting and neutralises both banks).
  The timer also starts from RUN NAVIGATION and freezes on STOP NAVIGATION or
  STOP MOTORS. Hardware command acknowledgement is asynchronous: watch the
  robot status/log for an arm-sort failure before leaving a run unattended.
- Validation: Teensy 4.0 PlatformIO firmware build succeeded; all 12 focused
  test_mission_navigation.py tests pass, including new run-button and docking
  route checks. test_arena_view.py has older unrelated expectations (for
  example six sensors versus eight configured) and is not currently green.
  This firmware was not uploaded or physically exercised in this turn.
